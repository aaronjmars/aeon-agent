self-improve: hardened the feature skill (lowest success rate, 0.85) against the compound-bash sandbox trap. Its temp-dir workflow invites 'cd /tmp/... && cmd' chains, which the sandbox auto-denies — the exact pattern in its last logged failure. Added a Sandbox Note warning. PR: https://github.com/aaronjmars/aeon-agent/pull/115
