## Upstream sync: `aeonfun/aeon` `21b82db..95142d1`

**25 commits** (2026-09-07 → 2026-09-12) · **44 applied** (15 added · 27 updated · 2 3-way merged) · **10 manual** (7 conflicts · 3 new skills deferred) · baseline → `95142d1`.

Catalog (`skills.json`, `packs.json`) and `eyebrowlock.json` were reconciled by hand for the changed **existing** skills, because the `bin/generate-*` generators and the `eyebrow` binary can't run in the headless skill sandbox. See "Green-PR notes" below.

### Applied cleanly

- **Modified skills:** `deploy-uni-hook` (mandatory 10 bps AeonFee on every hook + templates incl. new `AeonFee.sol`), `vuln-scanner` (Riva research kernel + `riva.md`; bounded trufflehog scan), `competitor-monitor` (opt-in table-row diffing), `changelog`, `github-trending`, `aeon-update` (eyebrow version now derived from CI).
- **Scripts / harness:** `harness-adapter/lib/sandbox.sh` (keep `memory/` + `output/` writable in read-only sandbox), `scripts/competitor-monitor.mjs`, `scripts/stage-deploy-uni-hook.sh`, + new `scripts/build-vuln-context.sh`, `compare-riva-shadow.sh`, `git-push-retry.sh`, `resolve-riva-capabilities.sh`, `validate-riva-candidates.sh`, and 8 new `scripts/tests/test_*.sh` (riva / read-only / chain-no-action).
- **Workflows:** `.github/workflows/chain-runner.yml` (no-action telemetry).
- **Auto-merged (3-way):** `apps/webhook/package.json` + `package-lock.json` — your local overrides kept; upstream's `sharp` override + dep bumps applied.
- **Skill manual (claude + plugin):** `.claude/skills/aeon/**` and `plugin/skills/aeon/**` reference edits.
- **Docs / other:** `docs/ECOSYSTEM.md`, `scripts/tests/test_vuln_scanner_status.py`.
- **Derived (regenerated, not copied):** `catalog/skills.json` (`deploy-uni-hook` files 11→12 + new description; `vuln-scanner` files 0→1), `catalog/packs.json` (deploy-uni-hook description), `eyebrowlock.json` (6 entries swapped verbatim from upstream HEAD).

### ⚠️ New skills deferred (need generators that can't run headless)

Three brand-new upstream skills are **not** in this PR. Auto-installing them needs `catalog/skills.json` + `packs.json` + `skill-icons.json` + `apps/dashboard/lib/skill-icons.data.ts` + a per-skill SVG regenerated by `bin/generate-skills-json` / `generate-packs-json` / `generate-skill-icons` (node/bash, blocked in this sandbox), plus an `eyebrowlock.json` entry from `eyebrow scan`. Deferring keeps this PR green rather than landing it CI-red. To add each:

```bash
bin/add-skill aeonfun/aeon compute-resell    # then miroshark-matchday, submit-hook
bin/generate-skills-json && bin/generate-packs-json && bin/generate-skill-icons
node scripts/gen-agents-md.js || true
eyebrow scan --path . --lockfile eyebrowlock.json
# then enable in aeon.yml (upstream ships all three disabled):
#   miroshark-matchday: { enabled: false, schedule: "0 9 * * 5", var: "" }
#   compute-resell:     { enabled: false, schedule: "15 6,14,18 * * *", var: "" }
```

- `compute-resell` — multi-provider compute reselling on Surplus (#1036).
- `miroshark-matchday` — weekly football matchday sims + video handoff (#1056).
- `submit-hook` — Uniswap v4 hook marketplace submission (#1040).

### Needs manual review (conflicts — your local copy diverges from upstream)

These were **not** touched; your local versions stand. Upstream changed the same regions you customized.

- `.github/workflows/aeon.yml` — read-only-guard log fixes (#1051/#1052) + no-action telemetry (#1053) landed on lines your fork narrowed. Also adds `miroshark-matchday` + `compute-resell` (both `enabled: false`) to `skills:` — fold in when you install those skills.
- `.github/workflows/ci-tests.yml` — upstream adds test steps for the new riva / read-only / chain-no-action suites (**those test files ARE applied here**, they just aren't wired to run until you merge these steps) + adds `aeon.yml` to trigger paths (#1058).
- `CHANGELOG.md` — upstream appended #1034/#1039/#1040 entries; your fork's changelog diverges.
- `apps/dashboard/lib/skill-icons.data.ts` + `docs/assets/skill-icons/cortx-reliability.svg` — generated icon assets; resolve by running `bin/generate-skill-icons` after installing the new skills.
- Carried from prior runs (upstream unchanged this window): `.github/workflows/messages.yml`, `.github/README.md`, `docs/skill-packs.md`, `llms.txt`, `skills/skill-health/SKILL.md` (still needs #1018 accept + `eyebrow scan`).

### Operator config changed upstream (not auto-applied)

- `aeon.yml` — see the two new `enabled: false` skill rows above; merge them if/when you install the skills. Your enable/schedule/model choices are untouched.

### Green-PR notes (how the derived files were reconciled without the binaries)

- **`eyebrowlock.json`:** the 6 changed **existing** skills are byte-identical to upstream HEAD on this branch, so their upstream-HEAD lock entries (v0.4.2, deterministic) were swapped in verbatim. Policy is `allowContentDrift: true` (fails only on capability-expansion / critical), so `.claude/skills/aeon` (caps `{}`) needed no swap, and the coverage gate passes (84/84 skills have entries).
- **`catalog/*.json`:** only `deploy-uni-hook` and `vuln-scanner` changed; their dir file-sets are byte-identical to upstream HEAD, so the two entries' semantic fields equal a fresh regen (git-derived `sha`/`updated` are normalized out by `ci-skills-json`/`ci-packs-json`). `total` stays 84 (no skill added).

### Upstream commits

| SHA | Summary |
|-----|---------|
| f96beb9 | docs(changelog): run site formatter after edit (#1034) |
| 487b23f | feat(deploy-uni-hook): enforce mandatory 10 bps AeonFee (#1035) |
| 3f3509c | feat(skills): add compute-resell (#1036) |
| 162bf20 | fix(egress): stop iron-proxy MITM redding artifact uploads (#1038) |
| 44ddb43 | fix(aeon-update): derive eyebrow version from CI (#1037) |
| 0154c0e | Port submit-hook marketplace publish to canon (#1040) |
| 63078b4 | docs: sync PRs #1034-#1040 (#1041) |
| 51ccf0c | fix(harness): keep memory/ and output/ writable in read-only sandbox (#1042) |
| c28ab4c | feat(competitor-monitor): opt-in table-row diffing (#1043) |
| 7e1c6ad | chore(deps): bump hono in /apps/mcp-server (#1044) |
| 58b2e53 | chore(deps-dev): bump js-yaml in /apps/webhook (#1046) |
| 4d9e42a | chore(deps): bump js-yaml (#1047) |
| c554e8b | chore(deps): bump sharp in /apps/dashboard (#1048) |
| a3d402c | chore(deps-dev): bump js-yaml in /apps/dashboard (#1049) |
| 5d005b6 | chore(deps): override sharp in apps/webhook (#1050) |
| 5573848 | fix(aeon): read-only guard stub loses memory/logs baseline (#1051) |
| 3ffe60d | fix(aeon): commit a read-only skill's failure log (#1052) |
| 19aafe6 | feat(vuln-scanner): add Riva research kernel (#1039) |
| 244f7ac | fix(chain): distinguish no-action telemetry (#1053) |
| 90c369f | fix(vuln-scanner): bound the trufflehog fs scan (#1054) |
| d1ca7e7 | fix(vuln): keep github-trending feed machine-parseable (#1055) |
| f891ede | Add miroshark-matchday skill (#1056) |
| 3fa3bef | fix(tests): tolerant vuln-scanner status test (#1057) |
| 9bf02fb | docs: update Hunch logo image |
| 95142d1 | fix(ci): add aeon.yml to ci-tests trigger paths (#1058) |

🤖 Generated with [Claude Code](https://claude.com/claude-code)
