🔴 FAILED: token-report (4 consecutive failures today, last ~13:26 UTC, zero-token signature) · secured-watch (3 consecutive failures today, last ~13:25 UTC, same signature)

Both share the `total_cost_usd:0`/all-zero-token error signature — looks like a shared-dependency blip (LLM gateway), not two separate code regressions. `tweet-digest` dispatched later (17:00 UTC) and completed cleanly, so the dependency likely recovered by then — a manual re-dispatch of either skill would confirm before tomorrow's cron.

Auto-filed health threads: [#230](https://github.com/aaronjmars/aeon-agent/issues/230) (token-report, 4 regression comments today) · [#231](https://github.com/aaronjmars/aeon-agent/issues/231) (secured-watch, 3 regression comments today)

🔵 MEMORY: two carried flags, unchanged, no new alert — working-tree anomaly (`AGENTS.md` deleted-uncommitted, `notify`/`notify-jsonrender` untracked, 35+ days) and scratch-file git hygiene (`.tmp-sw/`, `output/.tw-*` tracked in git from `/tmp` sandbox fallback)

Status page: 🟡 WATCH → 🔴 DEGRADED
