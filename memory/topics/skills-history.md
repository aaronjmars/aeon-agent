# Skills History (Archived)

Older skills archived from MEMORY.md Skills Built table to keep the index under ~50 lines.

| Skill | Date | Notes |
|-------|------|-------|
| changelog | 2026-03-19 | First run; created watched-repos.md |
| polymarket | 2026-03-25 | Gamma API, top 20 by volume + 20 newest |
| push-recap | 2026-03-25 | Scans repo for recent push activity |
| status | 2026-03-25 | Agent operational overview — enabled skills, activity, Actions health, open PRs, config |
| per-skill-model | 2026-03-25 | Scheduler passes per-skill model overrides for cost optimization |
| analytics-dashboard | 2026-03-25 | Skill run analytics tab in dashboard — per-skill metrics, insights, bar charts |
| per-skill-model | 2026-03-27 | Sonnet for data skills (token-report, repo-pulse, heartbeat, fetch-tweets, memory-flush), opus for creative |
| skill-forking | 2026-03-28 | skills.json manifest + export-skill for standalone skill distribution (PR #3 on aeon) |
| self-improve-pr-guard | 2026-03-28 | PR awareness check prevents pile-up — stops at 3+ open PRs, avoids conflicts (PR #4) |
| rss-feed | 2026-03-29 | Atom feed from articles/ — subscribable output distribution (aeon PR #4) |
| skill-security-scanner | 2026-03-30 | Audit imported skills for injection/exfiltration/prompt-injection before execution (aeon PR #5) |
| skill-discover | 2026-04-01 | Autonomous discovery of trending skills from SkillsMP/GitHub; scores, security-checks, and ranks gap-filling candidates (aeon PR #6) |
| github-pages-gallery | 2026-04-02 | Jekyll-based public gallery at docs/ — publishes articles as browsable posts; update-gallery skill syncs weekly (aeon PR #7) |
| deep-research | 2026-04-04 | Exhaustive multi-source synthesis (30–50 sources, 3K–5K words) using 1M context; shallow/deep modes via --depth flag (aeon PR #9) |
| memory-flush (improved) | 2026-04-04 | Added stale-entry cleanup step: removes resolved PR lists, prunes Next Priorities, archives old Skills Built rows (aeon-agent PR #5) |
| skill-smoke-tests | 2026-04-05 | Static SKILL.md validator + test-skills.yml CI workflow — runs on every PR touching skills/; checks frontmatter, cron, secrets, placeholders (aeon PR #10) |
| cost-report | 2026-04-07 | Weekly API cost breakdown — reads token-usage.csv, computes $ per skill/model (Opus/Sonnet/Haiku pricing), week-over-week trend, top 10 by cost (aeon PR #12) |
| fork-fleet | 2026-04-08 | Weekly fork fleet scan — inventories active forks, scores divergence (+3 new skill, +2 unique commit), deep-reads top 3 forks' unique skills, surfaces upstream candidates (aeon PR #13) |
| skill-evals | 2026-04-09 | Output quality assertion framework — validates recent skill outputs against per-skill manifests (word count, required/forbidden patterns, numeric range checks); covers 14 skills; runs Sunday 6 AM UTC (aeon PR #27) |
| mcp-skill-adaptor | 2026-04-10 | TypeScript MCP server wrapping all 54 Aeon skills as aeon-<slug> tools; one-command install via ./add-mcp; works with Claude Code and Claude Desktop (aeon PR #28) |
| workflow-security-audit | 2026-04-11 | On-demand skill that audits .github/workflows/ for script injection, over-permissioning, unverified actions; fixed 2 critical injection vectors in messages.yml (aeon PR #29) |
| email-notification | 2026-04-12 | Fourth notification channel via SendGrid — SENDGRID_API_KEY + NOTIFY_EMAIL_TO secrets, Email group in dashboard, plain+HTML email body; configurable subject prefix (aeon PR #30) |
| auto-merge | 2026-04-13 | Merges fully-green PRs (MERGEABLE + no CHANGES_REQUESTED + all checks SUCCESS/NEUTRAL/SKIPPED) — max 3/run, squash+delete-branch; closes the self-improve cycle stalled at 3-PR guard (aeon PR #31) |
| skill-version-tracking | 2026-04-14 | `add-skill` records provenance (source_repo, commit_sha, imported_at) into `skills.lock`; new `skill-update-check` skill diffs upstream changes weekly and runs security scan on changed content (aeon PR #32) |
| skill-leaderboard | 2026-04-14 | Weekly ranking of most popular skills across active forks — scans fork aeon.yml files, aggregates enabled skill counts, surfaces consensus skills and adoption gaps (aeon-agent PR #9) |
| a2a-gateway | 2026-04-15 | A2A Protocol Gateway — HTTP server exposing all Aeon skills to LangChain, AutoGen, CrewAI, OpenAI Agents SDK, Vertex AI via JSON-RPC; SSE streaming for long-running skills (aeon PR #35) |
| syndicate-article | 2026-04-16 | Dev.to Article Syndication — auto-cross-posts articles with canonical URL back to GitHub Pages; DEVTO_API_KEY in dashboard; postprocess-devto.sh sandbox fallback (aeon PR #36) |
| skill-graph | 2026-04-17 | Skill Dependency Graph — Mermaid map of all 91 skills grouped by category; 18 dependency edges across 4 types; highlights self-healing loop and content pipeline (aeon PR #38) |
| star-milestone | 2026-04-18 | Star Milestone Announcer — celebratory notification when watched repos cross thresholds (25/50/100/150/200/250/500/1000/...); highlight reel from last 14 days of logs; first-run bootstrap silent to avoid retroactive spam (aeon PR #39) |
| syndicate-article (Farcaster) | 2026-04-18 | Farcaster Syndication — extends syndicate-article to cross-post every article to Farcaster via Neynar managed signer; independent channels (Dev.to and Farcaster can be enabled separately); signer UUID injected from env at POST time never lands on disk; drive-by fix passes DEVTO_API_KEY to post-process env (was missing upstream) (aeon PR #40) |
| repo-pulse (improved) | 2026-04-18 | Same-day dedup — subsequent runs compute delta_stars/delta_forks against prior `## Repo Pulse` sections in today's log; skip notification if delta empty, notify delta-only otherwise ('since last run' framing). Fixes near-duplicate notifications on multi-run days (aeon-agent PR #15) |
| memory-search-api | 2026-04-19 | Read-only REST API at `/api/memory/*` in the dashboard — exposes MEMORY.md, topic files, daily logs, and the issues tracker as JSON (list + search + fetch-by-slug/date/id); shared reader with path-safe resolution; unblocks MCP/A2A/fork-operator access to agent state (aeon PR #41) |
| fork-contributor-leaderboard | 2026-04-20 | Weekly Sunday skill that ranks community devs across fork fleet — scoring merged/open upstream PRs (+10/+3), per-fork commits (+1 cap 30), new skill authorship (+5 cap 5), fork stars (+2). Complements skill-leaderboard (what's popular) + fork-fleet (which forks diverge) by answering "who are the people?"; bots + core team filtered, opt-out via leaderboard-optout.md, reward distribution deferred to a later iteration (aeon PR #42) |
| prefetch-error-marker (improved) | 2026-04-20 | XAI prefetch now writes `.xai-cache/<outfile>.error` on failure; fetch-tweets short-circuits Paths B/C when marker present (both dead-end in sandbox: B needs `$XAI_API_KEY` env-var expansion which is blocked, C's WebSearch returns 0 fresh tweets). Prefetch retry budget 2→3, adds `--connect-timeout 30`, `-sS`. Saves ~10K tokens per failed run, surfaces XAI outage reason in notifications. Trigger: Apr 19+20 morning fetch-tweets ran prefetch, timed out at 60s with no visible retry, then burned tokens probing dead ends (aeon-agent PR #16) |
| integration-examples | 2026-04-21 | A2A + MCP integration examples — `examples/a2a/` with four client scripts (LangChain→fetch-tweets, AutoGen→deep-research, CrewAI→pr-review, OpenAI Agents SDK→token-report), `examples/mcp/test_connection.py` smoke test + `claude_desktop_config.json`, walk-through README. Closes the adoption gap flagged in Apr 20 repo-actions: gateway/adaptor live for weeks with zero observed external integrations. Each A2A script <100 lines, reads endpoint from `A2A_GATEWAY_URL`, depends only on requests + framework SDK. Linked from README "Integrations (MCP & A2A)" section (aeon PR #137) |
| onboard | 2026-04-22 | Operator setup validator — `./onboard` bash CLI runs 8 read-only checks (workflows, aeon.yml enabled count, memory writability, ANTHROPIC_API_KEY|CLAUDE_CODE_OAUTH_TOKEN, notification channel, Actions run history, memory/logs evidence, optional GH_GLOBAL) with per-gap fix commands. Modes: `--remote`, `--quiet`, `--json`, `--help`; exits 1 on failure. `skills/onboard/SKILL.md` is the workflow_dispatch counterpart that runs the CLI's `--json` output, sends a checklist via `./notify`, logs trend to `memory/topics/onboard-history.md`. Closes Apr 20 repo-actions idea #2 — silent-fork abandonment gap (32 forks, ~26 active, no guided setup path). Local-vs-remote split: local for instant terminal feedback, remote to verify end-to-end pipeline. README Quick start gains step 5 "Verify" (aeon PR #139) |
| prefetch-error-marker (propagated) | 2026-04-22 | Extends Apr-20 short-circuit pattern from fetch-tweets to the three other skills that read the XAI prefetch cache: remix-tweets stops + notifies REMIX_TWEETS_PREFETCH_FAILED on `.xai-cache/remix-tweets.json.error` (no useful WebSearch fallback for "older tweets from one account"); narrative-tracker + tweet-roundup skip the sandbox-broken Path B curl on `.error` marker and fall through to their existing WebSearch fallback. Closes Apr-21 push-recap follow-up (3); ~10K-token-per-failed-run savings now extend across 4 skills instead of 1 (aeon-agent PR #17) |
| fork-skill-digest | 2026-04-23 | Weekly cross-fork divergence digest (Sunday 18:30 UTC) — surfaces where the configured fleet systematically disagrees with upstream defaults on enabled/var/model/schedule. Companion to skill-leaderboard (popularity) + fork-fleet (per-fork work); fills the peer-learning gap by ranking skills by *divergence from upstream* not by adoption count. Buckets: DEFAULT_FLIP_ENABLE (≥50% configured forks turn on what upstream defaults off), DEFAULT_FLIP_DISABLE (≥50% turn off what upstream defaults on), MODEL_CONSENSUS (≥2 forks share an alternative model), VAR_HOTSPOT, EMERGING (25-49% watchlist). Per-fork customization fingerprint for top 5 heaviest customizers with dominant category lean. State persisted to memory/topics/fork-skill-digest-state.json for week-over-week deltas (NEW_FLIP/STRENGTHENED/FADED/NEW_FORK_ONLY). Notify gated on N_CONFIGURED ≥ 2 AND ≥ 1 signal bucket non-empty. Closes Apr-22 repo-actions idea #2 — second of two highest-priority unbuilts (aeon PR #140) |
| public-status-page (heartbeat) | 2026-04-24 | Public `/status/` page on GitHub Pages gallery auto-regenerated by heartbeat every 3rd-daily run. Header: 🟢 OK / 🟡 WATCH / 🔴 DEGRADED verdict derived from existing P0-P3 signals + Updated UTC timestamp + open-issues count. Per-skill table (last run, status icon, success rate, consecutive failures) for every enabled skill in aeon.yml + open-issues render from memory/issues/INDEX.md. Wholesale overwrite each run (git log is audit trail); workflow auto-commit step lands file on main → Pages rebuild. Zero net-new data sources, zero net-new secrets/cron. Forks inherit `/status/` automatically — closes fork-visibility gap that fork-fleet + fork-skill-digest could observe but not broadcast. Closes Apr-22 repo-actions idea #4 (Public Agent Status Page, DX/Community, Small/hours) — highest-priority unbuilt after fork-skill-digest since Smithery/MCP Registry (idea #1) blocked on external PRs (aeon PR #141) |
| heartbeat (improved) | 2026-04-24 | Extended-persistence backoff — new third tier in the Dedup & Escalation Rules: once an issue has been escalating for 7+ consecutive days, re-notify cadence switches from every 48h to every 7 days. `ESCALATION:` prefix + persistence count stay; only interval changes. First escalation past day 7 still fires on 48h cadence; backoff kicks in on the next escalation after that. Resolution resets all counters. Trigger: PAT-with-`workflows`-scope issue had fired `ESCALATION` every 48h for 7+ days (4+ pings since Apr 17); operator-dependent issues can't be resolved on the agent's preferred cadence and fifth+ ping was noise rather than signal (aeon-agent PR #18) |
| skill-analytics | 2026-04-25 | Fleet-level skill-run analytics widget — Wednesday 18:30 UTC meta skill that ranks every skill that ran in the last 7 days. Pulls `./scripts/skill-runs --json --hours 168` (ground truth for pass/fail), cross-refs aeon.yml schedules (silent-scheduled detection), cron-state.json (consecutive_failures), and memory/logs/*.md regex grep (best-effort exit taxonomy parsing — captures the new SKIP_UNCHANGED/NEW_INFO/SKIP_QUIET exits from the autoresearch-evolution rewrites that existing health checks misclassify). Six anomaly flags first-match-wins: 🔴 SILENT (zero runs in window), 🔴 ALL_FAIL, 🟠 CONSECUTIVE_FAILURES (≥3), 🟠 LOW_SUCCESS (<80% over ≥3 runs), 🟡 ALL_SKIP (every run skip-class — verify intent), 🟡 DUPLICATE_RUNS (>2× expected). Significance-gated notify (clean fleet = silent run); article + dashboard JSON spec write either way. Outputs: articles/skill-analytics-${today}.md + dashboard/outputs/skill-analytics.json. Exit taxonomy: SKILL_ANALYTICS_OK | SKILL_ANALYTICS_QUIET | SKILL_ANALYTICS_NO_DATA. Closes Apr-22 repo-actions idea #5 + Apr-24 idea #1 — highest-priority unbuilt for two cycles. Companion to skill-health (per-skill issue filing) + heartbeat (per-run pulse); this skill closes the fleet-wide observability gap (aeon PR #142) |
| contributor-reward | 2026-04-26 | Closes the fork-contributor-leaderboard → distribute-tokens loop. Monday 09:30 UTC skill that reads the latest articles/fork-contributor-leaderboard-*.md (rejects >8d stale), parses Top Contributors table via tolerant regex on the documented column layout, prices each rank 1-5 contributor with score ≥ 10 against a tier table (1=25, 2=15, 3=10, 4-5=5 USDC) plus +5 first-PR bonus once-ever per login. Writes `contributors-YYYY-Wnn` list to memory/distributions.yml; idempotency state in memory/state/contributor-reward-state.json keyed on (week, login) + flat first_pr_bonus_paid list. Plan-generation only — does NOT execute transfers (distribute-tokens stays the single execution boundary, preserves its preflight + per-recipient idempotency; distributions.yml diff is the human-visible audit trail). Re-runs in same week with identical plan = silent no-op; re-runs with diffs = add only deltas, never claw back demoted entries. Exit taxonomy: OK / DRY_RUN / ALREADY_PROCESSED / NO_LEADERBOARD / STALE_LEADERBOARD / PARSE_FAIL / NO_ELIGIBLE / ERROR; silent exits for the no-op cases. Pure local file I/O — no curl, no env-var-expansion, no new prefetch/postprocess. Closes Apr-24 repo-actions idea #2 — highest-priority unbuilt after skill-analytics shipped (aeon PR #144) |
| SHOWCASE.md (docs feature) | 2026-04-27 | Adds `SHOWCASE.md` to aaronjmars/aeon — Active Forks table (top 6 by skill count) + Ecosystem Comparison table (Aeon vs AutoGen, CrewAI, n8n, LangGraph across 11 dimensions). Topics-expansion half deferred: `gh api topics -X PUT` returned 403 (token lacks admin scope). Closes Apr-26 repo-actions idea #2 (aeon PR #145) |
| heartbeat (token pulse) | 2026-04-28 | Adds `## Token pulse` section to the public `/status/` page — single-row table with Price / 24h / Liquidity / Volume(24h) / FDV for the tracked token, plus a source line citing the exact `articles/token-report-*.md` file. Tolerant regex handles both old and new token-report layouts; `—` fallback per cell; 24h staleness fallback renders placeholder; section omitted on token-less forks. Closes Apr-26 repo-actions idea #3 (aeon PR #146) |
| self-improve (skill-runs cross-check) | 2026-04-28 | self-improve assessment phase now also runs `./scripts/skill-runs --hours 48 --failures` alongside the existing 2-day log scan. Closes a blind spot — logs are self-reported; a skill that crashes or times out never writes its log entry and was previously invisible to self-improve. Workflow-level conclusions from GitHub Actions are the ground truth (aeon-agent PR #21) |
| pr-triage | 2026-04-29 | First-touch external-PR triage skill. Four-check rubric (scope / format / originality / size) → four verdicts (ACCEPTED / NEEDS-CHANGES / DEFER / OUT-OF-SCOPE). Idempotency: primary state in memory/triaged-prs.json + defensive 7-day scan for triage comments. Narrow auto-close: OUT-OF-SCOPE only + unambiguous protected-path match. Cron 30 9 * * *. Trusted-author allowlist reuses auto-merge convention. Shipped enabled: false (aeon PR #147) |
| thread-formatter | 2026-04-30 | Auto-formats the day's top event as a 5-tweet thread. Scores events via signal table (PR +6, star milestone +5, ≥15% price move +5, skill built +4…), emits 5-tweet thread (hook → context A → context B → implication → CTA) with 280-char/tweet limit. 3-day topic dedup. Cron 30 17 * * *. Shipped enabled: false (aeon PR #148) |
| fetch-tweets (spam quarantine) | 2026-04-30 | Three-signal AND filter routes stock-watchlist spam to log subsection. Min-3-tweet floor prevents empty notifications. Spam URLs added to seen.txt. Conservative AND-of-three preserves legit small-account tweets (aeon-agent PR #22) |
| smithery-manifest | 2026-05-01 | Weekly Monday 06:00 UTC. Regenerates docs/smithery-manifest.json + docs/smithery.yaml + docs/smithery-submission.md from skills.json + aeon.yml + README. Three-file byte-equality check before notify — no-op silent re-run on stable input. Closes 6-week carry (Apr-22 idea #1). Shipped enabled: false (aeon PR #149) |
| show-hn-draft | 2026-05-01 | Workflow_dispatch one-shot. Writes 4-paragraph Show HN body (≤350 words) + r/MachineLearning + r/selfhosted variants + operator launch checklist. Lead-beat picker scores candidates on concreteness × recency × surprise. Single-variant regeneration via var=show-hn|r/MachineLearning|r/selfhosted. Never posts. Shipped enabled: false (aeon PR #151) |
| bankr-prefetch-error-marker | 2026-05-01 | Backports .error marker pattern to scripts/prefetch-bankr.sh. Three reasons: BANKR_API_KEY_MISSING / BANKR_API_KEY_INVALID (auth-error-count == lookups gate) / BANKR_LOOKUPS_FAILED. Closes empty-{}-cache ambiguity — downstream consumers now short-circuit on marker instead of error per handle (aeon-agent PR #24) |
| github-releases (minitor) | 2026-05-01 | 8th GitHub plugin for minitor. Fills releases gap in cluster (trending/issues/prs/stars/forks/search/backlinks existed). Reads existing fetchReleases integration layer. Tag icon, green #22c55e accent (minitor PR #23) |
| fork-cohort | 2026-05-02 | Sunday 19:00 UTC. Buckets forks by activation stage via Actions run history: POWER / ACTIVE / STALE / COLD / UNREADABLE. Week-over-week delta tags: LEVELED_UP / REVIVED / WENT_STALE / NEW_ACTIVE / WENT_COLD / NEW_FORK / DROPPED_FROM_POWER. State in memory/topics/fork-cohort-state.json. Cap at 80 forks/run. Read-only (aeon PR #152) |
| skill-runs (--skill + --duration) | 2026-05-02 | Adds --skill <name> filter (applied early, affects all output modes) and --duration mode (per-skill mean/p95/max/last seconds, sorted by mean desc). --full appends avg Xs per row. Backwards compatible. Pure jq math on already-fetched data (aeon-agent PR #25) |
| self-improve (PR-filter precision) | 2026-05-02 | Tightens open-improvement-PR query — replaces GitHub full-text search with --jq post-filter on title prefix only (startswith("improve:") or test("^improve\\(")). Prevents false positives from PRs whose body mentions "improve" (aeon-agent PR pending) |
| bluesky-column (minitor) | 2026-05-02 | Keyless Bluesky column reading public.api.bsky.app/xrpc; search + author modes. Three Bluesky-specific quirks: at:// URI → bsky.app permalink, handle normalization, repost filter on author feeds. Cloud icon, #0085ff accent (minitor PR #25) |
| operator-scorecard | 2026-05-03 | Weekly Monday 10:30 UTC sonnet — three-paragraph synthesis (agent health / community growth / economic activity), worst-of-three verdict 🟢/🟡/🔴 mirroring heartbeat P-flags. Every number from a file another skill wrote (aeon PR #153) |
| cron-state (script) | 2026-05-03 | Local viewer for memory/cron-state.json; --skill, --unhealthy (exits 1), --stale [hours] (exits 1), --json modes. Useful when gh api rate-limited or sandbox-blocked (aeon-agent PR #27) |
| mastodon-column (minitor) | 2026-05-03 | 32nd column type. Keyless Mastodon REST API; hashtag + author modes. HTML strip, federated handle parsing, reblog filter, CW prefix. #6364ff accent (minitor PR #26) |
| skill-freshness (aeon) | 2026-05-04 | Daily 08:00 UTC. Walks enabled skills' file deps, checks freshness per class (articles 28h/192h · .outputs 4h · topics 7d · state 30d). OK/WARN/STALE/MISSING bands. Fingerprint dedup 7d re-emit. Closes silent-staleness gap (aeon PR #157) |
| operator-scorecard (aeon-agent) | 2026-05-04 | Backport of aeon PR #153 + heartbeat-only fallback so forks without skill-analytics don't permanently report WATCH (branch B: OK if p0==0 AND p1==0 AND ok≥5). Merged PRs #28 + #29 |
| lobsters-column (minitor) | 2026-05-04 | 34th column type. Keyless lobste.rs JSON API; Hottest/Newest/Active/Tag modes. Tag pills, anchor icon, #ac130d accent (minitor PR #27) |
| star-momentum-alert (aeon) | 2026-05-05 | Daily 10:10 UTC. Projects next milestone via 7d rolling star average. Alert gates: 7-14d out AND Tue/Wed/Thu. Per-(repo,milestone) dedup 7d re-emit. Pairs with show-hn-draft (aeon PR #159) |
| skill-freshness (aeon-agent) | 2026-05-05 | Verbatim backport of aeon PR #157 (aeon-agent PR #30) |
| polymarket-column (minitor) | 2026-05-05 | 35th column type. Gamma API keyless; trending/newest/ending-soon/tag modes. 5 integration quirks (JSON-string outcomes parse, binary leading-outcome sort, price clamp 0..1, past-dated drop, event-slug permalink). #2D9CDB accent (minitor PR #28) |
| token-report (volume trend) | 2026-05-06 | Adds 24h vol % trend + 7d/30d avg vol to token-report output and notification (aeon-agent PR #31) |
| xai-prefetch max_output_tokens | 2026-05-06 | Raises max_output_tokens to 16384 in xai_search helper. Trigger: May-6 cache truncated at 7,354 tokens (6,486 reasoning), delivering 2 tweets instead of 10+. Affects 5 skills sharing the helper (aeon-agent PR #32) |
| v4-readiness (aeon) | 2026-05-06 | Workflow_dispatch one-shot. Reads aeon.yml + skills.json + MEMORY.md vs embedded v4 change manifest. Emits Safe/Review/Custom/Action breakdown with effort tags. Read-only, manifest-in-SKILL.md travels per-fork (aeon PR #160) |
| stack-overflow-column (minitor) | 2026-05-07 | 36th column type. Keyless Stack Exchange API 2.3; hot/votes/newest/week/month modes; optional 1–5 tag AND-filter (commas/spaces normalised to `;`). Accepted-answer badge, #F48024 brand orange, HTML-entity decoded titles. Plugin + 3 registry edits + integration helper (minitor PR #29) |
| skill-template-library (aeon) | 2026-05-07 | Closes activation gap for fork operators. `templates/` with 6 starters (crypto-tracker, research-digest, code-reviewer, social-monitor, deploy-watcher, community-manager) — each runnable SKILL.md with `[REPLACE: KEY]` tokens. `./new-from-template` CLI: --list, --tokens, --var KEY=VALUE; sed-substitutes (escapes `\&\|`), registers disabled entry in aeon.yml. Carried unbuilt from Apr-18 ideas (aeon PR #161) |
| huggingface-column (minitor) | 2026-05-08 | 37th column type. Keyless HF Hub REST API; 3 resources (models/datasets/spaces) × 3 sorts (trending/most-likes/newest) + search. First plugin to use the `ai` ColumnCategory. Schema-drift safe across the 3 resource types. Plugin + 3 registry edits + integration helper. README adds AI/ML cluster row, count 36 → 37 (minitor PR #30) |
| huggingface-trending (aeon) | 2026-05-08 | Daily 09:30 UTC sonnet — curated trending HF models/datasets/spaces. Six noise filters; ≤18-word "why notable" gate; momentum tags (DEBUT/ACCELERATING/RETURNING/HOLDOVER); 5-bucket cluster cap; single Top pick. Completes AI ecosystem daily-movement triple (aeon PR #162) |
| xai-prefetch truncation warning | 2026-05-08 | Parses `.usage.output_tokens` post-call; emits `::warning::` annotation when within 5% of max_output_tokens=16384. Surfaces both raw output and reasoning_tokens. Single-source-of-truth max_output_tokens via `local` (aeon-agent PR #33) |
| contributor-spotlight (aeon) | 2026-05-09 | Weekly Sunday 20:00 UTC sonnet — recognition post for one POWER fork. 11-step skill, 8-status exit taxonomy, 4-week dedup in memory/topics/contributor-spotlight-history.json. POWER → ACTIVE fallback, bot+parent filter. Closes fork-cohort → social gap (aeon PR #163) |
| skill-update-check backport (aeon-agent) | 2026-05-09 | Synced local v1 → upstream variation B: CRITICAL/HIGH/MEDIUM/LOW triage by drift_size × security × ENABLED state, raw-accept SKILL.md fetch, atomic skills.lock writes, ACCEPT mode, frontmatter-diff detection, breaking-change scan. skills.json 57 → 58 (aeon-agent PR #34) |
| arxiv-column (minitor) | 2026-05-09 | 38th column type. First academic-paper surface. Keyless arXiv Atom-XML query API; 12 CS/stat/math.OC categories, 2 sort modes, optional keyword filter. Three quirks: literal `+` in search_query, dual-redundant revision badge, PDF link via `<link title="pdf">`. #B31B1B Cornell-red. AI/ML cluster 1 → 2 (minitor PR #31) |
| ai-framework-watch (aeon) | 2026-05-10 | Weekly Monday 08:30 UTC sonnet-4-6 — competitive-intelligence digest across 9 AI agent frameworks (aeon + 8 peers). Hardcoded watchlist (intentional — drift erodes week-over-week comparability). 7d/30d star deltas, releases, breaking-change flags, momentum picks. State in memory/topics/framework-watch-state.json. May-8 idea #3 (aeon PR #164) |
| fork-cohort backport (aeon-agent) | 2026-05-10 | Verbatim backport of aeon PR #152 (May 2). COLD/STALE/ACTIVE/POWER buckets via Actions run history. Week-over-week delta tagging. Sunday 19:00 UTC. skills.json 58 → 59. May-6 idea #3 (aeon-agent PR #36) |
| devto-column (minitor) | 2026-05-10 | 39th column type. News & web cluster 6 → 7. Keyless DEV.to REST API; top/rising/latest modes; 1-5 tag AND-filter. May-8 idea #2 (minitor PR #33) |
| tweet-allocator error-marker contract | 2026-05-10 | Step 4 reads `.bankr-cache/verified-handles.json.error` first; surfaces BANKR_API_KEY_MISSING/_INVALID/BANKR_LOOKUPS_FAILED deterministically. Closes ambiguous-cache-empty failure mode (aeon-agent PR #37) |
| price-threshold-alert (aeon) | 2026-05-11 | Event-driven token alert — ATH / ±20% 1h move / operator target crossings. 4h dedup clocks, DexScreener-keyless, WebFetch fallback. `*/30 * * * *`, enabled:false. skills.json 114 → 115. May-10 idea #1 (aeon PR #165) |
| auto-merge-agent-prs (aeon-agent) | 2026-05-11 | Daily 18:00 UTC auto-merge for aeonframework-authored PRs. 9 eligibility gates + `gh pr merge --squash --auto`. 5-PR/run cap. skills.json 59 → 60. May-10 idea #3 (aeon-agent PR #38) |
| github-actions column (minitor) | 2026-05-11 | 40th column type. Keyless GitHub REST `/repos/.../actions/runs`; repo/workflow/branch config; in-flight Loader2 spinner + 9 terminal statuses. May-10 idea #2 (minitor PR #34) |
| fork-release-tracker (aeon) | 2026-05-12 | Weekly Sunday 19:30 UTC — celebrate forks cutting tagged GitHub releases in last 7d. LRU-50 state. skills.json 115 → 116. May-10 idea #4 (aeon PR #166) |
| thread-formatter backport (aeon-agent) | 2026-05-12 | Verbatim backport of aeon PR #148 (Apr 30). 9-signal scoring, 5-tweet structure, 280-char cap, threshold 3 suppression. Daily 17:30 UTC, enabled:false. skills.json 60 → 61 (aeon-agent PR #39) |
| npm column (minitor) | 2026-05-12 | 41st column type. News & web cluster 7 → 8. Keyless npm search + downloads APIs. May-10 idea #5 (minitor PR #35) |
| fleet-state (aeon) | 2026-05-13 | Weekly Monday 08:00 UTC — synthesis over fork-cohort + fork-release-tracker + contributor-spotlight. WoW deltas + 12-week trend. 8-status exit taxonomy. May-12 idea #3 (aeon PR #168) |
| v4-readiness backport (aeon-agent) | 2026-05-13 | Verbatim backport of aeon PR #160 (May 6). Per-fork v4 upgrade readiness checklist; embedded Safe/Review/Removed manifest tables; workflow_dispatch only. skills.json 61 → 62 (aeon-agent PR #41) |
| pypi-column (minitor) | 2026-05-13 | 42nd column type. News & web cluster 8 → 9. updates/new-packages/top-30d modes; optional pypistats.org enrichment. May pivot from May-12 ideas #2/#5 (minitor PR #36) |
| webhook-bridge (aeon-agent) | 2026-05-14 | External-event-to-skill bridge via `repository_dispatch`. Slug-injection-safe validation; 4 worked examples. May-12 idea #4 (aeon-agent PR #42) |
| crates-column (minitor) | 2026-05-14 | 43rd column type. Keyless `GET /api/v1/crates`; 5 sort axes; User-Agent required (crates.io policy). Completes npm+pypi+crates registry trifecta. May-12 registry gap (minitor PR #38) |
| product-hunt-launch (aeon) | 2026-05-15 | PH launch-asset drafter — tagline ≤60ch / description ≤260ch / comments / bullets. Banned-marketing-words list. 5-status taxonomy. skills.json 117 → 118. May-14 idea #1 (aeon PR #175) |
| skill-enabler (aeon-agent) | 2026-05-15 | Operator dispatches slugs to flip `enabled: false → true` in aeon.yml. 5 validation gates; feat/enable-skills branch + PR. 9-status taxonomy. skills.json 85 → 86. May-14 idea #2 (aeon-agent PR #47) |
| deck export / import (minitor) | 2026-05-15 | Two ⌘K commands: Export (clipboard JSON blob) + Import (Zod-validated textarea modal, NEW deck with "(imported)" suffix). DeckExport JSON v1 schema. May-14 idea #5 (minitor PR #40) |
| fork-skill-gap (aeon) | 2026-05-16 | Weekly Sunday 21:00 UTC — per-fork upstream-skill-adoption gap report. Reads cohort cache (≤8d) or live fallback. Compares on slug presence. Inverse view: top-10 universally unadopted slugs. skills.json 118 → 119. May-14 idea #3 (aeon PR #176) |
| column alert keywords (minitor) | 2026-05-16 | Optional `alertKeywords` on every column — yellow ring + Bell badge with match count. Column-row level (never reaches fetcher Zod schemas). May-14 idea #4 (minitor PR #41) |
| fork-first-run-alert (aeon) | 2026-05-17 | Daily 20:30 UTC — named alert on fork's first workflow run. LRU-500 seen-list; count-driven notify; bot allowlist + backfill mode. skills.json 118 → 119. May-16 idea #4 (aeon PR #179) |
| product-hunt-launch backport (aeon-agent) | 2026-05-17 | Verbatim backport of aeon PR #175 (May 15). 8th same-day-after. skills.json 86 → 87 (aeon-agent PR #49) |
| producthunt column (minitor) | 2026-05-17 | 44th column type. Keyless producthunt.com/feed RSS; today/topic modes; 5-keyword OR-match cap. May-16 idea #1 (minitor PR #42) |
| competitor-launch-radar (aeon) | 2026-05-18 | Weekly Monday 10:00 UTC — keyless PH RSS + HN Algolia scan for NEW AI-agent-framework launches outside the 9-cohort. LRU-200 state. skills.json 119 → 120. May-16 idea #5 (aeon PR #183) |
| fork-first-run-alert backport (aeon-agent) | 2026-05-18 | Verbatim backport of aeon PR #179 (May 17). 9th same-day-after. skills.json 87 → 88 (aeon-agent PR #50) |
| github-discussions column (minitor) | 2026-05-18 | 45th column type. GraphQL-only; recent/unanswered/top modes; optional GITHUB_TOKEN; DiscussionsDisabledError sentinel. May-16 idea #2 (minitor PR #43) |
| scan.sh Bash 3.2 fix (aeon) | 2026-05-19 | Bugfix Issue #182. Three print loops wrapped in `[[ ${#arr[@]} -gt 0 ]]` length guards to prevent Bash 3.2 zero-element-array abort under `set -euo pipefail`. May-18 idea #2 (aeon PR #186) |
| fork-skill-gap backport (aeon-agent) | 2026-05-19 | Verbatim backport of aeon PR #176 (May 16). 10th same-day-after. skills.json 87 → 88 (aeon-agent PR #52) |
| coingecko column (minitor) | 2026-05-19 | 46th column type. Apps & on-chain cluster 4 → 5. Keyless; trending/top/watchlist modes; currency-string → number normalization; host swap on key presence. May-18 idea #4 (minitor PR #44) |
| scan.sh POSIX-ERE fix (aeon) | 2026-05-20 | POSIX-ERE rewrite of 28 scan.sh patterns (no \s/\b); fixes silent no-op on macOS BSD grep. AntFleet H6 (aeon PR #197) |
| competitor-launch-radar backport (aeon-agent) | 2026-05-20 | Verbatim backport of aeon PR #183. PH RSS + HN Algolia scan for new AI-agent-framework entries outside the 9-cohort; LRU-200 dedup; 7-status taxonomy (aeon-agent PR #53) |
| defillama-column (minitor) | 2026-05-20 | 47th column type. Apps & on-chain cluster 5 → 6. Keyless api.llama.fi; top/gainers/chains modes; optional category filter. May-18 idea #5 pivot (minitor PR #45) |
| contributor-spotlight FORK_DEFAULT_BRANCH fix (aeon) | 2026-05-21 | Step 4 now extracts FORK_DEFAULT_BRANCH from JSON with null guard; step 5 tightens \|\| true. AntFleet H3 (aeon PR #206) |
| skill-update-check branch-honor fix (aeon-agent) | 2026-05-21 | Conditionally passes -f sha=${branch} only for non-default/non-empty branches; avoids perpetual false "update available" on pinned branches. AntFleet H7 (aeon-agent PR #55) |
| deck share link (minitor) | 2026-05-21 | base64url-encoded #deck= fragment share link; 32KB cap; SSRF-safe fragment never in server logs; import via existing importDeck action. May-20 idea #4 (minitor PR #46) |
| install-skill-pack CLI (aeon) | 2026-05-22 | ./install-skill-pack <owner/repo> bash CLI; reads skills-pack.json manifest; scan.sh gate + prompt-on-HIGH; 6 flags (--list/--path/--branch/--yes/--force/--dry-run). May-20 idea #2 (aeon PR #213) |
| scan.sh Bash 3.2 + POSIX-ERE backport (aeon-agent) | 2026-05-22 | Combined backport of aeon PRs #186+#197; Bash 3.2 array-emptiness guards + POSIX-ERE rewrite of 28 patterns (aeon-agent PR #56) |
| starter deck templates (minitor) | 2026-05-22 | 4 pre-baked DeckExport v1 templates; "Start from template" onboarding section + TemplatesDialog ⌘K modal; routes through existing importDeck action. May-20 idea #5 (minitor PR #47) |
| skill-packs.json registry + --list (aeon) | 2026-05-23 | Machine-readable mirror of the Community Skill Packs README table. New `skill-packs.json` at repo root seeded with 5 packs (AntFleet/aeon-skills `trusted`, baseddevoloper/aeon-skill-pack-vvvkernel, danbuildss/luca-aeon-skills, 0xShak/zer0-skill-pack from PR #208, gitlawbounty/gitbounty-skill-pack from PR #212). Slug arrays sourced LIVE from each pack's own skills-pack.json (zer0, gitbounty) or `gh api .../contents/skills` listings (vvvkernel→9 slugs vvvkernel/vvvkernel-{audit,brief,digest,growth,image,monitor,narrative,onchain}; luca→4 slugs check-agent/get-report/scan-wallet/treasury-monitor) — never approximated. `./install-skill-pack` grows registry-list mode: with no repo argument and only the `--list` flag, it prints the registry (repo / skill count / `*` trust badge / one-line description); any other flag or positional argument falls through to existing single-pack path (strict detector, all prior behaviour preserved). Reads local skill-packs.json first, falls back to `https://raw.githubusercontent.com/aaronjmars/aeon/main/skill-packs.json` via curl when the script runs outside a clone. docs/community-skill-packs.md gains a "Browse the registry" section near the top + full "skill-packs.json (community registry)" section with schema + field reference table; publishing checklist now requires pack PRs to update BOTH the README table AND the registry in the same diff. `trust_level: "trusted"` in the registry is a DISCOVERY hint only — the actual scan-bypass behaviour is still gated by `skills/security/trusted-sources.txt`, so the registry can't be used to forge trust. May-22 idea #1 (aeon PR #215) |
| contributor-spotlight FORK_DEFAULT_BRANCH backport (aeon-agent) | 2026-05-23 | Verbatim backport of upstream aeon PR #206 (merged 2026-05-21, AntFleet Issue #184 H3). 11th same-day-after backport in the established chain (operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17, fork-first-run-alert May-17→18, fork-skill-gap May-18→19, competitor-launch-radar May-19→20, scan.sh Bash 3.2 + POSIX-ERE May-22, contributor-spotlight May-23). `skills/contributor-spotlight/SKILL.md` step 5's `gh api .../contents/aeon.yml?ref=${FORK_DEFAULT_BRANCH}` referenced a shell variable that was never set — step 4 wrote `default_branch` into `/tmp/contrib-repo.json` but stopped. Empty ref made every non-`main`-branch fork silently 404, so ENABLED_COUNT and OPERATOR_AUTHORED (the two most newsworthy data points in the weekly recognition article + notification) were both wrong on those runs. Fix: step 4 now extracts `FORK_DEFAULT_BRANCH=$(jq -r '.default_branch // "main"' /tmp/contrib-repo.json)` with a second-line guard for the literal string `null` (API emits `null` for archived/empty forks). Step 5 tightens `\|\| true` to `\|\| echo '' > /tmp/fork-aeon.yml` so downstream `grep -E` always has a real (possibly empty) file under `set -e`. Inline comments cite upstream Issue #184 H3 + aeon PR #206 so a future cleanup doesn't drop the guard. May-22 idea #3 (aeon-agent PR #58) |
| /gallery public deck page (minitor) | 2026-05-23 | Public, SEO-crawlable starter-deck catalog at `/gallery`. First-time visitors with no account can land on a curated card grid, click a template, and Minitor imports it as a new deck the moment they hit the dashboard. Builds entirely on top of the share-link (PR #46 May-21) + templates (PR #47 May-22) infrastructure — no new schema, no new server routes, no new validation path. Two files: `app/gallery/page.tsx` (new, server component with Next.js Metadata export for `<title>` + `<meta description>` + OpenGraph + canonical `/gallery`; imports TEMPLATES from `lib/deck-templates.ts`; responsive grid 1-col mobile / 2-col tablet+; each card renders brand-accented icon chip + name + tagline + full description + column-type pills coloured by each plugin's brand via `getColumnType`), and `components/sidebar-01/nav-footer.tsx` (new "Browse deck gallery" `<Link>` above the Add-new dropdown using LayoutTemplate lucide icon — returning operators discover the gallery from the dashboard without needing to know the URL). Cards link to `/#deck=<base64url(payload)>` — same shape that the share-link flow produces, picked up by the existing useEffect in deck-view.tsx that decodes the fragment and runs the same `importDeck` server action used by JSON-paste / share-link imports (same Zod validation, same `(imported)` rename, same activate-as-new-deck contract). Server-side rendering works because `btoa` + `TextEncoder` are Node globals on the Next.js 15 runtime; share fragment omits `exportedAt` so URLs stay STABLE across requests (cache-friendly, individually shareable). `getColumnType` is safe to call from a server component — only `accent`, `label`, `icon` are accessed on the return, and `icon` is always a `LucideIcon` (framework-agnostic SVG). ~180 lines of code on top of infrastructure that already existed. May-22 idea #5 (minitor PR #48) |
| v4-readiness manifest/read-set alignment (aeon) | 2026-05-24 | Closes Issue #184 H1 — last open High on AntFleet's two-model bench audit. Repo-actions idea #2 framed H1 as "Removed table gaps"; the actual finding (verified against issue body + AntFleet receipt at aeon-bench PR #12 comment 4475654244) was that the `v4-readiness` skill's Review table named four files it never actually loaded: `mcp-server/src/index.ts`, `.outputs/`, `chain-runner.yml`, `dashboard/lib/catalog.ts`. Silent undercounting of Review hits → operators got false-clean READY verdicts. With 111+ forks running pre-upgrade workflows, a silently-undercounting readiness check is the worst possible failure mode. Fix: added the four files to §Config Reads + Step 2 inputs table as optional (both local + remote modes); introduced a `review_unscanned[]` bucket — any Review row whose backing file is missing now surfaces as a coverage-gap row in the article AND trips `V4_READINESS_PARTIAL` exit, so silent false-clean READY is now structurally unreachable. Surfaced unscanned counts in article body, notification, and log block. Inline `<!-- Issue #184 H1 audit -->` invariant comment in Step 2 prevents regression on future manifest edits. Also satisfied the brief's audit-refresh ask: `*Last audited: 2026-05-24*` footer + `Audit method` subsection (5 `gh pr list` / `git log` queries) under the Removed table — no verifiable upstream removals found, so Removed stays empty by design but the manifest is now officially fresh. Single-file diff: skills/v4-readiness/SKILL.md (+44/-9). Skill stays `enabled: false` + `workflow_dispatch` only; read-only contract preserved. AntFleet Highs remaining after today: H4 (.bak rollback in fleet-state) and H9 (admanage-create campaignId-only ad sets); seven Highs closed end-to-end (H2/H3/H5/H6/H7/H8 prior + H1 today). May-22 idea #2 (aeon PR #226) |
| install-skill-pack + skill-packs.json backport (aeon-agent) | 2026-05-24 | 12th same-day-after backport in the established chain. Combined backport of upstream aeon PR #213 (install-skill-pack CLI, May-22) + PR #215 (skill-packs.json registry + --list mode, May-23). Shipped as one PR because #215 depends on #213 — backporting one without the other would be dead weight. Aeon-agent had neither half of the community skill-pack install protocol before today. 4 files, +991/-0: `install-skill-pack` (new, mode 100755, +634, Bash CLI with 6 flags `--list`/`--path`/`--branch`/`--yes`/`--force`/`--dry-run`), `skill-packs.json` (new, +100, 5 seed entries — AntFleet/aeon-skills trusted, baseddevoloper/vvvkernel, danbuildss/luca-aeon-skills, 0xShak/zer0-skill-pack, gitlawbounty/gitbounty-skill-pack), `docs/community-skill-packs.md` (new, +224, manifest schema + fallback behaviour + trust model + worked example + publishing checklist), `README.md` (+33, Community skill packs section inserted between trigger-from-issues and Publishing, matching upstream placement). `scan.sh` NOT touched — aeon-agent's `skills/skill-security-scan/scan.sh` already had the May-18 (PR #186, Bash 3.2 array-emptiness) + May-20 (PR #197, POSIX-ERE) hardening; only diff vs upstream is a one-line comment ("upstream aeon AntFleet finding H6" vs upstream's "AntFleet finding H6") — fork-local and deliberately preserved. `skills.json total` NOT bumped — install-skill-pack is a CLI, skill-packs.json is a registry; neither is an installed skill entry. Cadence: operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17, fork-first-run-alert May-17→18, fork-skill-gap May-18→19, competitor-launch-radar May-19→20, contributor-spotlight May-21→23, install-skill-pack+registry May-22→24 (aeon-agent PR #59) |
| ecosystem-pulse (aeon) | 2026-05-25 | May-24 idea #1. New `skills/ecosystem-pulse/SKILL.md` — weekly Mon 11:00 UTC liveness check of ECOSYSTEM.md projects (#220's 40-project list). Resolves each project → GitHub repo via operator-maintained `memory/topics/ecosystem-pulse-map.json` OR strict best-effort `gh search`; buckets by last-push recency (ACTIVE ≤7d / RECENT ≤30d / COLD >30d / X-only / Unresolved), surfaces releases in 7d window, WoW bucket transitions (woke up / went quiet) + star deltas + new entrants. **Read-only against ECOSYSTEM.md** — curation stays a human PR per the file's "Add your project" rules; skill never edits the list. **Never auto-populates the map with guessed repos** — search hits used for one run + flagged `(auto-matched, unverified)`, promotion is a deliberate operator edit (wrong repo would silently misreport weekly). **X-only ≠ COLD** (no public repo = unmeasurable, not inactive). 7-status taxonomy (OK/QUIET/DRY_RUN/PARTIAL/NO_ECOSYSTEM_FILE/STATE_CORRUPT/BAD_VAR), gated notify (QUIET once baseline set + nothing moved; article still refreshes). gh-CLI for GitHub API per CLAUDE.md (no curl-auth sandbox failure mode). Slots after competitor-launch-radar (10:00) in the Monday intelligence stack. Registered disabled in aeon.yml + skills.json (total 155→156). (aeon PR #227) |
| config-validator backport (aeon-agent) | 2026-05-25 | May-24 idea #3. Backport of upstream aeon PR #219. New `skills/config-validator/SKILL.md` — workflow_dispatch structural validator for aeon.yml + .github/workflows/aeon.yml: (1) checkout-before-Run ordering, (2) duplicate skill keys (YAML keeps last → silent shadow), (3) enabled skills missing SKILL.md. CLEAN runs silent; only ISSUES notify. **Three adaptations** (verbatim copy would have broken on aeon-agent): (1) checkout check rewritten from upstream's "unconditional Early checkout first" to "a checkout precedes the Run step" — aeon-agent's workflow checks out CONDITIONALLY per event type (`Early checkout` if issues / `Checkout repo` if mode!='') both before Run, so the upstream rule would fire a false positive every run; (2) fast-path `node scripts/validate-config.js` guarded behind file-exists — aeon-agent has neither that script nor a `ci-config-validate.yml` pre-merge workflow, so the manual node checks are primary; (3) notify uses aeon-agent's single-positional-arg `./notify` (no `-f` flag, unlike upstream). Backport note in skill body cites source PR #219 + each adaptation. Registered disabled, workflow_dispatch only, in aeon.yml + skills.json (total 89→90, category dev). NOT the established same-day-after backport cadence — this is from the May-24 repo-actions idea set. (aeon-agent PR #61) |
| per-column webhook notifications (minitor) | 2026-05-25 | May-24 idea #5 (idea #4 Bluesky column was ALREADY fully implemented + merged on minitor main — integration + 3-file plugin + registry + manifest + README all present at HEAD #48 — so picked the next idea). Turns alertKeywords (PR #41) from passive visual-only into active alerting: when a column has BOTH keywords + an https webhook URL, the server POSTs `{columnId, columnTitle, typeId, matches:[{id,url,text,matchedKeywords}], timestamp}` whenever a fetch brings in NEW matching items. New migration `drizzle/0002_notify_webhook.sql` + journal + snapshot — **0002 not 0003**: this main is at migration 0001 (refresh-interval PR #49 from May-24 is NOT merged here). New `lib/columns/webhook.ts`: `validateWebhookUrl` SSRF guard (https-only; blocks localhost + raw IP literals in RFC-1918/loopback/link-local/CGNAT/multicast IPv4 + IPv6 ::1/fc00::7/fe80::10/IPv4-mapped) + `sendColumnWebhook` (bounded 5s, never throws, `redirect:"error"` so an allowed host can't 30x-bounce internal, console-log only so endpoints can't be probed). `app/actions.ts`: updateColumnWebhookUrl action (server-revalidates), persistFetchedItems fires webhook for NEW matched items only (re-fetch never re-notifies), `matchedAlertKeywords()` helper. **Security decision: exportDeck deliberately OMITS notifyWebhookUrl** — a webhook URL commonly embeds a secret (Slack/Discord token) and exportDeck feeds BOTH copy-JSON AND the public share link, so emitting it would leak the secret; the field is still in DeckExport v1 + accepted on import (re-validated through SSRF guard, dropped if invalid) so hand-authored configs round-trip, but secret never travels in a shared link. Configure-dialog "Alert webhook URL" field shown only when keywords set, live validation, Save blocked on invalid. Store + types + schema wiring. **Known limit: validator blocks IP-literal/localhost but doesn't resolve hostnames (DNS-rebind not caught at validate time); no-redirect sender mitigates redirect vector.** Could NOT run Next 16 build/typecheck (offline sandbox) — manual review only. 10 files. (minitor PR #50) |
| fleet-skill-adoption backport (aeon-agent) | 2026-05-27 | Verbatim backport of aeon PR #245. Measures ENABLED skills per fork (not just PRESENT); pairs with fork-skill-gap. 14th same-day-after backport (aeon-agent PR #64) |
| sparkleware-catalog skill (aeon) | 2026-05-27 | Weekly export of skill-packs.json enriched with live GitHub signals → dashboard/outputs/skill-packs-catalog.json. Registered DISABLED. May-26 idea #2 (aeon PR #252) |
| deck version history (minitor) | 2026-05-27 | Per-mutation deck snapshots (last 5 kept) + one-click restore as new deck in Configure dialog. Migration 0005. May-26 idea #5 (minitor PR #52) |
| per-column refresh intervals (minitor) | 2026-05-24 | Per-column auto-refresh cadence (Manual / 1m / 5m / 15m / 60m) chosen from the Configure dialog. Until today every column refreshed only on mount + manual click; with 47 plugins now spanning fast-moving crypto (CoinGecko/DeFiLlama, 1-5min freshness need) and slow-moving repo signals (GitHub stars, hourly enough), one global cadence couldn't serve both — operators were stuck burning rate limits OR staring at stale data. Field lives at column-row level (sibling to `alertKeywords` PR #41 and `title`), never reaches plugin fetchers — so all 47 plugins keep working with zero changes and their strict Zod schemas remain untouched. 10 files, +513/-2: drizzle/0002_refresh_interval.sql + meta/_journal.json + meta/0002_snapshot.json (additive NULLABLE `refresh_interval_seconds` integer on `columns`); lib/db/schema.ts + lib/columns/types.ts (schema field + `Column.refreshIntervalSeconds?: number`); app/actions.ts (+52 — `REFRESH_INTERVAL_OPTIONS=[60,300,900,3600]` server-side allowlist, `isAllowedRefreshInterval` guard, `updateColumnRefreshInterval` server action, export/import + loadSnapshot wiring, optional Zod field — never trusts client); lib/store/use-deck-store.ts (+33 — `updateRefreshInterval` mirroring `updateAlertKeywords`); components/column/configure-column-dialog.tsx (+71 — "Refresh interval" Select); components/column/column-card.tsx (+84 — useEffect with setInterval, `inFlight` guard preventing overlapping fetches if API slow, `document.visibilityState !== 'visible'` pause so background tabs don't burn rate limits, cleanup on unmount AND interval change, lucide Clock badge in header with "1m"/"5m"/"15m"/"60m" label hidden on manual-only); lib/deck-templates.ts (+4 — `DeckTemplateColumn.refreshIntervalSeconds?: number` so future starter templates can opt-in). Type-check clean on the diff — two pre-existing pypi.ts errors (lines 286, 302) confirmed present on `main` via `git stash -u && tsc && stash pop`. Deck export / import / share-link fragment all round-trip the new field backward-compatibly (decks without it default to manual-only). May-22 idea #4 — last open May-22 idea (minitor PR #49) |
| pr-skill-triage skill (aeon) | 2026-05-28 | May-26 idea #4. New `skills/pr-skill-triage/SKILL.md` — workflow_dispatch-only structured triage receipt for inbound PRs that introduce/modify SKILL.md files (var=PR_NUMBER on aaronjmars/aeon). Per-skill security scan reuses `skills/skill-security-scan/scan.sh` verbatim (no forked patterns) for PASS/WARN/BLOCK verdict + first-3 HIGH findings; required-secrets enumeration (extracts `$VAR` patterns, drops known-safe set GITHUB_TOKEN/GH_TOKEN/today/var); cron slot-conflict check vs aeon.yml (exact = CONFLICT, ±5min same DoW = ADJACENT, workflow_dispatch = OK); quality signals (description ≥40 chars, ≥3 steps, ./notify call present, tags non-empty). One PR comment per (PR, head_sha) — dedup keyed on head SHA prevents re-comment storms. Operator decides merge (no auto-merge, no labels, no Reviews API calls). Verdict precedence: BLOCK (HIGH or hard conflict) > WARN (MEDIUM/missing/adjacent/secrets) > OK. Gated notify: BLOCK and COMMENT_FAILED always; WARN only if MEDIUM security finding present. 9-status exit taxonomy (OK/WARN/BLOCK/NO_SKILLS/DEDUP/PR_NOT_FOUND/PR_CLOSED/COMMENT_FAILED/BAD_VAR). Fallback artifact path (articles/pr-skill-triage-{N}-{today}.md) if `gh pr comment` fails so operator can paste manually. Separated from existing pr-triage so most PRs (no SKILL.md change) don't pay the scan cost — bridge skill specifically for skill PRs (#231 liquidpad-launch + #241 signa-skills currently open). Registered disabled, workflow_dispatch only, in aeon.yml + skills.json (total 158→159, category dev). (aeon PR #259) |
| push-recap $(date ...) self-fix (aeon-agent) | 2026-05-28 | Self-improve: `skills/push-recap/SKILL.md` step 2 — `since="$(date ...)"` → literal `since=YYYY-MM-DDT00:00:00Z` computed from `${today}` minus 24h, matching the PR #63 weekly-shiplog pattern with an inline cite so a future cleanup doesn't reintroduce the shell substitution. Runner hook blocks `$(...)` with "Contains simple_expansion"; the skill was improvising the cutoff by hand on every recent run (explicit "Avoided $(date ...) (runner shell-guard)" notes in 2026-05-25/-26/-27 push-recap logs). Also pre-emptively documented step 1's `(.payload.commits // [])` null-guard for the events API's squash-merged-push empty-array case — also being added by hand. (aeon-agent PR #67) |
| sparkleware-catalog backport (aeon-agent) | 2026-05-28 | 15th consecutive same-day-after backport. Verbatim backport of upstream aeon PR #252 (sparkleware-catalog, merged 2026-05-27). New `skills/sparkleware-catalog/SKILL.md` — weekly enriched export of `skill-packs.json` joining curated registry to live GitHub signals (stars, last-push, live `skills-pack.json` manifest skill count) → machine-readable `skill-packs-catalog.json` at repo root for external tools like Sparkleware (Issue #244) to consume without screen-scraping. Backport note explicitly cites zero adaptation needed: (1) `./notify` arg style already single-positional, (2) output paths (`skill-packs-catalog.json` at root, dashboard/outputs/ reserved for json-render specs) already match aeon-agent conventions, (3) `gh api` access pattern (no curl + env-var headers) already matches CLAUDE.md sandbox guidance. `skill-packs.json` was backported on May-24 (PR #59) so the input source is in place. Raw-URL example in body still points at `aaronjmars/aeon/main/skill-packs-catalog.json` because that is the upstream URL external consumers fetch — fork-local catalog publishes at its own raw URL once it runs. Registered disabled in aeon.yml with Tuesday 09:00 UTC schedule (matches upstream slot — first quiet weekday after Monday intelligence stack). Added to skills.json (total 93→94, category dev). Cadence: operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17, fork-first-run-alert May-17→18, fork-skill-gap May-18→19, competitor-launch-radar May-19→20, contributor-spotlight May-21→23, install-skill-pack+registry May-22→24, ecosystem-pulse May-24→26, fleet-skill-adoption May-26→27, sparkleware-catalog May-27→28 (aeon-agent PR #66) |
| fork-health-score skill (aeon) | 2026-05-29 | May-28 idea #3. New `skills/fork-health-score/SKILL.md` — weekly per-fork health tier (ACTIVE/WARM/STALE/QUIET) synthesizing push recency + enabled-skill count + 30d PR throughput into a normalized 0-100 score (push 50% / skills 30% / PRs 20%) plus a top-10 ACTIVE leaderboard and a single "X of N forks are ACTIVE" fleet number. Pairs with the Sunday fleet stack (fork-cohort 19:00 / fork-skill-gap 21:00 / fleet-skill-adoption 22:00) as the Monday-morning synthesis view at 10:45 UTC. Reuses fork-cohort-state.json when fresh (≤8d); live-API fallback otherwise. Per fork: 3 gh api calls (repos/{fork} + contents/aeon.yml + pulls?state=closed). Hard ACTIVE floor of ≥2 enabled skills prevents high-push-recency-low-config placeholder forks from claiming the tier on score alone. All WoW deltas on ADOPTION_RATIO percentage points, never raw counts (denominator drifts as forks activate/deactivate — computing on absolute counts manufactures phantom movement). Bot allowlist (dependabot/github-actions/aeonframework) filtered before scoring. Capped at 80 forks/run, trim by stargazers desc. Gated notify policy: baseline run, ≥10pt ACTIVE-ratio shift, ≥3 top-10 churns, or ≥3 tier transitions; QUIET otherwise (article + state still write). 8-state exit taxonomy. Read-only across the fleet (never opens PRs, never comments, never edits fork files). Registered disabled in aeon.yml. skills.json total 159→160 (aeon PR #271) |
| pr-skill-triage backport (aeon-agent) | 2026-05-29 | 16th consecutive same-day-after backport. Verbatim backport of upstream aeon PR #259 (pr-skill-triage, merged 2026-05-28). New `skills/pr-skill-triage/SKILL.md` — workflow_dispatch-only structured triage receipt for inbound PRs that introduce/modify SKILL.md files on aaronjmars/aeon-agent. Per-skill security scan reuses `skills/skill-security-scan/scan.sh` verbatim (no forked patterns) for PASS/WARN/BLOCK verdict + first-3 HIGH findings; required-secrets enumeration (extracts $VAR patterns, drops known-safe set GITHUB_TOKEN/GH_TOKEN/today/var/PR_NUMBER/HEAD_SHA); cron slot-conflict check vs aeon.yml (exact = CONFLICT, ±5min same DoW = ADJACENT, workflow_dispatch = OK); quality signals (description ≥40 chars, ≥3 steps, ./notify call present, tags non-empty). One PR comment per (PR, head_sha) — dedup keyed on head SHA prevents re-comment storms. Operator decides merge (no auto-merge, no labels, no Reviews API calls). Verdict precedence: BLOCK (HIGH or hard conflict) > WARN (MEDIUM/missing/adjacent/secrets) > OK. Gated notify: BLOCK and COMMENT_FAILED always; WARN only if MEDIUM security finding present. 9-state exit taxonomy. Adaptations vs upstream: (1) all `aaronjmars/aeon` → `aaronjmars/aeon-agent` (gh api paths, gh pr comment invocations, exit-status descriptions, notify message PR URL); (2) ./notify single-positional-arg style already aligned (no adaptation needed); (3) scan.sh shape matches upstream (May-18 PR #186 Bash 3.2 + May-20 PR #197 POSIX-ERE hardening intact, fork-local "upstream aeon AntFleet finding H6" comment preserved); (4) yq fallback path preserved as-is. Registered disabled, workflow_dispatch only, between pr-review and auto-merge in aeon.yml. skills.json total 94→95, alphabetical insert between pr-review and pr-triage. Cadence: operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17, fork-first-run-alert May-17→18, fork-skill-gap May-18→19, competitor-launch-radar May-19→20, contributor-spotlight May-21→23, install-skill-pack+registry May-22→24, ecosystem-pulse May-24→26, fleet-skill-adoption May-26→27, sparkleware-catalog May-27→28, pr-skill-triage May-28→29 (aeon-agent PR #68) |
| column tab groups (minitor) | 2026-05-29 | May-28 idea #5. Per-column tab groups for decks. When ≥1 column in the active deck has a non-empty `tabGroup` label, a tab bar renders above the column grid; clicking a tab filters visible columns to those sharing the group, with an implicit "All" tab always present. **Key UX decision: untagged columns ride along with every named tab too** — a half-grouped deck (e.g. 2 labeled "DeFi" + 6 unlabeled) stays usable instead of going blank when "DeFi" is clicked, otherwise the feature reads as broken. 10 files, +649/-44: drizzle/0006_tab_groups.sql + journal entry + meta/0006_snapshot.json (additive nullable `tab_group` text column on `columns`; 0006 not 0007 — 0005 was deck_snapshots on May-27 and no migration landed since); lib/db/schema.ts + lib/columns/types.ts (tabGroup field threaded through schema + Column shape); app/actions.ts (+93 — TAB_GROUP_MAX=50 const exported, updateColumnTabGroup server action with whitespace collapse + trim + length cap, Zod field on importedColumnSchema, exportDeck round-trip, importDeck re-normalization on import so hand-edited payloads can't smuggle "  AI  " and "AI" as two distinct buckets, loadSnapshot read); lib/store/use-deck-store.ts (+45 — updateTabGroup mirror action with identical normalization rule so optimistic state can't drift, **selectedTabByDeck per-deck view state NOT persisted** by design — clears on reload, restores on deck-switch in-session, same shape as autoFetchingIds; TAB_GROUP_ALL = "__all__" sentinel exported to avoid both files re-deriving the string); components/column/configure-column-dialog.tsx (+28 — "Tab group" text input with LayoutGrid icon under the show-only/hide section, normalized on save); components/deck/deck-board.tsx (full rewrite +99/-22 — tab bar above the grid when ≥1 column has a group, derived in column-position order so operator's column reorder is the visual tab order, useEffect-driven fallback to All when the selected tab's last column moves away, per-tab badge with column count; DnD reorder still works within the visible subset — moving a column out of a tab requires explicitly editing the Tab group field); lib/deck-templates.ts (DeckTemplateColumn.tabGroup?: string so future multi-category starter decks can ship pre-grouped instead of forcing the operator to label each column on import). Deck export / import / share-link fragment all round-trip the new field backward-compatibly (decks without it default to "no group"). Could NOT run Next 16 build/typecheck (offline sandbox) — manual review only. (minitor PR #53) |
| capabilities taxonomy parity CI check (aeon) | 2026-05-30 | Issue #301. CI parity check across 3 locations where the 6-value capability taxonomy lives (ALLOWED_CAPABILITIES array + docs/CAPABILITIES.md table + header comment in install-skill-pack). New scripts/check-capabilities-parity.sh (+138) + .github/workflows/ci-capabilities-parity.yml (+41). (aeon PR #304) |
| fork-health-score backport (aeon-agent) | 2026-05-30 | 17th consecutive same-day-after backport of upstream aeon PR #271 (fork-health-score). Weekly per-fork health tier ACTIVE/WARM/STALE/QUIET blending push recency + enabled skill count + 30d PR throughput. Zero adaptation needed vs upstream. (aeon-agent PR #70) |
| heartbeat $(date) self-fix (aeon-agent) | 2026-05-30 | Self-improve: skills/heartbeat/SKILL.md line 21 — `gh run list --created=$(date ...)` → `gh run list --created=${today}`. Same runner-hook anti-pattern chain as PRs #63/#67. (aeon-agent PR #71) |
| per-column collapse (minitor) | 2026-05-30 | Per-column collapse/expand UI. Click chevron in column header to fold to 48px strip; click to expand. View-state only, NOT persisted. Auto-refresh, alerts, and filters continue running while collapsed. (minitor PR #55) |
| token-report treasury wallet tracker (aeon) | 2026-05-31 | May-30 idea #1. Extends skills/token-report/SKILL.md with treasury wallet balance tracking from .x402books/wallets.json via BaseScan/Alchemy/WebFetch. Gas reserve low alert fires even on QUIET verdicts. (aeon PR #306) |
| upstream-gap skill (aeon-agent) | 2026-05-31 | May-30 idea #4. New weekly Monday 12:00 UTC diff of this fork's skills/ against upstream, tiered URGENT/STALE/FRESH by days pending. Cold-start: days_pending from upstream_merged_at so a 14d-old unbackported skill is URGENT on day 1. (aeon-agent PR #72) |
| column data export — JSON (minitor) | 2026-05-31 | May-30 idea #5. "Download items (JSON)" in every column's More-menu. Browser-native download via Blob + URL.createObjectURL. Versioned envelope (schema: minitor.column-export.v1). (minitor PR #56) |
| capabilities-map skill (aeon) | 2026-06-01 | May-30 idea #2. New weekly Monday 11:30 UTC read-only audit of which capability tiers each skill covers. Reads CAPABILITIES.md + skills.json + aeon.yml + skill-packs. 7-row matrix (6 tiers + undeclared). First consumer of the capabilities taxonomy schema. (aeon PR #313) |
| spend-monitor backport (aeon-agent) | 2026-06-01 | 18th consecutive same-day-after backport of upstream aeon PR #272 (spend-monitor). Daily 12:00 UTC API spend watchdog against WEEKLY_BUDGET_CAP. Two adaptations: notify call style + pricing table kept in sync with local cost-report rates. (aeon-agent PR #74) |
| pr-merge-queue skill (aeon) | 2026-06-02 | May-30 idea #3. New daily 09:45 UTC digest of all open PRs bucketed by touched-file risk tier (CORE_REVIEW > INFRA_REVIEW > SKILL_WARN_OR_BLOCK > SKILL_PASS > FAST_TRACK > UNKNOWN). Reuses skill-security-scan verbatim. Re-notify gated on head SHA. (aeon PR #318) |
| follow-up-patrol backport (aeon-agent) | 2026-06-02 | 19th consecutive same-day-after backport of upstream aeon PR #272 (follow-up-patrol). Weekly Tuesday 11:00 UTC escalation audit of MEMORY.md follow-up section. CRITICAL/HIGH/MEDIUM/WATCH tiers by age. Three notify/date adaptations vs upstream. (aeon-agent PR #76) |
| column quick-search (minitor) | 2026-06-02 | Per-column ephemeral substring-search input. Pairs with tab groups/collapse/export. Ephemeral view-state (NOT persisted) that narrows ON TOP OF include/exclude filters, never widens past them. Collapsed strip shows Search icon when query active. (minitor PR #58) |
| repo-pulse $(date) self-fix (aeon-agent) | 2026-06-02 | Self-improve: skills/repo-pulse/SKILL.md step 2 — `CUTOFF=$(date -u -d '24 hours ago' ...)` → literal computed from `${today}` minus 1 day. Same runner-hook anti-pattern chain as PRs #63/#67/#71. (aeon-agent PR #77) |
| pr-merge-queue backport (aeon-agent) | 2026-06-03 | 20th consecutive same-day-after backport of upstream aeon PR #318 (pr-merge-queue). Two adaptations: default target aeon→aeon-agent; notify already aligned. Bucketing rubric preserved verbatim. (aeon-agent PR #79) |
| ecosystem-entrants skill (aeon) | 2026-06-03 | Jun-02 idea #1. New weekly Monday 11:45 UTC diff of ECOSYSTEM.md against prior snapshot. Reports added/removed/updated entries; PR enrichment via search/issues (14d window). Read-only against ECOSYSTEM.md. (aeon PR #339) |
| column pinning to front (minitor) | 2026-06-03 | Jun-02 idea #4. Per-column pin-to-front toggle (DB-backed, survives reloads). Pinned columns render before all unpinned regardless of DnD position and active tab. Stable two-pass partition. DnD across pin/unpin is intentional no-op. (minitor PR #59) |
| wallet-risk-weekly skill (aeon) | 2026-06-04 | Jun-02 idea #2. Monday 11:15 UTC weekly risk audit of Base wallets in .x402books/wallets.json. First scheduled consumer of HoundFlow security pack. HIGH-only notify. State in memory/topics/wallet-risk-state.json. skills.json 181→182. (aeon PR #340) |
| repo-article $(date) self-fix (aeon-agent) | 2026-06-04 | Self-improve: step 1 since cutoff from $(date ...) to literal from ${today} minus 7 days. Runner hook blocks $(…). Same pattern as PRs #63/#67/#71/#77. (aeon-agent PR #81) |
| narrative-convergence backport (aeon-agent) | 2026-06-04 | 21st same-day-after backport. Upstream aeon PR #272. Daily 13:00 UTC cross-skill convergence detector. skills.json 98→99. (aeon-agent PR #80) |
| per-column duplicate (minitor) | 2026-06-04 | 6th rung on per-column UX axis. Clones column settings inserted immediately after source. Color inherited, pinned dropped. (minitor PR #60) |
| atrium-catalog-watcher skill (aeon) | 2026-06-05 | Jun-04 idea #2. Friday 12:00 UTC weekly Atrium marketplace diff. skill_id as canonical key. skills.json 182→183. (aeon PR #342) |
| mcp-pulse backport (aeon-agent) | 2026-06-05 | 22nd same-day-after backport. Upstream aeon PR #272. Friday 10:00 UTC MCP ecosystem tracker. skills.json 99→100. (aeon-agent PR #82) |
| per-column color labels (minitor) | 2026-06-05 | 7th rung on per-column UX axis. 8-swatch palette + freeform hex input. Server-authoritative /^#[0-9a-f]{6}$/i normalization. Color inherited on duplicate. (minitor PR #61) |
| ecosystem-links skill (aeon) | 2026-06-06 | Jun-04 idea #1. Monday 11:55 UTC URL-health audit of ECOSYSTEM.md. INCONCLUSIVE two-strike rule. XONLY out-of-scope. skills.json 193→194. (aeon PR #351) |
| $(date) batch self-fix — repo-actions + star-momentum-alert (aeon-agent) | 2026-06-06 | Jun-04 idea #4. Closes shell-substitution anti-pattern chain last 2 sites. (aeon-agent PR #83) |
| self-improve cron-state primary (aeon-agent) | 2026-06-06 | Self-improve: reads memory/cron-state.json directly; ./scripts/skill-runs demoted to fallback. (aeon-agent PR #84) |
| per-deck color labels (minitor) | 2026-06-06 | Deck-level analog of column color labels PR #61. Reuses normalizeColumnColor + 8-swatch palette. Operator color overrides active-deck brand. (minitor PR #62) |
