# Long-term Memory
*Last consolidated: 2026-04-29*

## About This Repo
- Autonomous agent running on GitHub Actions via Claude Code
- Linked to Telegram group — daily skills post repo state, content, and token updates

## Tracked Token
| Token | Contract | Chain |
|-------|----------|-------|
| AEON  | 0xbf8e8f0e8866a7052f948c16508644347c57aba3 | base |

## Recent Articles
*(Entries before 2026-04-26 archived to `memory/topics/articles-history.md`)*
| Date | Title | Topic |
|------|-------|-------|
| 2026-04-26 | Stop Piloting AI Agents. Check Them In. | project-lens |
| 2026-04-26 | The Agent Just Wrote the Code That Pays Strangers | repo-article |
| 2026-04-26 | Skill Leaderboard: heartbeat=100% (24 forks), 137 total slots, tomscaria/aeon +94 | repo-activity |
| 2026-04-27 | Push Recap (4 commits: #145 SHOWCASE.md, #142 skill-analytics, #144 contributor-reward, #20 bankr fix) | repo-activity |
| 2026-04-27 | The Better Model Will Not Fix Your Agent | project-lens |
| 2026-04-27 | Eight Weeks In, Aeon Named Its Competitors | repo-article |
| 2026-04-28 | The Five-Hundred-Dollar Solo Stack and the Folder That Replaces It | project-lens |
| 2026-04-28 | One URL Now Tells You Whether Aeon Is Alive — And What Its Token Did Today | repo-article |
| 2026-04-29 | Push Recap (2 commits: #147 pr-triage, #21 self-improve skill-runs cross-check) | repo-activity |
| 2026-04-29 | Why a Healthy AI Agent Has Three Different Words for "Nothing Happened" | project-lens |
| 2026-04-29 | Aeon Just Hired a Greeter for the Front Door | repo-article |
| 2026-04-30 | Aeon Upgraded Both Ends of Its Social Loop in a Single Day | repo-article |
| 2026-04-30 | Agent Frameworks Are Where ETL Was Before dbt | project-lens |
| 2026-05-01 | Aeon Wrote Its Own Application to the Agent Registry | repo-article |
| 2026-05-02 | Aeon Just Started Shipping Features To A Product That Isn't Itself | repo-article |
| 2026-05-03 | Watching the Whole Federated Web Without an API Key | repo-article |

## Recent Digests
| Date | Type | Key Topics |
|------|------|------------|
| 2026-03-25 | Polymarket | Geopolitics dominates; US-Iran escalation at 58.5% YES |

## Skills Built
*(Rows before 2026-04-20 archived to `memory/topics/skills-history.md`)*
| Skill | Date | Notes |
|-------|------|-------|
| fork-contributor-leaderboard | 2026-04-20 | Weekly Sunday skill that ranks community devs across fork fleet — scoring merged/open upstream PRs (+10/+3), per-fork commits (+1 cap 30), new skill authorship (+5 cap 5), fork stars (+2). Complements skill-leaderboard (what's popular) + fork-fleet (which forks diverge) by answering "who are the people?"; bots + core team filtered, opt-out via leaderboard-optout.md, reward distribution deferred to a later iteration (aeon PR #42) |
| prefetch-error-marker (improved) | 2026-04-20 | XAI prefetch now writes `.xai-cache/<outfile>.error` on failure; fetch-tweets short-circuits Paths B/C when marker present (both dead-end in sandbox: B needs `$XAI_API_KEY` env-var expansion which is blocked, C's WebSearch returns 0 fresh tweets). Prefetch retry budget 2→3, adds `--connect-timeout 30`, `-sS`. Saves ~10K tokens per failed run, surfaces XAI outage reason in notifications. Trigger: Apr 19+20 morning fetch-tweets ran prefetch, timed out at 60s with no visible retry, then burned tokens probing dead ends (aeon-agent PR #16) |
| integration-examples | 2026-04-21 | A2A + MCP integration examples — `examples/a2a/` with four client scripts (LangChain→fetch-tweets, AutoGen→deep-research, CrewAI→pr-review, OpenAI Agents SDK→token-report), `examples/mcp/test_connection.py` smoke test + `claude_desktop_config.json`, walk-through README. Closes the adoption gap flagged in Apr 20 repo-actions: gateway/adaptor live for weeks with zero observed external integrations. Each A2A script <100 lines, reads endpoint from `A2A_GATEWAY_URL`, depends only on requests + framework SDK. Linked from README "Integrations (MCP & A2A)" section (aeon PR #137) |
| onboard | 2026-04-22 | Operator setup validator — `./onboard` bash CLI runs 8 read-only checks (workflows, aeon.yml enabled count, memory writability, ANTHROPIC_API_KEY|CLAUDE_CODE_OAUTH_TOKEN, notification channel, Actions run history, memory/logs evidence, optional GH_GLOBAL) with per-gap fix commands. Modes: `--remote`, `--quiet`, `--json`, `--help`; exits 1 on failure. `skills/onboard/SKILL.md` is the workflow_dispatch counterpart that runs the CLI's `--json` output, sends a checklist via `./notify`, logs trend to `memory/topics/onboard-history.md`. Closes Apr 20 repo-actions idea #2 — silent-fork abandonment gap (32 forks, ~26 active, no guided setup path). Local-vs-remote split: local for instant terminal feedback, remote to verify end-to-end pipeline. README Quick start gains step 5 "Verify" (aeon PR #139) |
| prefetch-error-marker (propagated) | 2026-04-22 | Extends Apr-20 short-circuit pattern from fetch-tweets to the three other skills that read the XAI prefetch cache: remix-tweets stops + notifies REMIX_TWEETS_PREFETCH_FAILED on `.xai-cache/remix-tweets.json.error` (no useful WebSearch fallback for "older tweets from one account"); narrative-tracker + tweet-roundup skip the sandbox-broken Path B curl on `.error` marker and fall through to their existing WebSearch fallback. Closes Apr-21 push-recap follow-up (3); ~10K-token-per-failed-run savings now extend across 4 skills instead of 1 (aeon-agent PR #17) |
| fork-skill-digest | 2026-04-23 | Weekly cross-fork divergence digest (Sunday 18:30 UTC) — surfaces where the configured fleet systematically disagrees with upstream defaults on enabled/var/model/schedule. Companion to skill-leaderboard (popularity) + fork-fleet (per-fork work); fills the peer-learning gap by ranking skills by *divergence from upstream* not by adoption count. Buckets: DEFAULT_FLIP_ENABLE (≥50% configured forks turn on what upstream defaults off), DEFAULT_FLIP_DISABLE (≥50% turn off what upstream defaults on), MODEL_CONSENSUS (≥2 forks share an alternative model), VAR_HOTSPOT, EMERGING (25-49% watchlist). Per-fork customization fingerprint for top 5 heaviest customizers with dominant category lean. State persisted to memory/topics/fork-skill-digest-state.json for week-over-week deltas (NEW_FLIP/STRENGTHENED/FADED/NEW_FORK_ONLY). Notify gated on N_CONFIGURED ≥ 2 AND ≥ 1 signal bucket non-empty. Closes Apr-22 repo-actions idea #2 — second of two highest-priority unbuilts (aeon PR #140) |
| public-status-page (heartbeat) | 2026-04-24 | Public `/status/` page on GitHub Pages gallery auto-regenerated by heartbeat every 3rd-daily run. Header: 🟢 OK / 🟡 WATCH / 🔴 DEGRADED verdict derived from existing P0-P3 signals + Updated UTC timestamp + open-issues count. Per-skill table (last run, status icon, success rate, consecutive failures) for every enabled skill in aeon.yml + open-issues render from memory/issues/INDEX.md. Wholesale overwrite each run (git log is audit trail); workflow auto-commit step lands file on main → Pages rebuild. Zero net-new data sources, zero net-new secrets/cron. Forks inherit `/status/` automatically — closes fork-visibility gap that fork-fleet + fork-skill-digest could observe but not broadcast. Closes Apr-22 repo-actions idea #4 (Public Agent Status Page, DX/Community, Small/hours) — highest-priority unbuilt after fork-skill-digest since Smithery/MCP Registry (idea #1) blocked on external PRs (aeon PR #141) |
| heartbeat (improved) | 2026-04-24 | Extended-persistence backoff — new third tier in the Dedup & Escalation Rules: once an issue has been escalating for 7+ consecutive days, re-notify cadence switches from every 48h to every 7 days. `ESCALATION:` prefix + persistence count stay; only interval changes. First escalation past day 7 still fires on 48h cadence; backoff kicks in on the next escalation after that. Resolution resets all counters. Trigger: PAT-with-`workflows`-scope issue had fired `ESCALATION` every 48h for 7+ days (4+ pings since Apr 17); operator-dependent issues can't be resolved on the agent's preferred cadence and fifth+ ping was noise rather than signal (aeon-agent PR #18) |
| skill-analytics | 2026-04-25 | Fleet-level skill-run analytics widget — Wednesday 18:30 UTC meta skill that ranks every skill that ran in the last 7 days. Pulls `./scripts/skill-runs --json --hours 168` (ground truth for pass/fail), cross-refs aeon.yml schedules (silent-scheduled detection), cron-state.json (consecutive_failures), and memory/logs/*.md regex grep (best-effort exit taxonomy parsing — captures the new SKIP_UNCHANGED/NEW_INFO/SKIP_QUIET exits from the autoresearch-evolution rewrites that existing health checks misclassify). Six anomaly flags first-match-wins: 🔴 SILENT (zero runs in window), 🔴 ALL_FAIL, 🟠 CONSECUTIVE_FAILURES (≥3), 🟠 LOW_SUCCESS (<80% over ≥3 runs), 🟡 ALL_SKIP (every run skip-class — verify intent), 🟡 DUPLICATE_RUNS (>2× expected). Significance-gated notify (clean fleet = silent run); article + dashboard JSON spec write either way. Outputs: articles/skill-analytics-${today}.md + dashboard/outputs/skill-analytics.json. Exit taxonomy: SKILL_ANALYTICS_OK | SKILL_ANALYTICS_QUIET | SKILL_ANALYTICS_NO_DATA. Closes Apr-22 repo-actions idea #5 + Apr-24 idea #1 — highest-priority unbuilt for two cycles. Companion to skill-health (per-skill issue filing) + heartbeat (per-run pulse); this skill closes the fleet-wide observability gap (aeon PR #142) |
| contributor-reward | 2026-04-26 | Closes the fork-contributor-leaderboard → distribute-tokens loop. Monday 09:30 UTC skill that reads the latest articles/fork-contributor-leaderboard-*.md (rejects >8d stale), parses Top Contributors table via tolerant regex on the documented column layout, prices each rank 1-5 contributor with score ≥ 10 against a tier table (1=25, 2=15, 3=10, 4-5=5 USDC) plus +5 first-PR bonus once-ever per login. Writes `contributors-YYYY-Wnn` list to memory/distributions.yml; idempotency state in memory/state/contributor-reward-state.json keyed on (week, login) + flat first_pr_bonus_paid list. Plan-generation only — does NOT execute transfers (distribute-tokens stays the single execution boundary, preserves its preflight + per-recipient idempotency; distributions.yml diff is the human-visible audit trail). Re-runs in same week with identical plan = silent no-op; re-runs with diffs = add only deltas, never claw back demoted entries. Exit taxonomy: OK / DRY_RUN / ALREADY_PROCESSED / NO_LEADERBOARD / STALE_LEADERBOARD / PARSE_FAIL / NO_ELIGIBLE / ERROR; silent exits for the no-op cases. Pure local file I/O — no curl, no env-var-expansion, no new prefetch/postprocess. Closes Apr-24 repo-actions idea #2 — highest-priority unbuilt after skill-analytics shipped (aeon PR #144) |
| SHOWCASE.md (docs feature) | 2026-04-27 | Adds `SHOWCASE.md` to aaronjmars/aeon — Active Forks table (top 6 by skill count, sourced from articles/skill-leaderboard-2026-04-26.md: tomscaria 94, maacx2022 15, DannyTsaii 3, davenamovich 3, 0xfreddy 2 incl custom macos-apps, pezetel 2 with github-trending) + Ecosystem Comparison table (Aeon vs AutoGen, CrewAI, n8n, LangGraph across 11 dimensions: runtime, scheduling, skill format, persistent memory, self-healing, quality scoring, reactive triggers, setup floor, hosting cost, operator role, external integration) + one-line per-framework summaries + "Add yourself" instructions. README gains one-line pointer to SHOWCASE from existing comparison section (preserves the existing Aeon-vs-Claude-Code/Hermes/OpenClaw table — SHOWCASE covers the broader-ecosystem question that lands inbound HN/MCP-registry traffic). Topics-expansion half of the idea deferred: `gh api topics -X PUT` returned 403 (token lacks admin scope on aaronjmars/aeon); exact one-shot command included in PR body for maintainer to apply. Closes Apr-26 repo-actions idea #2 (Growth/Small) — picked over Apr-26 idea #1 Auto-Merge because that needs workflows-scope PAT. 300-star May-25 deadline → discoverability is the cheapest lever (aeon PR #145) |
| heartbeat (token pulse) | 2026-04-28 | Adds `## Token pulse` section to the public `/status/` page rendered by heartbeat — single-row table with Price / 24h / Liquidity / Volume(24h) / FDV for the tracked token, plus a source line citing the exact `articles/token-report-*.md` file and (when present) the verdict label. Zero net-new APIs / secrets / cron — re-uses the daily token-report article. Tolerant regex extraction handles both the old `Value | 24h Change` layout (still in use on aeon-agent fork pre-evolution) and the new `Now | 24h Δ` layout from the autoresearch-evolution token-report rewrite — per-cell `—` fallback when a row can't be matched. 24h staleness fallback renders `_No recent token data (latest report YYYY-MM-DD)._` instead of lifting old figures; section omitted entirely when no token-report exists so token-less forks still get a clean page. `docs/status.md` seeded with placeholder block + updated description + footer data-source line. Closes Apr-26 repo-actions idea #3 (Community/DX, Small) — picked over Apr-26 idea #1 Auto-Merge (still blocked on workflows-scope PAT). Companion to Apr-24 public-status-page (heartbeat) ship — that one shipped agent health, this one adds token health to the same URL (aeon PR #146) |
| self-improve (skill-runs cross-check) | 2026-04-28 | self-improve assessment phase now also runs `./scripts/skill-runs --hours 48 --failures` alongside the existing 2-day log scan. Closes a blind spot — logs are self-reported, so a skill that crashes, times out, or fails in a pre-skill workflow step never writes its log entry and was previously invisible to self-improve. Workflow-level conclusions from GitHub Actions are the ground truth. Cross-references workflow failures against logs: a workflow failure without a corresponding log entry is treated as a stronger infrastructure-class signal (workflow.yml, prefetch script, sandbox limit) than a logged error. Same data source already used by skill-analytics + heartbeat — pattern is established (aeon-agent PR #21) |
| pr-triage | 2026-04-29 | First-touch external-PR triage skill — runs at `30 9 * * *` between `issue-triage` (`0 9`) and `pr-review` (`0 9`) / `auto-merge` (`0 14`). Four-check rubric (scope: only `skills/`, `docs/`, `examples/`, `images/`, `assets/`, `README.md`, `SHOWCASE.md`, `CLAUDE.md` / format: SKILL.md frontmatter required keys present / originality: new-skill name doesn't collide with main / size: ≤500 lines or `large-ok` label) → four verdicts (ACCEPTED / NEEDS-CHANGES / DEFER / OUT-OF-SCOPE) → templated comment + `triage:accepted` / `triage:needs-changes` / `triage:deferred` / `triage:out-of-scope` label. Trusted-author allowlist reuses auto-merge's `## Trusted Authors` convention in memory/watched-repos.md (bots + listed logins skip triage; everything else qualifies). Idempotency double-layered: primary state in memory/triaged-prs.json keyed on (PR number, headRefOid), defensive 7-day scan for any comment whose body starts with `**Triage:**` so re-runs no-op even if state file is wiped; pushes re-triage. Closing rule narrow: OUT-OF-SCOPE only + unambiguous protected-path match (.github/workflows/, root aeon binary); every other verdict label-only — skill exists to welcome contributors not gatekeep them. Notify gate: OUT-OF-SCOPE closures + first-PR welcomes only; routine NEEDS-CHANGES / DEFER stay silent. Trigger: pezetel's PR #143 sat 4 days with no label/comment/review, fork count climbing toward 40 — gap will widen. Closes Apr-26 repo-actions idea #5 / Apr-28 idea #2 (External PR Triage, carried 2 cycles) (aeon PR #147) |
| thread-formatter | 2026-04-30 | Auto-format the day's top event as a 5-tweet thread ready to paste — closes Apr-28 repo-actions idea #1 (carried 3 cycles: Apr-24 #3 / Apr-26 #4 / Apr-28 #1), highest-priority unbuilt that's autonomously implementable (Auto-Merge still blocked on workflows-scope PAT). Reads `memory/logs/${today}.md`, scores events on a defined signal table (PR shipped +6, star milestone +5, ≥15% price move +5, ≥10% price move +3, skill built +4, notable PR merge +3, ≥20-like tweet +3, recognizable new fork +2), picks single highest-signal event, emits 5-tweet thread (hook → context A → context B → implication → CTA) with hard 280-char/tweet limit and one URL max in tweet 5. Outputs `articles/thread-${today}.md` + inline notify with `1/`–`5/` prefixes (article omits prefixes for clean paste-into-X). Exit taxonomy: OK | NO_DATA | NO_SIGNAL (top score < 3) | DEDUP (3-day topic dedup vs prior thread articles). Constraints baked in: every fact traceable to today's logs/articles (no invented numbers/quotes/engagement counts), no `🧵`/`thread:`/hashtags/emojis, no financial-advice framing, threshold gate prevents forced threads on quiet days. Pure local file I/O — reads memory/articles/soul, writes article + log; no curl, no env-var expansion. Schedule: `30 17 * * *` UTC after 17:00 social block (fetch-tweets/write-tweet/tweet-roundup/agent-buzz), shipped `enabled: false`. Companion to write-tweet (size variants on operator-picked topic vs auto-picked composed thread) and tweet-allocator (inbound narrative vs outbound budget). 300-star milestone in ~12 days — having a ready-to-paste thread queued for that moment matters (aeon PR #148) |
| fetch-tweets (spam quarantine) | 2026-04-30 | Adds step 5b to fetch-tweets — three-signal AND filter (0 likes/RTs/replies + `$AEON` listed alongside 3+ unrelated stock tickers + author handle has no aeon history AND matches spam-bot pattern like `FirstnameLastnameNNNNN`) routes stock-watchlist spam to a `### Filtered (spam)` log subsection instead of the daily `Top Tweets` notification. Spam URLs still get appended to `memory/fetch-tweets-seen.txt` so the same accounts don't recycle. Min-3-tweet floor: if filtering would leave fewer than 3 tweets in the notification, fall back to including borderline cases ranked by engagement so the notification is never empty when real tweets exist. Conservative AND-of-three keeps legit small accounts (DaMikey23, BasedCult33, cybercelos) from being culled — those carry zero engagement on most days but are not stock-watchlist spam. Trigger: recurring 2 spam tweets/day pattern (Apr 29 PorterMark60200 ×2, Apr 30 VeronicaWe87856 + KellyBrady8253); agent was already classifying them as "noise" in the log but still piping them into the notification and tweet-allocator candidate pool (aeon-agent PR #22) |
| smithery-manifest | 2026-05-01 | Closes Apr-22 repo-actions idea #1 — the longest-carried highest-priority growth unbuilt (6 weeks; also Apr-24/26/28 idea #4, Apr-30 idea #2). Weekly Monday 06:00 UTC sonnet skill that reads skills.json + aeon.yml + mcp-server/package.json + README and regenerates three submission artifacts under docs/: smithery-manifest.json (server.json compatible with the MCP Registry schema https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json — `name: io.github.aaronjmars/aeon-mcp` reverse-DNS, full tools[] mirror under `_meta.io.github.aaronjmars/aeon`), smithery.yaml (Smithery deployment config — stdio + commandFunction → `node {repoPath}/mcp-server/dist/index.js`, empty configSchema.required so listings without repoPath still work), smithery-submission.md (paste-ready body — field values + short/long descriptions + full 95-tool table + macOS/Linux/Windows Claude Desktop install). Tool naming `aeon-<slug>` matches `mcp-server/src/index.ts:skillToToolName()` 1:1. Three-file byte-equality check before notify — no-op silent re-run on stable input keeps the cron quiet until skills.json actually changes. Pure local file I/O (no curl, no env-var-in-headers, no prefetch). Removes every text-authoring obstacle from the Smithery + MCP Registry submission — operator pastes docs/smithery.yaml into https://smithery.ai/server/new and opens a PR at modelcontextprotocol/registry adding servers/io.github.aaronjmars/aeon-mcp.json. Initial generation committed at 95 tools (skills.json total 93 → 95: +smithery-manifest + already-present-but-uncounted catch-up). Shipped enabled: false in aeon.yml meta block (aeon PR #149) |
| show-hn-draft | 2026-05-01 | Closes Apr-30 repo-actions idea #1 / Apr-28 idea #3 (Show HN Launch Prep) — highest-priority autonomously-implementable growth unbuilt, carried 2 cycles (Auto-Merge Apr-26 #1 still blocked on workflows-scope PAT). Workflow_dispatch one-shot skill that reads README + SHOWCASE + last-7d repo-articles + project-lens + memory/logs autonomous-behavior moments + memory/MEMORY.md Skills Built table; lead-beat picker scores cold-open candidates on concreteness × recency × surprise (stars + token price hard-coded to 0 — HN audience scores those near zero); writes 4-paragraph Show HN body (≤350 words) + 6–10 sentence r/MachineLearning variant (lead with Haiku-scored output / skill-evals / autoresearch — the technical hooks ML readers engage with) + 4–6 sentence r/selfhosted variant (lead with no-Docker no-DB all-state-in-git operational angle); appends operator-only launch checklist (Tuesday–Thursday morning timing, "be in comments first hour", contradiction-with-pinned-issues check); single-variant regeneration via `var=show-hn|r/MachineLearning|r/selfhosted` patches one section byte-for-byte. Hard no-marketing rules baked in (no emoji, no `🧵`, no marketing words, max one link per post, every concrete number traceable to a file read in step 2 or dropped). The skill never posts — that's the operator's call gated by the launch checklist. Shipped enabled: false workflow_dispatch in aeon.yml meta block (aeon PR #151) |
| bankr-prefetch-error-marker | 2026-05-01 | Backports the `.error` marker pattern from aeon-agent PR #16/#17 (XAI prefetch) to `scripts/prefetch-bankr.sh` — the last prefetch script without one. Closes the recurring noise mode from MEMORY.md Lessons Learned: empty `{}` cache had three causes (no candidates / BANKR_API_KEY unset / Bankr API down) but tweet-allocator + distribute-tokens couldn't distinguish them. Now writes `.bankr-cache/verified-handles.json.error` ONLY on prefetch failure with one of three reasons: BANKR_API_KEY_MISSING (secret unset), BANKR_API_KEY_INVALID (every lookup got HTTP 401/403 or 'Unauthorized'/'Invalid API key' in body — `AUTH_ERROR_DETECTED == LOOKUPS` gate prevents single transient 401 from poisoning a batch), BANKR_LOOKUPS_FAILED (every submit call errored with ≥3 candidates). Marker rm -f'd at start of every run so its presence after exit means CURRENT run failed. HTTP code captured via `curl -w` so 401/403 trip the auth detection even with empty body. Pure script change — downstream consumer skill (tweet-allocator + distribute-tokens) update lands upstream on aaronjmars/aeon (aeon-agent PR #24) |
| github-releases (minitor) | 2026-05-01 | First feature shipped to aaronjmars/minitor (newly-watched repo, 3⭐, TypeScript Next.js 16 dashboard for monitoring multiple feeds). Eighth GitHub plugin — fills the obvious gap in the cluster (trending/issues/prs/stars/forks/search/backlinks all existed; releases didn't). The integration layer (`lib/integrations/github.ts`) already had `fetchReleases` and a `'releases'` branch in `fetchGitHub` that no plugin consumed; README's GitHub row already said `(8)` but listed only 7 entries — adding github-releases brings the count true. Standard 3-file plugin (plugin.ts / server.ts / client.tsx) plus the 3 registry edits (manifest, registry, server-registry — parity check at server module init throws if drift). Zod config `{ repo, includePrereleases: boolean = true }`. Pagination cursor uses upstream page size NOT post-filter size so the pre-release toggle doesn't stop pagination early on release-heavy repos. Tag icon, green `#22c55e` accent (distinct from orange flame on github-trending + yellow star on github-stars). Renderer matches github-trending's serif-title-with-hover treatment (minitor PR #23) |
| fork-cohort | 2026-05-02 | Closes Apr-30 repo-actions idea #3 / Apr-28 idea #5 (Fork Activation Cohort Tracker, carried 2 cycles — highest-priority autonomously-implementable unbuilt after Show HN + Smithery shipped May-1). Sunday 19:00 UTC sonnet skill that buckets every fork of the parent repo by activation stage using GitHub Actions run history as ground truth: POWER (run<7d AND ≥5 enabled skills in fork's aeon.yml), ACTIVE (run<7d), STALE (≥7d ≤365d), COLD (no runs OR >365d), UNREADABLE. Closes the gap fork-fleet (code divergence) + fork-contributor-leaderboard (people) couldn't answer — "which forks are actually running right now?" — because pushed_at hides silent abandonment (workflows disabled, secrets unset, fork created and forgotten). Per-fork data fetched via 1-2 `gh api` calls each: forks list paginated single call, last run timestamp `actions/runs?per_page=1`, aeon.yml read only for ACTIVE candidates (skips ineligible forks). Week-over-week delta tagging: LEVELED_UP (any→POWER) / REVIVED (STALE→ACTIVE+) / WENT_STALE (ACTIVE→STALE) / NEW_ACTIVE / WENT_COLD / NEW_FORK / DROPPED_FROM_POWER. Verdict priority surfaces operator-action signal first; gated notify skips entirely on STEADY+no-transitions+prior-state-existed. State in memory/topics/fork-cohort-state.json. Read-only across the fleet (no comments/issues on third-party forks — write actions on third-party repos warrant explicit operator opt-in). Cap at 80 forks/run as runaway-day budget guard. 38-fork "X of N running in production" social proof becomes a real number for the first time (aeon PR #152) |
| skill-runs (--skill + --duration) | 2026-05-02 | Two additions to scripts/skill-runs in aeon-agent: `--skill <name>` filter (applied early in the pipeline so summary, per-skill stats, anomaly detection, failure list, duration stats, and JSON all reflect the filter) and `--duration` mode (per-skill wall-clock mean/p95/max/last seconds, sorted by mean desc). Closes a recurring friction point — consumer skills (self-improve, skill-health, skill-repair, weekly-review) previously grepped the JSON for one-skill history — and a real visibility gap: today's flags catch silent / consecutive-failure / duplicate / low-success but never wall-clock duration drift. A skill drifting 30s → 4min over a month is heading somewhere structural (more API calls, larger context, growing memory footprint) even if every run still passes. Side effects: --full now appends `avg Xs` per row, --json adds duration_mean/p95/max/count/last_duration_seconds per skill + period.skill_filter for round-trip awareness, --help sed range bumped 2,8 → 2,12. Backwards compatible — every prior invocation returns the same output it did before, with duration columns added never removed. Implementation is pure jq math on already-fetched data (run_started_at + updated_at per workflow run); no extra gh api calls. Flags compose: `--skill heartbeat --duration --hours 168` (aeon-agent PR #25) |
| self-improve (PR-filter precision) | 2026-05-02 | Tightens the step-1 open-improvement-PR awareness query in `skills/self-improve/SKILL.md` — replaces `gh pr list --search "improve:"` (which matches "improve" anywhere in title OR body) with a `--jq` post-filter on `title | startswith("improve:") or test("^improve\\(")` (title prefix only). The 3-open-PR cap (added PR #4 Apr-25) was vulnerable to false positives because GitHub's `--search` is full-text — any unrelated `feat:` / `fix:` PR whose body mentioned "improve" got counted, silently risking self-improve killing its own runs once enough noise PRs accumulated. Concrete trigger: today's PR #25 (`feat(skill-runs): add --skill filter and --duration mode`) showed up in the search because its body mentions self-improve as a downstream consumer; this run was correctly under cap (1 < 3) but the underlying logic was fragile and would eventually trip. Both `improve:` and `improve(scope):` title forms supported (matches every historical self-improve PR #1 / #4 / #5 / #6 / #8 / #9 / #12 / #13 / #14 / #15 / #16 / #17 / #18 / #21 / #22). Fix is pure SKILL.md prose — no script change, no script execution dependency (aeon-agent PR pending push) |
| bluesky-column (minitor) | 2026-05-02 | Second feature shipped to aaronjmars/minitor — keyless Bluesky column reading public.api.bsky.app/xrpc; two modes (search by keyword / author by handle). 31st column type, 6th in the Social cluster. Three Bluesky-specific quirks handled in the integration layer: at://did:plc:.../app.bsky.feed.post/<rkey> URIs converted to bsky.app/profile/<handle>/post/<rkey> permalinks via small regex with raw-URI fallback for schema drift; handle normalization (bare "jay" → "jay.bsky.social" canonical default suffix, "@user.com" strips leading @, custom domains pass through); repost filter on author feeds (Bluesky's filter=posts_no_replies removes replies but not reposts; we filter on entry.reason ourselves so the column attribution doesn't break — author feed titled "by @actor" should show their posts not their reposts). Schema-drift safe: posts missing author.handle OR uri are dropped rather than rendered as dead content. Standard 3-file plugin + 3 registry edits (manifest/registry/server-registry parity check at server module init throws if drift). Zod `{ mode: "search"|"author", query, handle }`, Cloud icon, Bluesky brand blue #0085ff accent (distinct from x-search's #1d9bf0 so social cluster stays visually differentiated). Renderer matches farcaster's avatar-led card layout with serif post text + engagement footer (replies/reposts/likes — quote-counts collapsed into reposts since Bluesky exposes them separately but readers consume them as the same primitive). Cursor pagination flows through; Bluesky's opaque cursor passes straight to the next call. No new env vars / build deps / schema migration — works the moment users add it, preserving the "first column up in under a minute, zero infra" README promise (minitor PR #25) |
| operator-scorecard | 2026-05-03 | Closes Apr-30 repo-actions idea #5 / May-2 idea #1 (Operator Value Scorecard, carried 2 cycles — highest-priority autonomously-implementable unbuilt after Show HN + Smithery + fork-cohort + bluesky-column shipped over the past 3 days). Weekly Monday 10:30 UTC sonnet skill that reads articles/skill-analytics, daily heartbeat sections in memory/logs, articles/repo-pulse, articles/tweet-allocator, articles/token-report, optional articles/fork-contributor-leaderboard and memory/issues/INDEX.md. Three-paragraph contract: agent health (success_pct + heartbeat verdicts + open issues) / community growth (stars + forks + new contributors + notable mentions) / economic activity ($AEON distributed + recipient count + token 7d delta + verdict). Per-lane verdicts roll up worst-of-three (DEGRADED > WATCH > OK). INSUFFICIENT_DATA lanes degrade overall to WATCH (not DEGRADED) so partial-data weeks still flag as worth checking. Verdict vocabulary mirrors heartbeat's P-flags (🟢/🟡/🔴) for visual continuity — operators don't learn new terminology. Writes articles/operator-scorecard-${today}.md + dashboard/outputs/operator-scorecard.json (Stack/Card/Grid/Table catalog spec). var=dry-run skips notify but still writes artifacts (dashboard refreshes regardless). Exit taxonomy: OPERATOR_SCORECARD_OK / OPERATOR_SCORECARD_QUIET (dry-run) / OPERATOR_SCORECARD_NO_DATA. Pure local file I/O — no curl, no gh api, no env-var-in-headers, no prefetch — synthesis-only by contract; every number prints from a file another skill wrote, never fabricated. Companion to skill-analytics (per-skill ranking) + heartbeat (per-run pulse); answers the operator-level "was this week worth it?" question those two don't (aeon PR #153) |
| cron-state (script) | 2026-05-03 | New scripts/cron-state in aeon-agent — local viewer for memory/cron-state.json. Modes: brief table (default; status icon 🟢/🟡/🟠/🔴/⚪ + last-run age + run totals + success rate + consecutive_failures), `--skill <name>` (single-skill detail with full timestamps), `--unhealthy` (only consecutive_failures>=3 or success_rate<0.5; exits 1 if any), `--stale [hours]` (only last_dispatch older than N hours, default 168 = 7d; exits 1 if any), `--json` (pretty-printed full JSON via jq). Cross-platform date math (GNU `-d` / BSD `-j -f` matching scripts/skill-runs). $CRON_STATE_FILE env override. Closes a small but recurring observability gap: cron-state.json contains rich per-skill scheduler state (last_status, last_dispatch, last_success, last_failure, total_runs, consecutive_failures, success_rate) but no script reads it; consumers (heartbeat, skill-health, self-improve) hand-rolled jq queries. scripts/skill-runs queries GitHub Actions API for ground truth; this script surfaces local-side scheduler view useful when API is rate-limited or sandbox-blocked. Composes into health checks via --unhealthy exit code; triage workflow now has both views (`cron-state --skill X` local + `skill-runs --skill X` API). Pure local file I/O — no curl, no gh api, no env-var-in-headers. CLAUDE.md updated to document under Tools section. No conflict with PRs #25 (skill-runs --skill/--duration) or #26 (self-improve PR-filter) (aeon-agent PR #27) |
| mastodon-column (minitor) | 2026-05-03 | Third feature shipped to aaronjmars/minitor — keyless Mastodon column reading public Mastodon REST API; two modes (hashtag via /api/v1/timelines/tag/{tag} / author via /api/v1/accounts/lookup + /statuses). 32nd column type, 6th-or-7th in Social cluster (depends on Bluesky PR #25 merge order). Closes May-2 repo-actions idea #4 — completes the decentralized social trifecta in minitor (Bluesky shipped May 2 + Farcaster already in main + Mastodon today). Mastodon's full-text status search /api/v2/search?type=statuses requires authentication on every public instance and is intentionally NOT used — keyless contract is the whole point; hashtag search is the keyless equivalent and covers the vast majority of monitoring use cases. Six Mastodon-specific quirks handled in the integration layer: HTML status content stripped to plain text via targeted regex (Mastodon returns sanitized fragments with p/br/a/span only — safe without a full parser; newlines from <br> and </p>); local-account acct normalization (bare 'gargron' rendered as 'gargron@mastodon.social' for unambiguous attribution); federated handle parsing (user, @user, user@server, @user@server — user@server form transparently routes lookup to that server, not configured instance — so a mastodon.social column can still follow @gargron@example.org without misconfiguring `instance`); reblog filter on author timelines (exclude_reblogs=true API + defensive `if (s.reblog) return null` guard); content-warning prefix on statuses with spoiler_text ([CW: …]); schema-drift safe (drops statuses missing id or url rather than rendering dead content). Standard 3-file plugin + 3 registry edits. Zod `{ instance: 'mastodon.social' default, mode: 'hashtag'|'author', query, handle }`, AtSign icon, Mastodon brand purple #6364ff (distinct from x-search blue #1d9bf0, farcaster purple #7c65c1, Bluesky blue #0085ff so social cluster stays visually differentiated). Renderer matches farcaster's avatar-led card layout with serif post text + engagement footer (replies/reblogs/favourites). No new env vars / build deps / schema migration — works the moment users add it (minitor PR #26) |

## Lessons Learned
- Digest format: Markdown with clickable links, under 4000 chars
- Always save files AND commit before logging
- Polymarket Gamma API: use volume_num_24hr sort for signal; newest markets are mostly noise (zero-volume crypto bets)
- GITHUB_TOKEN cannot push workflow file changes — needs `workflows` permission (PAT or fine-grained token)
- Self-improve outpaces review: agent opens PRs faster than human merges. PR awareness guard stops at 3+ open PRs to prevent pile-up and conflicts
- fetch-tweets dedup: now handled by persistent seen-file; notify has SHA256 message-hash layer; repo-pulse has per-run delta; scheduler has catch-up gate — three dedup layers stack end-to-end
- weekly-shiplog heartbeat escalation (6 days) was a false positive — skill works; Mon-only cron simply hadn't fired during heartbeat's observation window
- aeon-agent still at pre-autoresearch-evolution SKILL.md versions (aeon PRs #46–#136 not yet backported) — exit taxonomy (SKIP_UNCHANGED/NEW_INFO), significance gates, delta-vs-prior patterns not active on this running instance yet
- Paid-ads skill cluster (aixbt-pulse/schedule-ads/create-campaign, PR #138) is the first Aeon category to spend real money on external platforms — three stacked guardrails: PAUSED-by-default launches, daily spend cap circuit breaker, dry-run silent mode
- tweet-allocator Bankr prefetch: empty `.bankr-cache/verified-handles.json` (BANKR_API_KEY missing/invalid) causes error exits; subsequent runs self-recover once prefetch succeeds — notify dedup ensures only one error alert fires per day

## Repo Actions Ideas Pipeline
~60 ideas generated (13 runs). Recently built: integration-examples (Apr-20 idea #1), onboard (Apr-20 idea #2), fork-skill-digest (Apr-22 idea #2), public-status-page (Apr-22 idea #4), skill-analytics (Apr-22 idea #5 / Apr-24 idea #1), contributor-reward (Apr-24 idea #2), SHOWCASE.md docs feature (Apr-26 idea #2 Repo Discovery Refresh — content half; topics half deferred to maintainer-with-admin-scope), heartbeat token pulse (Apr-26 idea #3 / Apr-24 idea #5 — duplicate carry resolved), pr-triage (Apr-26 idea #5 / Apr-28 idea #2 External PR Triage — closed after 2-cycle carry), thread-formatter (Apr-28 idea #1 / Apr-26 idea #4 / Apr-24 idea #3 Twitter Thread Auto-Formatter — closed after 3-cycle carry), smithery-manifest (Apr-30 idea #2 / Apr-28 idea #4 Smithery Manifest Auto-Generator — also unblocks Apr-22 idea #1 Smithery + MCP Registry Submission, the longest-carried 6-week growth unbuilt), show-hn-draft (Apr-30 idea #1 / Apr-28 idea #3 Show HN Launch Prep — closed after 2-cycle carry), fork-cohort (Apr-30 idea #3 / Apr-28 idea #5 Fork Activation Cohort Tracker — closed after 2-cycle carry), operator-scorecard (Apr-30 idea #5 / May-2 idea #1 Operator Value Scorecard — closed after 2-cycle carry), mastodon-column (May-2 idea #4 Mastodon Column for minitor — closed same cycle, completes decentralized social trifecta with Bluesky + Farcaster). Apr-26 unbuilts: #1 Auto-Merge Agent PRs (needs workflows-scope PAT). Apr-30 unbuilts: #4 Skill Dependency Freshness Validator. May-2 unbuilts: #3 Star Momentum Alert, #5 v4 Readiness Checker. Still unbuilt: Dashboard Live Feed, Webhook-to-Skill Bridge, Skill Template Library. See `articles/repo-actions-*.md`.

## Next Priorities
- Enable pr-triage in aeon.yml — shipped `enabled: false` in PR #147 (Apr 29); PR #143 from pezetel is the first natural triage candidate
- Build Auto-Merge Agent PRs skill (Apr-26 idea #1) — closes last human bottleneck; needs workflows-scope PAT first
- Enable thread-formatter in aeon.yml — shipped `enabled: false` in PR #148 (Apr 30); first natural use is the 300-star milestone (~12 days at current ~4/day momentum)
- Enable smithery-manifest in aeon.yml — shipped `enabled: false` in PR #149 (May 1); maintainer reviews initial generation, optionally publishes aeon-mcp to npm, then submits docs/smithery.yaml to https://smithery.ai/server/new + opens MCP Registry PR with docs/smithery-manifest.json contents (closes Apr-22 idea #1 the longest-carried 6-week growth unbuilt)
- Enable show-hn-draft in aeon.yml — shipped `enabled: false` workflow_dispatch in PR #151 (May 1); operator dispatches manually when stars approach 300-milestone (currently 257⭐, ~43 from milestone) and pastes the resulting `articles/show-hn-draft-${today}.md` body at HN launch time
- Enable fork-cohort in aeon.yml — shipped `enabled: false` in PR #152 (May 2); first natural Sunday run lands May 3 if enabled, gives the "X of 39 forks running in production" social-proof number for the first time
- Enable operator-scorecard in aeon.yml — shipped `enabled: false` in PR #153 (May 3); first natural Monday run lands May 4 if enabled, weekly "was this week worth it?" synthesis (closes Apr-30 idea #5 / May-2 idea #1, carried 2 cycles)
- Enable mastodon column on minitor — shipped in PR #26 (May 3); zero infra cost, completes Bluesky + Farcaster + Mastodon decentralized social trifecta
- Build Skill Dependency Freshness Validator (Apr-30 idea #4) — maps consume: + ad-hoc reads, alerts on stale upstream files
- Build Operator Value Scorecard (Apr-30 idea #5) — weekly synthesis of skill-analytics + heartbeat + cost-report into a plain-language "what did Aeon do for you this week"
- Backport 80 autoresearch-evolution rewrites (aeon PRs #46–#136) to aeon-agent — pre-evolution SKILL.md versions still running here (day 12)
- Run more digest types (HN, RSS, papers, DeFi)
- Fix token permissions: need PAT with `workflows` scope to push workflow changes (also needed for repo topics admin-scope writes — see Apr-27 SHOWCASE PR #145)
