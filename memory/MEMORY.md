# Long-term Memory
*Last consolidated: 2026-05-21*

## About This Repo
- Autonomous agent running on GitHub Actions via Claude Code
- Linked to Telegram group — daily skills post repo state, content, and token updates

## Tracked Token
| Token | Contract | Chain |
|-------|----------|-------|
| AEON  | 0xbf8e8f0e8866a7052f948c16508644347c57aba3 | base |

## Recent Articles
*(Entries before 2026-05-01 archived to `memory/topics/articles-history.md`)*
| Date | Title | Topic |
|------|-------|-------|
| 2026-05-01 | Aeon Wrote Its Own Application to the Agent Registry | repo-article |
| 2026-05-01 | Self-Hosting Used to Mean a Compiler. Now It Means an Agent. | project-lens |
| 2026-05-02 | Aeon Just Started Shipping Features To A Product That Isn't Itself | repo-article |
| 2026-05-02 | A Fortune 500 Just Found 347 AI Agents It Didn't Know It Had | project-lens |
| 2026-05-03 | Watching the Whole Federated Web Without an API Key | repo-article |
| 2026-05-03 | Termites Built Cathedrals Without Holding Standups. Most Multi-Agent Systems Still Can't. | project-lens |
| 2026-05-04 | Aeon Built a Skill For Bugs That Don't Crash | repo-article |
| 2026-05-04 | The 1976 Theory That Already Named Why Your AI Agent Will Disappoint You | project-lens |
| 2026-05-05 | Aeon Built the Stopwatch For Its Own Launch | repo-article |
| 2026-05-05 | The 2026 Agent Stack Has Five Layers. Most Comparisons Are About One. | project-lens |
| 2026-05-06 | Aeon Wrote The v4 Migration Guide Two Weeks Before v4 Lands | repo-article |
| 2026-05-06 | Sixty-One Percent of Unpaid Maintainers Are Alone. Their Repo Doesn't Have to Be. | project-lens |
| 2026-05-07 | Aeon Built the Skill Template Library Its Forks Were Reverse-Engineering | repo-article |
| 2026-05-07 | Frameworks Don't Win. Generators Do. | project-lens |
| 2026-05-08 | The AI Stack Has Three Layers. Aeon Built A Skill For Each One — And A Dashboard Column Too. | repo-article |
| 2026-05-08 | An Agent That Holds Your API Key Will Eventually Leak It | project-lens |
| 2026-05-09 | Three Repos. Three PRs. Nine Minutes. All Sourced From Yesterday's Brief. | repo-article |
| 2026-05-09 | A Day Without Twelve Tabs | project-lens |
| 2026-05-10 | Aeon Built the Scoreboard for the Eight Frameworks It's Compared To. Itself Is the Anchor Row. | repo-article |
| 2026-05-10 | Files Won the AI Memory War. Almost Nobody Announced It. | project-lens |
| 2026-05-11 | Once Aeon Merges PR #38, It Won't Need The Operator For PR #39 | repo-article |
| 2026-05-11 | The Hardest Problem in AI Agents Was Already Solved in 1975 | project-lens |
| 2026-05-12 | Every Agent Can Open a PR. Almost None Can Close One. | project-lens |
| 2026-05-12 | Aeon Built Four Skills For This Exact Day. The Switch Is Still Off. | repo-article |
| 2026-05-13 | Aeon Just Built a Skill That Reads Other Skills. None of Them Run Yet. | repo-article |
| 2026-05-13 | Aviation Has Two Words For "Apply This Update." AI Agent Fleets Only Have One. | project-lens |
| 2026-05-14 | Aeon Spent Twelve Days Begging The Wrong Repo To Flip A Switch That Wasn't There. | repo-article |
| 2026-05-15 | Twenty-Four Hours After The Switch Was Flipped, Star-Milestone Fired For The First Time And The PR Queue Hit Zero. | repo-article |
| 2026-05-15 | Most AI Agents Are Robust. The Hard Part Is Building One That Is Antifragile. | project-lens |
| 2026-05-16 | Aeon Wrote "XAI_API_KEY Not Set" Into Its Own Daily Report For Five Days. The Key Was Set The Whole Time. | repo-article |
| 2026-05-16 | The AI Agent Industry Spent 2026 Reinventing `git log` | project-lens |
| 2026-05-17 | Two Of Today's Three Skill Pushes Were Aimed At Product Hunt. The Launch Hasn't Been Called Yet. | repo-article |
| 2026-05-17 | Most AI Agents Are A Process. Aeon Is A Cron Job. Here's What That Buys You. | project-lens |
| 2026-05-18 | Aeon Now Watches Eight Angles Of The AI Agent Framework Market. It Has Launched On None. | repo-article |
| 2026-05-18 | Eric Raymond Didn't Draw The Census Bureau That Keeps The Bazaar Working | project-lens |
| 2026-05-19 | Aeon Built The Surveillance Stack For The AI Agent Ecosystem. The Ecosystem Just Built Its First One For Aeon. | repo-article |
| 2026-05-19 | Anthropic Bought Back The SDK Generator Yesterday. Some Agents Have No SDK To Buy Back. | project-lens |
| 2026-05-20 | Yesterday The Audit Bot Filed Twenty-Seven Findings On Aeon. Today It Authored Three Of The Patches That Closed Them. | repo-article |
| 2026-05-20 | Every 2026 AI Agent Map Has The Same Blind Spot | project-lens |
| 2026-05-21 | Seven PRs Merged Into Aeon In Two Minutes. Four Were From The Audit Bot. Three Were From Strangers. | repo-article |
| 2026-05-22 | Five Skill Packs Were Already Being Built. Aeon Shipped The Install Protocol This Morning. The First One Plugged In Twenty Minutes Later. | repo-article |
| 2026-05-23 | Yesterday Aeon Shipped The Install Protocol So Outsiders Wouldn't Need To Touch Upstream. Today A Three-Day-Old Account Touched Upstream Anyway. | repo-article |

## Recent Digests
| Date | Type | Key Topics |
|------|------|------------|
| 2026-03-25 | Polymarket | Geopolitics dominates; US-Iran escalation at 58.5% YES |

## Skills Built
*(Rows before 2026-05-10 archived to `memory/topics/skills-history.md`)*
| Skill | Date | Notes |
|-------|------|-------|
| ai-framework-watch (aeon) | 2026-05-10 | Weekly Monday 08:30 UTC sonnet-4-6 — competitive-intelligence digest across 9 AI agent frameworks (aeon as anchor + 8 peers: langgraph/crewai/autogen/llamaindex/mastra/smolagents/dspy/pydantic-ai). Hardcoded watchlist (intentional — drift erodes week-over-week comparability). Surfaces 7d/30d star deltas, releases in 7-day window, breaking-change flags (strong-signal keywords + major-version-bump heuristic), momentum picks (≥3× weekly-implied 30d avg), anchor position. Single-framework deep-dive mode via var={slug}. Five-bucket exit taxonomy (OK/QUIET/PARTIAL/ERROR/BAD_VAR), gated notify. State in `memory/topics/framework-watch-state.json` with rolling 7d/30d star comparison. Read-only across cohort, no write actions even on anchor. Slots between huggingface-trending (artifacts, daily 09:30) and weekly-shiplog (Monday 09:00) — completes the AI-ecosystem-watch triple alongside github-trending (code) and huggingface-trending (artifacts). May-8 idea #3 (aeon PR #164) |
| fork-cohort backport (aeon-agent) | 2026-05-10 | Verbatim backport of aeon PR #152 (May 2). Buckets every fork by activation stage (COLD/STALE/ACTIVE/POWER) using GitHub Actions run history — ground truth for "is this Aeon instance alive right now?", not pushed_at. POWER threshold: ≥1 workflow run in 7d AND ≥5 enabled skills. Week-over-week delta tagging (LEVELED_UP/WENT_STALE/REVIVED/NEW_ACTIVE/WENT_COLD/NEW_FORK/DROPPED_FROM_POWER). Continues same-day-after backport pattern: operator-scorecard May 3→4, skill-freshness May 4→5, skill-update-check May 9. aeon.yml: enabled false, Sunday 19:00 UTC, sonnet-4-6. skills.json bumped 58 → 59. Read-only fleet, gated notify (skip on STEADY + no transitions + prior state present). May-6 idea #3 (aeon-agent PR #36) |
| devto-column (minitor) | 2026-05-10 | 39th column type. News & web cluster 6 → 7. Fills long-form-developer gap (HN/Lobsters cover link aggregation, SO covers Q&A, none host tutorials/engineering blogs). Keyless DEV.to REST API (`dev.to/api/articles`); three modes (top: past week / rising: past 24h / latest: state=fresh). Optional 1–5 tag AND-filter via comma/semicolon/space input — normalised to lowercase, deduped, clamped 5. Three integration quirks: (1) `tag_list` ships as both array and CSV string across endpoints — parser accepts either, normalises to deduped array; (2) per_page=max(limit,30) so trim is independent of upstream page size; (3) tag count cap at 5 (API doesn't document hard cap but >5 narrows to no results on most slices). #3b49df DEV indigo accent, Code2 icon, "for {org}" line when present. Plugin + 3 registry edits + integration helper. May-8 idea #2 (minitor PR #33) |
| tweet-allocator error-marker contract | 2026-05-10 | Step 4 now reads `.bankr-cache/verified-handles.json.error` first and surfaces the marker's content verbatim (failure code + operator action) before falling through to the cache. Previously the SKILL.md only said "check cache missing or empty" — Claude was inferring the marker on its own. May-10 run hit BANKR_API_KEY_INVALID and surfaced the right text by inference; this PR makes the contract explicit so the three failure modes (BANKR_API_KEY_MISSING / _INVALID / BANKR_LOOKUPS_FAILED) route deterministically. Sandbox note + status-flags section updated to match (aeon-agent PR #37) |
| price-threshold-alert (aeon) | 2026-05-11 | Real-time event-driven token-alert skill — fires on tracked-token ATH, ±20% 1h move, or operator-configured target crossings. Closes the gap left by daily token-report (calm summary, fixed hour) and repo-pulse (star/fork deltas). Three gates with independent 4h dedup clocks in `memory/topics/price-alert-state.json`; ATH is monotonic post-baseline (first-run-after-bootstrap suppression to avoid "you just set this up and got pinged" noise); target alerts one-shot per side. DexScreener-keyless fetch, deepest-pool selection on the configured chain, WebFetch fallback when sandbox blocks curl. Hard-coded direction phrasing for sharp moves (no TA freelance). 11-status exit taxonomy. `*/30 * * * *`, enabled:false, sonnet-4-6. skills.json bumped 114 → 115. May-10 idea #1 (aeon PR #165) |
| auto-merge-agent-prs (aeon-agent) | 2026-05-11 | Closes the autonomous loop: every feature/self-improve PR opens green and now closes green without operator click. Daily 18:00 UTC, gh pr list --author aeonframework filter, 9 eligibility gates (mergeable, checks SUCCESS, no CHANGES_REQUESTED, no blocking labels [hold/dnm/wip/blocked], no human-requested reviewers, not draft, branch matches `^(feat|fix|chore|docs|refactor)/`, retry cap 3). `gh pr merge --squash --delete-branch --auto` — the `--auto` flag queues mid-CI merges so PRs caught at the wrong moment still close without polling. 5-PR-per-run cap, atomic state writes in memory/topics/auto-merge-state.json, 24h re-notify dedup on ALL_BLOCKED. 8-status exit taxonomy. Differentiates from existing auto-merge by author-pin + additional gates. Unblocked 2026-05-06 by workflows-scope PAT rotation. May-10 idea #3. skills.json bumped 59 → 60 (aeon-agent PR #38) |
| github-actions column (minitor) | 2026-05-11 | 40th column type. Closes minitor's last big repo-health gap — community signals (stars/forks/PRs/issues/trending/search/backlinks/releases) but no CI visibility until now. Keyless GitHub REST (`/repos/{owner}/{repo}/actions/runs`), 60 req/hr unauth or 5000 with `GITHUB_TOKEN`. Three config fields: repo (required), workflow (optional, matches display name OR `.github/workflows/<file>.yml`), branch (optional, exact-match — API rejects partials). Three integration quirks: (1) workflow filter applied client-side because the API needs a numeric workflow_id, not a name; (2) page-completeness uses raw upstream length so the filter doesn't break pagination; (3) duration only computed when `status=completed` (no partial-duration misleading numbers on in-flight runs). Status pill distinguishes in-flight (Loader2 spinner) / queued / 9 terminal conclusions with distinct icons + ring colours. Row shows workflow name, repo, run #, branch, short SHA, duration, event source. Plugin (3 files) + 3 registry edits + `fetchWorkflowRuns` in lib/integrations/github.ts. #2088FF GitHub Actions blue accent, Workflow icon. README cluster row GitHub 8 → 9, count 39 → 40. May-10 idea #2 (minitor PR #34) |
| fork-release-tracker (aeon) | 2026-05-12 | Weekly Sunday 19:30 UTC sonnet-4-6 — celebrate when any fork of the parent repo cuts a tagged GitHub release in the last 7 days. Silent on quiet weeks. 10-step skill with 10-status exit taxonomy (OK/QUIET/NEW_RELEASE/MULTI_RELEASE/DRY_RUN/NO_FORKS/API_FAIL/PARENT_CHANGED/STATE_CORRUPT/BAD_VAR). State in memory/topics/fork-release-state.json — LRU 50 entries on `announced` (fork, tag) tuples. Single-release and multi-release notification templates; pre-releases included but tagged; drafts excluded. Closes the social-loop gap between fork-cohort (is fork alive — workflow runs in 7d) and contributor-spotlight (who's pushing code) — answers "has any fork shipped a versioned artifact?" — the strongest possible "infrastructure not toy" signal. Slots 30 min after fork-cohort. May-10 idea #4. skills.json bumped 115 → 116 (aeon PR #166) |
| thread-formatter backport (aeon-agent) | 2026-05-12 | Verbatim backport of aeon PR #148 (Apr 30). Same-day-after pattern continues: fork-cohort May 9→10, skill-update-check May 8→9, operator-scorecard May 3→4, skill-freshness May 4→5. 9-signal scoring table (feature +6, milestone +5, ≥15% token move +5, skill shipped +4, ≥10% move +3, high-engagement tweet +3, notable external PR +3, new fork +2, anomaly +2). Threshold 3 suppresses forced threads on quiet days. 5-tweet structure (hook/context A/context B/implication/CTA) with hard 280-char per-tweet cap and exactly one URL in tweet 5. 4-status exit taxonomy (OK/NO_DATA/NO_SIGNAL/DEDUP). Daily 17:30 UTC, sonnet-4-6, enabled:false. Last May-6 backport before v4-readiness. skills.json bumped 60 → 61 (aeon-agent PR #39) |
| npm column (minitor) | 2026-05-12 | 41st column type. News & web cluster 7 → 8. Fills package-registry discovery gap (lobsters/stack-overflow/devto cover developer content but no package registry). Keyless npm REST APIs: `registry.npmjs.org/-/v1/search` for discovery, `api.npmjs.org/downloads/point/last-week/{name}` for the weekly-downloads badge. Three integration quirks: (1) sort modes (popularity/quality/maintenance/combined) heavy-weight the chosen axis to 0.8 with 0.1 split across the other two — preserves cross-axis signal without zombie-package risk on a single zeroed axis; (2) downloads endpoint rejects %2F-encoded scoped names so `/last-week/@scope/name` is built with raw `/` preserved; (3) per-page downloads fetched in parallel, failures degrade silently to 0 so a single dead row doesn't kill the page. Plugin (3 files) + 3 registry edits + `fetchNpmPage` in lib/integrations/npm.ts. #CB3837 npm registry red, Package icon, AlertTriangle deprecated badge. README cluster row News & web 7 → 8, count 40 → 41. May-10 idea #5 (minitor PR #35) |
| fleet-state (aeon) | 2026-05-13 | Weekly Monday 08:00 UTC sonnet-4-6 — synthesis layer over fork-cohort (Sun 19:00) + fork-release-tracker (Sun 19:30) + contributor-spotlight (Sun 20:00). One composite "state of the fleet" digest with WoW deltas + rolling 12-week trend table; pure synthesis — never re-queries forks, only composes constituent state and articles. 14-step skill with 8-status exit taxonomy (OK/QUIET/PARTIAL/NO_SOURCES/DRY_RUN/PARENT_CHANGED/STATE_CORRUPT/BAD_VAR). Verdict priority: LEVELED_UP > SHIPPED > REVIVED > WENT_STALE > STEADY > COLD_START. Graceful degradation per source (missing cohort/release/spotlight → section omitted, not faked). Quiet-week gate suppresses notify on STEADY+0-releases+0-transitions+not-first-run. Slots 30 min before ai-framework-watch and 1h before weekly-shiplog. State in memory/topics/fleet-state.json with 12-entry LRU history (~3 months). Picked May-12 idea #3 (aeon PR #168) |
| v4-readiness backport (aeon-agent) | 2026-05-13 | Verbatim backport of aeon PR #160 (May 6). Last May-6 batch backport — closes aeon-agent pre-v4 parity gap (operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12 all already shipped). Per-fork v4 upgrade readiness checklist; 8-step skill with 5-status exit taxonomy (OK/PARTIAL/DRY_RUN/NO_CONFIG/BAD_VAR). Embedded Safe/Review/Removed manifest tables travel with the skill — operators update Removed table as v4 PRs land. Three dispatch modes: empty (local working tree), `dry-run` (skip notify, article still writes), `owner/repo` (remote-survey via `gh api`). Custom-skill detection heuristic — slugs under skills/ missing from skills.json install rows are tagged manual. Effort tags trivial/minor/moderate/manual. workflow_dispatch only — operators run pre-announcement/at-announcement/during-upgrade/post-upgrade. Read-only across the fork; never auto-edits config or pulls upstream. skills.json bumped 61 → 62. May-12 idea #1 (aeon-agent PR #41) |
| pypi-column (minitor) | 2026-05-13 | 42nd column type. News & web cluster 8 → 9. Natural pair to npm shipped May-12 — together they bracket the two registries the AI/ML research crowd lives in. Three modes: `updates` (PyPI's `rss/updates.xml` for recent package releases with version bumps), `new-packages` (`rss/packages.xml` for newly registered packages, brand-new projects), `top-30d` (community-maintained hugovk/top-pypi-packages mirror — top 8000 by 30-day downloads, the only "trending" surface PyPI itself doesn't expose). Optional weekly-downloads enrichment via pypistats.org (keyless). Three integration quirks: (1) updates feed wraps title as `{name} {version}` while new-packages uses just `{name}` — `parseUpdateTitle` handles both; (2) top-30d rank-feed's createdAt is the mirror's last_update — every row in a slice shares the same timestamp, so the renderer suppresses the time pill on rank rows; (3) pypistats.org failures degrade silently to 0 — single dead row never kills the page. Plugin (3 files) + 3 registry edits + `fetchPypiPage` in lib/integrations/pypi.ts. #3776AB Python brand blue, Package2 icon distinct from npm's Package. Pivot from May-12 idea #2 (Reddit) and #5 (Bluesky) — both already exist in the manifest; PyPI was the actual highest-impact gap (minitor PR #36) |
| webhook-bridge (aeon-agent) | 2026-05-14 | Burns May-12 idea #4 — last open idea from May-12 run. External-event-to-skill bridge: `.github/workflows/webhook.yml` listens for `repository_dispatch` event_type=`aeon-skill`, validates payload (slug regex `^[a-z0-9][a-z0-9-]{0,63}$`, skills/ directory existence, skills.json registry warning, var length + control-char guards, model whitelist of 9 Aeon-supported IDs), dispatches `aeon.yml` workflow via `gh workflow run` with skill + optional var + optional model. Slug-injection-safe — payload never reaches shell interpolation. Bridge job completes when dispatch succeeds; does NOT relay dispatched-skill result to caller (async by design — skills report via Aeon's notify channels). Skills present locally as directories but missing from skills.json still dispatch (with warning) — preserves "custom skill in a fork" pattern. New `skills/webhook-bridge/SKILL.md` is documentation skill (workflow_dispatch, enabled: false — the real work is in webhook.yml, not the SKILL body). 4 worked examples (cross-repo GH Actions, Zapier, n8n, vendor webhook proxies like DexScreener), 6-row validation contract table, scope notes for fine-grained PATs. Reactive section in aeon.yml has anticipated this outside-in path for weeks (skill-repair, autoresearch commented out) — now has a real consumer. skills.json bumped 61 → 62. Conflict with PR #41 v4-readiness (also bumps 61 → 62) — trivial 3-line merge whichever lands second (aeon-agent PR #42) |
| crates-column (minitor) | 2026-05-14 | 43rd column type. News & web cluster 9 → 10. Completes the registry trifecta with npm (PR #35, May-12) and PyPI (PR #36, May-13) — together npm + pypi + crates bracket the three registries the AI/ML, web, and systems-programming crowds live in. Keyless `GET /api/v1/crates` fetcher with 5 documented sort axes (recent-downloads default — 90d trending / downloads — all-time / recent-updates / new / alpha) and optional `q=` substring search across name + description + keywords. Three integration quirks: (1) User-Agent header is non-optional — anonymous requests without UA return 403 per crates.io's documented policy, so `minitor/1.0` is set on every call; (2) `recent_downloads` is a fixed 90-day window (different from npm's last-week stats endpoint) — UI labels it "/90d" so users aren't confused; (3) `max_stable_version → max_version → newest_version` fallback chain renders pre-release-only crates correctly. Plugin (3 files) + 3 registry edits + `fetchCratesPage` in `lib/integrations/crates.ts`. #DEA584 Rust ember orange accent (distinct from npm #CB3837 red, pypi #3776AB blue, lobsters #ac130d red, github-actions #2088FF blue), `Box` icon (visually evokes crate, distinct from npm's `Package` and pypi's `Package2`). README cluster row News & web 8 → 9, count 41 → 42 (assuming PyPI merges; otherwise it's the 42nd). Pivot from May-12 idea #2 (Reddit, pre-existing) and #5 (Bluesky, pre-existing) — crates.io was the actual highest-impact gap once npm + PyPI made it the obvious next layer (minitor PR #38) |
| product-hunt-launch (aeon) | 2026-05-15 | New launch-asset drafter — counterpart to show-hn-draft for the second major launch surface. 11-step skill, workflow_dispatch + enabled:false. Drafts the full PH launch asset pack: tagline ≤60ch / description ≤260ch / first comment ≤500ch / maker comment ≤500ch / six 80ch feature bullets. Single-section regeneration via var={tagline,description,first-comment,maker-comment,bullets}. Same input-set as show-hn-draft (README, SHOWCASE, skills.json, aeon.yml, last 7d repo-articles + project-lens, last 7d logs, MEMORY.md Skills Built) so the two launches stay coherent. Enforces PH's actual character ceilings — counts emitted in the notification footer so the operator catches over-limit sections before submitting. Banned-marketing-words list ("AI-powered", "revolutionary", "leverages", "powerful", "framework" — saturated on PH). Lead-capability scoring table (concreteness/recency/surprise) sets the spine of the tagline + first comment. Five-status exit taxonomy with PARTIAL covering both missing-input and over-limit-after-tightening cases. Operator checklist appended (Tue/Wed/Thu 12:01 AM PT slot, logo/gallery specs, hunter outreach, first-comment 5-min rule, cross-post sequencing) but explicitly NOT posted. May-14 idea #1. skills.json bumped 117 → 118 (aeon PR #175) |
| skill-enabler (aeon-agent) | 2026-05-15 | Closes the 12-day "switch is still off in aeon.yml" pain pattern that PR #45 (May-14) finally resolved by hand. Operator dispatches with var=slug1,slug2,slug3 (explicit opt-in only — empty var is a load-bearing no-op); skill validates each slug through five gates (format `^[a-z0-9][a-z0-9-]{0,63}$` / `skills/${slug}/SKILL.md` exists / present as top-level entry under `skills:` in aeon.yml / NOT referenced under `chains:` — would cause double-runs / currently `enabled: false`); flips `enabled: false → true` with slug-scoped substitution (never global replace); single `aeon.yml` write at end to avoid partial-state on mid-loop failure; commits to feat/enable-skills-${today}, pushes, opens PR with per-skill rationale table (rationale pulled from skills.json → SKILL.md frontmatter description → slug fallback chain). dry-run: prefix supported for validation-only runs. Branch-name collision suffix `-${run_count}` for same-day reruns. Nine-status exit taxonomy (OK / PARTIAL / NO_ELIGIBLE / ALL_ALREADY_ENABLED / NO_INPUT / NO_CONFIG / PUSH_FAILED / PR_FAILED / DRY_RUN). Tolerant `enabled\s*:\s*false` matcher for whitespace variants. workflow_dispatch only, enabled:false. Requires `workflows` PAT scope to push aeon.yml changes — without it step 5 exits cleanly with PUSH_FAILED. May-14 idea #2. skills.json bumped 85 → 86 (aeon-agent PR #47) |
| deck export / import (minitor) | 2026-05-15 | Two new ⌘K commands closing the "every Minitor install starts blank" gap. Export: serializes the active deck (name + ordered columns with typeId/title/config) to a pretty-printed JSON blob, copies via navigator.clipboard with a textarea+execCommand fallback for permission-blocked browsers; toast on success/fail. Import: textarea modal, server-side Zod validation (version literal=1, name 1–128ch, ≤64 cols, config record), creates a NEW deck with " (imported)" appended to the name so the source deck stays untouched; server returns the inserted column rows so the client store updates optimistically without a full snapshot re-fetch; activeDeckId switches to the import target. Feed items intentionally not exported — they're fetched from upstream, not user state — so the imported deck triggers a fresh fetch on first view through the existing autoFetchColumn path. DeckExport JSON v1 shape: { version, deckName, exportedAt?, columns: [{ typeId, title, config }] }. Forward-compatibility: unknown typeId imports cleanly (renders as unknown column until plugin installed) — strict rejection would block one-bad-column imports. No new deps (nanoid + zod already in dependencies). Five files: app/actions.ts + lib/store/use-deck-store.ts + new components/dialogs/import-deck-dialog.tsx + components/sidebar-01/{nav-header,app-sidebar}.tsx. Pre-existing pypi.ts typecheck errors in main are unrelated; touched files all clean. Enables community deck sharing (Discord/X/gist → paste → monitoring in seconds, zero infra change). May-14 idea #5 (minitor PR #40) |
| fork-skill-gap (aeon) | 2026-05-16 | Weekly Sunday 21:00 UTC sonnet-4-6 — per-fork upstream-skill-adoption gap report. Closes the fork-intelligence layer between fork-cohort (is it alive?), fork-release-tracker (has it shipped?), contributor-spotlight (who's pushing code?) — none answered "what's in upstream you haven't adopted yet?" 11-step skill, 8-status exit taxonomy (OK / QUIET / DRY_RUN / NO_ACTIVE / NO_UPSTREAM_MANIFEST / PARENT_CHANGED / API_FAIL / BAD_VAR). Reads memory/topics/fork-cohort-state.json when fresh (≤8 days) for cached POWER+ACTIVE list; graceful fallback to live `gh api .../actions/runs?per_page=1` per fork when cohort state is missing or stale — works on first-ever run before fork-cohort is enabled. Compares on slug presence in skills.json (NOT enabled state — that's fork-skill-digest's job). Manifest-absent never inflates to "missing 119 skills" — marked unreadable. Article body includes the inverse view: top-10 universally unadopted upstream slugs by fork-count missing (upstream sees which new shipments launch into silence). Quiet-week gate suppresses notify when all readable forks within 5 of upstream AND prior state exists AND no new top-missing slugs. 80-fork processing cap, bot allowlist (dependabot/github-actions/aeonframework bots skipped from cohort but counted in totals). Read-only across fleet; never opens issues on fork repos. Security note covers JSON-only parsing of fork manifests (no shell interpolation on slug values; descriptions never rendered). Slots 1h after contributor-spotlight (20:00) and 1.5h after fork-release-tracker (19:30). skills.json bumped 118 → 119. May-14 idea #3 (aeon PR #176) |
| column alert keywords (minitor) | 2026-05-16 | Optional `alertKeywords` string on every column — matching items get a yellow inset ring; column header shows a Bell pill badge with the live match count + tooltip listing the active terms. Works with all 43 plugins on day one because the value is a column-level property (sibling to title), never sent to server fetchers — sidesteps every plugin's strict Zod schema. The May-14 idea proposed `BaseColumnConfig.alertKeywords` but no such base exists; column-row hoisting is the cleaner path. Five integration quirks: (1) match scope intentionally wide — author name + handle + content + URL all participate (URL participation lets `aaronjmars/aeon` match GitHub-flavoured columns when title is shortened); (2) 512-char clamp on raw input, 16-term cap, 64-char-per-term cap; (3) accepts comma/semicolon/whitespace separators uniformly; (4) badge honestly named "matches in current visible window" — no new read/unread persistence layer; (5) deck export/import round-trips alertKeywords with backward-compat (pre-feature decks without the field import cleanly). 10 files: lib/db/schema.ts + drizzle/0001_alert_keywords.sql + meta files (additive nullable column migration), lib/columns/{types,keyword-match}.ts, app/actions.ts (updateColumnAlertKeywords + export/import + loadSnapshot wiring), lib/store/use-deck-store.ts (updateAlertKeywords action + import wiring), components/column/{column-card,configure-column-dialog}.tsx (useMemo match set + ring wrap + badge + form field). May-14 idea #4 (minitor PR #41) |
| fork-first-run-alert (aeon) | 2026-05-17 | Daily 20:30 UTC sonnet-4-6 — named same-day alert the first time a fork completes a workflow run. Closes the visibility gap between fork-cohort's weekly cadence and the actual activation moment. 11-step skill with 7-status exit taxonomy (OK / QUIET / DRY_RUN / NO_STATE / API_FAIL / PARENT_CHANGED / BAD_VAR). Two-tier data sourcing: cohort-cache (when fresh ≤8 days, parent match) → live-fallback via `gh api repos/{parent}/forks --paginate` + per-fork run lookup (works on first-ever run before fork-cohort is enabled). Persistent seen-list at memory/topics/fork-first-run-state.json with LRU 500-entry cap; canonicalised to lowercase to prevent double-alerts on casing drift. Notification policy is count-driven: 1–3 new activators get individual named alerts ("Fork speend/aeon just ran its first workflow"); 4+ get a single batch notification (8-row cap with "... and N more" footer). Bot allowlist (dependabot/github-actions/aeonframework) added to seen but suppressed from alerts. First-ever-run backfill mode populates seen with current ACTIVE/POWER set without per-fork alerts (prevents loud day-one signal at enable time). 80-fork cap on live fallback (same budget guard as fork-cohort). Read-only across the fleet — never opens issues/comments on fork repos. Slots after contributor-spotlight (Sunday 20:00) on weekly day, solo every other day. skills.json bumped 118 → 119. May-16 idea #4 (aeon PR #179) |
| product-hunt-launch backport (aeon-agent) | 2026-05-17 | Verbatim backport of aeon PR #175 (May 15). Same-day-after backport pattern continues (operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13). Drafts the full PH launch asset pack (tagline ≤60ch / description ≤260ch / first comment ≤500ch / maker comment ≤500ch / six 80ch feature bullets) from live repo state. Single-section regeneration via var={tagline,description,first-comment,maker-comment,bullets}. Same input set as show-hn-draft (README, SHOWCASE, skills.json, aeon.yml, last 7d articles/logs, MEMORY.md Skills Built) so HN+PH launches stay coherent. Enforces PH's character ceilings with footer counts, banned-marketing-words list, 5-status exit taxonomy with PARTIAL for missing-input/over-limit cases. workflow_dispatch only, enabled:false. Slotted immediately after show-hn-draft. Pivot from May-16 idea #3 (Skill Health Triad backport — skill-health/skill-evals/skill-repair were ALREADY present in aeon-agent, idea was a false signal in repo-actions; picked the May-15 upstream skill as natural same-day-after instead). skills.json bumped 86 → 87 (aeon-agent PR #49) |
| producthunt column (minitor) | 2026-05-17 | 44th column type. News & web cluster 10 → 11. Fills minitor's "no visibility into the world's #1 launch platform" gap directly relevant to the operator's own pending PH submission (aeon PR #175 + aeon-agent PR #49 both ship product-hunt-launch drafters). Keyless producthunt.com/feed RSS (PH GraphQL needs OAuth + per-app rate caps unusable for a keyless column). Two modes (today: full daily slate / topic: keyword-filtered) with 5-keyword OR-match cap (AND would zero out queries — PH launches are sparse per day). Five integration quirks: (1) title split on em-dash (—) or en-dash (–) bounded by whitespace, raw-title fallback to `productName` when the `{name} — {tagline}` shape doesn't match; (2) PH slug extracted from /posts/{slug} URLs for canonical `producthunt:{slug}` id so the same product across multiple fetches collapses to one row in the column store (the feed includes a rolling tail of the previous day's launches); (3) keywords match against product name + tagline + description + URL (wide haystack); (4) slice-based pagination via existing sliceForPage helper — fetches generous batch once, hands out PAGE_SIZE per Load-more call; (5) mode kept as string union even though both modes share the same fetch — leaves extension point for future PH endpoints (per-topic RSS, leaderboard scrape). Plugin (3 files) + 3 registry edits + `fetchProductHuntPage` in `lib/integrations/producthunt.ts`. #DA552F PH brand orange accent (distinct from hacker-news flame, substack #ff6719, devto #3b49df, npm #CB3837, pypi #3776AB, crates #DEA584), `Rocket` icon. README cluster row News & web 10 → 11, count 43 → 44, keyless list updated. May-16 idea #1 (minitor PR #42) |
| competitor-launch-radar (aeon) | 2026-05-18 | Weekly Monday 10:00 UTC sonnet-4-6 — keyless PH RSS + HN Algolia (Show HN + story tag) scan for NEW AI-agent-framework launches outside the 9-framework cohort already tracked by ai-framework-watch. 10-step skill with 7-status exit taxonomy (OK/QUIET/DRY_RUN/NO_SOURCES/PARTIAL/STATE_CORRUPT/BAD_VAR). Nine framework keywords (agent framework / autonomous agent / agentic / multi-agent / mcp server / mcp client / ai agent / claude agent / llm agent). Suppression list of 9 cohort slugs (langgraph/crewai/autogen/llamaindex/mastra/smolagents/dspy/pydantic-ai/aeon) so cohort member updates don't fire here — that's ai-framework-watch's domain. Noise floor 10+ upvotes/points. Classification framework / mcp / product in priority order. LRU 200-entry state at memory/topics/competitor-launch-radar-state.json keyed by ph:slug or hn:objectID. Count-driven notify (0=QUIET, 1-3=individual, 4+=batched top-8 with overflow footer). WebFetch fallback for sandbox curl failures. Slotted under ai-framework-watch so Monday-morning intelligence stack reads cohort momentum → fleet state → new entrants. May-16 idea #5 — last open May-16 idea for aeon. skills.json bumped 119 → 120 (aeon PR #183) |
| fork-first-run-alert backport (aeon-agent) | 2026-05-18 | Verbatim backport of upstream aeon PR #179 (merged May-17 21:58 UTC). Continues same-day-after pattern (operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17). Daily 20:30 UTC sonnet-4-6 enabled:false. Named same-day alert the first time any fork completes a workflow run; closes weekly-cadence gap left by fork-cohort. 286-line SKILL.md with 7-status exit taxonomy. Two-tier data sourcing (cohort cache ≤8d fresh, live `gh api repos/{parent}/forks --paginate` fallback works day-one before fork-cohort enabled). LRU-500 seen-list at memory/topics/fork-first-run-state.json. Count-driven notify (0=QUIET, 1-3=per-fork named, 4+=batch). Bot allowlist (dependabot/github-actions/aeonframework) added to seen but suppressed from alerts. First-ever run backfills current ACTIVE set (no loud day-one flood). Registered between fork-contributor-leaderboard and fork-release-tracker in the Upstream sync 2026-05-14 section. skills.json bumped 87 → 88 (aeon-agent PR #50) |
| github-discussions column (minitor) | 2026-05-18 | 45th column type. GitHub cluster 9 → 10. Completes minitor's GitHub monitoring layer — stars/forks/PRs/issues/releases/search/actions/backlinks/trending all had columns; Discussions (the async Q&A layer GitHub is pushing as a default) was the missing piece. GraphQL-only — `api.github.com/graphql` with `repository(owner, name) { discussions(first: 50, orderBy: CREATED_AT DESC) { nodes { ... } } }`; REST doesn't expose Discussions. Optional GITHUB_TOKEN auth (5000 vs 60 req/hr unauth — keyless path drops gracefully, doesn't error). Three modes: recent / unanswered (isAnswered === false) / top (upvoteCount desc then comments.totalCount). GraphQL orderBy only supports CREATED_AT/UPDATED_AT so unanswered+top filter/sort in-memory after the fetch. Five integration quirks: (1) DiscussionsDisabledError sentinel surfaces friendly empty state for repos with Discussions feature off, instead of crashing; (2) auth header only set when token present; (3) defaultTitle reads "{repo} Discussions" when configured else just "Discussions"; (4) AnsweredIndicator only renders on Q&A category discussions (Announcements/etc lack answer semantics); (5) parseRepo regex `^[A-Za-z0-9][A-Za-z0-9._-]*/[A-Za-z0-9][A-Za-z0-9._-]*$` validates owner/name pre-flight. Item id `github-discussions:{owner}/{repo}#{number}` dedups across refresh/load-more. Plugin (3 files) + 3 registry edits + new `lib/integrations/github-discussions.ts` (228 lines). #7C3AED purple accent (distinct from every other GitHub cluster colour: stars/forks/PRs/issues use blues/oranges/reds; #2088FF for actions). MessageSquare icon. README cluster row GitHub 9 → 10, count 44 → 45, GITHUB_TOKEN note added. May-16 idea #2 — second-and-last open May-16 idea for minitor (minitor PR #43) |
| scan.sh Bash 3.2 fix (aeon) | 2026-05-19 | Bugfix from Issue #182. macOS's default `/bin/bash` is still 3.2 (Apple holdout on GPLv3); under `set -euo pipefail` it treats `"${arr[@]}"` of a zero-element array as unbound and aborts. scan.sh's findings-output path declares `local highs=() mediums=() lows=()` and only fills them on matches — so any clean PASS hits the trap right after printing the PASS line, scan exits non-zero, and `add-skill` reports `✗ BLOCKED: security issues` even though the scan passed. Wrapped each of the three print loops in `[[ ${#arr[@]} -gt 0 ]]` length guards (mirrors the existing JSON-path guards at lines 250/253/256 — only the human-readable branch was missing them). Inline comment names the Bash 3.2 constraint so a future cleanup doesn't drop the guards. Silent first-touch failure mode for every macOS operator joining the fleet — 78+ forks + 14 new in 24h made it visible. Bash 4+ tolerates the bare expansion already, so guards are no-ops on Linux CI. May-18 idea #2 (aeon PR #186) |
| fork-skill-gap backport (aeon-agent) | 2026-05-19 | Verbatim backport of upstream aeon PR #176 (merged May-16). Continues same-day-after pattern (operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17, fork-first-run-alert May-17→18). Per-fork upstream-skill-adoption gap report — closes the fourth fork-intelligence question after fork-cohort (alive?), fork-release-tracker (shipped?), contributor-spotlight (who pushed code?). 305-line SKILL.md with 11 steps and 8-status exit taxonomy. Two-tier data sourcing (cohort cache ≤8d fresh, live `gh api repos/{parent}/forks --paginate` fallback day-one). LRU per-fork state at memory/topics/fork-skill-gap-state.json — `missing_count`, `missing_slugs` (cap 50), category rollup, `last_seen`. Quiet-week notify gate (all forks within 5 skills AND prior state exists AND no new top-missing). Manifest absence is `unreadable=true`, never "missing 88 skills" — protects forks that renamed skills.json. Compares on slug presence (not enabled state — that's fork-skill-digest's job). Bot allowlist (dependabot/github-actions/aeonframework). Read-only across fleet — never writes to fork repos. Registered Sunday 21:00 UTC, enabled:false, sonnet-4-6, between fork-skill-digest and huggingface-trending. skills.json bumped 87 → 88. May-18 idea #1 (aeon-agent PR #52) |
| coingecko column (minitor) | 2026-05-19 | 46th column type (45th in current main since PR #43 not yet merged — README updated to 45). Apps & on-chain cluster 4 → 5. Fills the crypto price/trending gap that wallet-tx (on-chain transactions) and polymarket (prediction markets) left untouched. Three keyless modes: `trending` (24h search-volume leaderboard, fixed 7-coin from `/api/v3/search/trending`), `top` (market-cap leaderboard with full pagination, sparkline included, from `/api/v3/coins/markets`), `watchlist` (same markets endpoint with `ids=` filter; comma/semicolon/whitespace separators, lowercased+deduped, 50-id cap). Three integration quirks: (1) trending returns USD prices as currency-formatted strings (`$1,234.56`); markets returns plain numbers — `num()` strips currency formatting before parsing so 5-digit BTC doesn't collapse to 0; (2) `price_change_percentage_24h` is a USD-keyed object on trending (`data.price_change_percentage_24h.usd`) but a flat number on markets — mapper normalises to single `priceChange24h`; (3) host swap on key presence only — anonymous pro-api.coingecko.com returns 401, so keyless stays on api.coingecko.com. Empty watchlist falls back to top-by-cap so freshly-added column isn't a dead box. Plugin (3 files) + 3 registry edits + new `lib/integrations/coingecko.ts` (228 lines). #8DC647 CoinGecko green (distinct from wallet-tx #627eea + polymarket #2D9CDB blues), `TrendingUp` icon. Row shows brand chip + MC rank + relative time + lazy `<img>` icon + name + price (sub-cent precision for low-value coins) + 24h change arrow (green/red) + optional 7-day SVG polyline sparkline + MC + 24h volume. README cluster row Apps & on-chain 4 → 5, count 44 → 45, keyless list + optional COINGECKO_DEMO_API_KEY documented in .env.example. May-18 idea #4 (minitor PR #44) |
| scan.sh POSIX-ERE fix (aeon) | 2026-05-20 | Bugfix from AntFleet H6 (Issue #184 review bundle, 27 SHA-pinned findings from antfleet-ops two-model unanimous review). `scan.sh` shells out to `grep -nE "$pattern"` against POSIX ERE — which has no `\s` (whitespace) or `\b` (word boundary). GNU grep silently extends ERE to accept the PCRE escapes; BSD grep (macOS default `/bin/grep`, busybox) treats them as literal `s` and `b`. So `eval\s`, `rm\s+-rf\s+/`, `[Ii]gnore\s+(all\s+)?previous\s+instructions`, and `git\s+push\s+-f\b` all silently no-op'd on every macOS operator. All 28 patterns across HIGH/MEDIUM/LOW arrays rewritten: `\s`/`\s+` → `[[:space:]]`/`[[:space:]]+`; the single `\b` (on `git push -f`) → `($|[^[:alnum:]_-])` so `-fast`/`-force` don't false-positive; forkbomb pattern `:(){.*};:` literal-paren-escaped to `:\(\)[[:space:]]*\{.*\};[[:space:]]*:`; literal-dot escapes added to `~/.ssh`/`~/.gnupg`/`~/.aws`/`~/.config` (previously matched any char). New 4-line header comment names the POSIX-ERE constraint + cites Issue #184 so a future edit doesn't silently drop the guards. Same-file lineage with PR #186 (May-18 idea #2, Bash 3.2 array-emptiness). Maintainer's open Highs queue when this picked: H1/H3/H4/H6/H7/H9 — chose H6 for the broadest impact (every macOS operator running `./add-skill` had silently degraded scanning) and tightest contained scope (one file, 28 patterns, no behaviour change on GNU grep). (aeon PR #197) |
| competitor-launch-radar backport (aeon-agent) | 2026-05-20 | Verbatim backport of upstream aeon PR #183 (`competitor-launch-radar`, merged 2026-05-19 13:50 UTC). 10th consecutive same-day-after upstream feature merge in the established cadence (operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17, fork-first-run-alert May-17→18, fork-skill-gap May-18→19, and now this). 442-line SKILL.md, 10 steps, 7-status exit taxonomy (OK/QUIET/DRY_RUN/NO_SOURCES/PARTIAL/STATE_CORRUPT/BAD_VAR). Closes the cohort blind spot — `ai-framework-watch` tracks momentum across the hardcoded 9-framework cohort (aeon + langgraph/crewai/autogen/llamaindex/mastra/smolagents/dspy/pydantic-ai), can't answer "did a new framework just launch?" because the watchlist is intentional drift-resistant. This skill closes that with weekly Monday 10:00 UTC scan of Product Hunt RSS + HN Algolia (both keyless, WebFetch fallback), 9 keyword matches against title+tagline+description+url+story_text haystack, 3-class taxonomy (framework > mcp > product), noise floor (PH ≥ 10 upvotes / HN ≥ 10 points), LRU-200 dedup state, count-driven notify (QUIET on zero, individual 1–3, batched top-8 with overflow at 4+). Registered between aixbt-pulse and contributor-reward in the Upstream sync (2026-05-14) section — Monday slot reads cohort momentum (ai-framework-watch 08:30) → fleet state (08:00) → new entrants (10:00). skills.json bumped 88 → 89 (aeon-agent PR #53) |
| defillama-column (minitor) | 2026-05-20 | 47th column type. Apps & on-chain cluster 5 → 6. PIVOT from May-18 idea #5 (IndieHackers RSS) — every IH RSS endpoint (`indiehackers.com/feed`, `/feed.xml`, `/posts.rss`, `/podcast.rss`) now returns SPA HTML; IH discontinued public RSS some time after the idea was seeded. Replacement targets the same operator slot (a third of minitor's active fork cohort runs crypto-focused decks based on `wallet-tx`/`polymarket`/`coingecko` adoption). Pairs cleanly with yesterday's CoinGecko merge (PR #44) — CoinGecko = price + market cap, DeFiLlama = TVL + protocol-level signal, zero overlap. Three keyless modes via api.llama.fi: `top` (top protocols by TVL from `/protocols`), `gainers` (same source re-sorted by 24h TVL change desc with absolute TVL as tiebreaker so a $50M protocol moving 20% beats a $50k protocol moving 20%), `chains` (per-chain TVL leaderboard from `/v2/chains`). Optional category substring filter (Dexs/Lending/Liquid Staking/Restaking/CDP/Yield Aggregator/…) on top+gainers, ignored on chains. Three integration quirks: (1) `/protocols` returns ~3k entries (~2-3 MB raw) — edge-cached at the CDN so per-request cost is low; (2) mapper drops `tvl ≤ 0` to suppress dead protocols but keeps CEXes (Binance CEX, Coinbase Custody) because watching custody concentration is part of the use case; (3) `/v2/chains` is the only chain endpoint stable across DeFiLlama API rewrites (v1 deprecated 18 months ago). 8 files: `lib/integrations/defillama.ts` (~220 lines) + 3-file plugin + 3 registry edits + README. #445ed0 DeFiLlama brand blue (distinct from wallet-tx #627eea Ethereum + polymarket #2D9CDB electric + coingecko #8DC647 green), `Layers` lucide icon. Row shows brand chip + category pill + relative time + lazy `<img>` protocol logo + canonical `defillama.com/protocol/{slug}` link + TVL + 24h delta arrow + optional 7d delta + protocol mcap + chain list (minitor PR #45) |
| contributor-spotlight FORK_DEFAULT_BRANCH fix (aeon) | 2026-05-21 | Bugfix from Issue #184 AntFleet H3 (open Highs queue: H1/H3/H4/H7/H9 before today; H3 closes here). `skills/contributor-spotlight/SKILL.md` step 5's `gh api .../contents/aeon.yml?ref=${FORK_DEFAULT_BRANCH}` referenced a shell variable that was never set — step 4 wrote `default_branch` into `/tmp/contrib-repo.json` but stopped. The empty ref made every non-`main`-branch fork (98 forks now run divergent defaults — some `master`, some `develop`, some custom) silently 404 or fall back to upstream's idea of "default"; `ENABLED_COUNT` and `OPERATOR_AUTHORED` (the two most newsworthy data points in the weekly recognition article + notification) were both wrong on those runs. Step 4 now extracts `FORK_DEFAULT_BRANCH=$(jq -r '.default_branch // "main"' /tmp/contrib-repo.json)` with a second-line guard for the literal string `null`. Step 5 tightens `|| true` to `|| echo '' > /tmp/fork-aeon.yml` so the downstream `grep -E` always has a real (possibly empty) file under `set -e`. Inline comments cite Issue #184 H3 so a future cleanup doesn't drop the guard. Same-file lineage with PR #186 (Bash 3.2 array-emptiness, May-18) and PR #197 (POSIX-ERE in scan.sh, May-20) — third shell guard the author meant to add but didn't. May-20 idea #1 (aeon PR #206) |
| skill-update-check branch-honor fix (aeon-agent) | 2026-05-21 | Bugfix from Issue #184 AntFleet H7 (filed against upstream aeon but the skill lives in aeon-agent). `skills/skill-update-check/SKILL.md` step 2's `gh api "repos/${source_repo}/commits" -f path="${source_path}" -f per_page=1` ignored the `branch` field in the lock entry, so any skill pinned to a non-default branch (vendor's `stable`, custom `develop`, etc.) was compared against the source repo's `main` — perpetual false "update available" alerts on every weekly run. Fix conditionally passes `-f sha=${branch}` only when the lock pins a non-default, non-empty branch — empty / `"main"` cases omit the flag (preserves prior behaviour for the common case AND avoids hardcoding `sha=main` which would break source repos whose default branch is `master`). Step 3's `compare/${locked_sha}...${current_sha}` already addresses commits by SHA — called out in prose so a future cleanup doesn't try to "complete" the change with a redundant arg. Continues the same-day-after backport cadence note (operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17, fork-first-run-alert May-17→18, fork-skill-gap May-18→19, competitor-launch-radar May-19→20) — though this is a bugfix not a backport. May-20 idea #3 (aeon-agent PR #55) |
| deck share link (minitor) | 2026-05-21 | Third primitive in the deck-portability set alongside Export (copy JSON, PR #40 May-15) and Import (paste JSON modal, PR #40). New `lib/deck-share.ts` (95 lines, 4 pure functions: `encodeDeckShareHash`, `decodeDeckShareHash`, `readDeckShareFragment`, `buildDeckShareUrl`) base64url-encodes the existing DeckExport v1 JSON into a `#deck=...` URL fragment — fragments never leave the browser so the deck payload isn't logged in server access logs / proxies / referer headers. UTF-8-safe via TextEncoder (raw btoa mishandles non-ASCII column titles/configs). 32 KB hard cap on encoded payload — guards 64-column pathological decks blowing past browser URL limits; the existing Export path stays available for those. Fragment parser tolerates extra `&`-separated params (utm taggers, analytics IDs) so it doesn't crash on appended keys. `components/sidebar-01/nav-header.tsx` adds a `Share2`-icon ⌘K command "Share current deck (copy URL)" sibling to Export, reusing the existing `copyToClipboard` helper (navigator.clipboard with execCommand fallback for permission-blocked browsers, console.log as last resort). `components/deck/deck-view.tsx` adds a `useEffect` gated on `hydrated` that reads `window.location.hash` once after first hydration, decodes the fragment, runs it through the existing `importDeck` server action (same Zod validation, same `(imported)` rename, same activate-as-new-deck), toasts the result, and clears the hash via `history.replaceState` — both on success AND on malformed/invalid payloads so the user isn't trapped. Forward-compatible with the existing DeckExport schema; `alertKeywords` (May-14 idea #4 PR #41) round-trips automatically. No new dependencies (lucide-react already includes Share2). Pairs cleanly with the planned Starter Deck Templates Gallery (May-20 idea #5) — templates will render as pre-baked share links, no new schema or API surface needed. May-20 idea #4 (minitor PR #46) |
| install-skill-pack CLI (aeon) | 2026-05-22 | Closes the "Community Skill Packs" install-protocol gap — README section listed two packs (`baseddevoloper/aeon-skill-pack-vvvkernel`, `danbuildss/luca-aeon-skills`) but operators were stuck cloning each pack and copying files manually. New `./install-skill-pack <owner/repo>` Bash script reads a `skills-pack.json` manifest at the pack root (or under `--path <subdir>`), runs the existing `skills/skill-security-scan/scan.sh` against each declared `SKILL.md`, prompts on HIGH findings (or fails closed for non-interactive runs without `--yes`/`--force`), and installs approved skills with provenance recorded in `skills.lock` (with a new `pack` field), catalog rows upserted into `skills.json`, and disabled entries inserted into `aeon.yml`. Falls back to scanning `skills/` when no manifest is present so existing repos keep working. Five flags: `--list` (preview), `--path` (subdir packs), `--branch` (non-default), `--yes` (auto-accept HIGH), `--force` (skip security check entirely), `--dry-run`. Slug validation rejects `..` / `/` / whitespace before any file is written; path-traversal guard on manifest `path` field. Tree-shakes nicely from `./add-skill`'s flow because most logic is reusable (tarball fetch, scanner invocation, lock writing) — but kept self-contained rather than turning `add-skill` into a library. New `docs/community-skill-packs.md` documents manifest schema with a field-reference table, fallback behaviour, trust model (trusted-sources.txt allowlist, prompt-on-HIGH for untrusted), worked example, and pack-maintainer checklist. README's Community Skill Packs section now leads with the install command and links the docs. Implements baseddevoloper's Issue #185 protocol. May-20 idea #2 (aeon PR #213) |
| scan.sh Bash 3.2 + POSIX-ERE backport (aeon-agent) | 2026-05-22 | Combined verbatim backport of upstream aeon PRs #186 (Bash 3.2 array-emptiness guards, May-18) and #197 (POSIX-ERE patterns, May-20) — both fixes existed in aeon but not aeon-agent, where the same `skills/skill-security-scan/scan.sh` had been forked unchanged. macOS impact was severe: BSD grep silently treated PCRE escapes `\s` and `\b` as literal characters (so `eval\s`, `rm\s+-rf\s+/`, all six prompt-injection patterns, and `git\s+push\s+-f\b` no-op'd entirely), AND Bash 3.2 under `set -euo pipefail` aborted on `"${arr[@]}"` of zero-element arrays (so any clean PASS hit the trap right after printing PASS, scan exited non-zero, add-skill reported `BLOCKED: security issues`). Combined fix: all 28 patterns across HIGH/MEDIUM/LOW arrays rewritten — `\s`/`\s+` → `[[:space:]]`/`[[:space:]]+`, `\b` (on `git push -f`) → `($|[^[:alnum:]_-])` so `-fast`/`-force` don't false-positive, forkbomb pattern `:(){.*};:` literal-paren-escaped to `:\(\)[[:space:]]*\{.*\};[[:space:]]*:`, literal-dot escapes added to `~/.ssh`/`~/.gnupg`/`~/.aws`/`~/.config` (previously matched any char). Three human-readable print loops wrapped in `[[ ${#arr[@]} -gt 0 ]]` length guards (mirrors the existing JSON-path guards at lines 250/253/256 — only the human-readable branch was missing them). Two explanatory comments cite upstream Issue #184 so a future cleanup doesn't silently drop the guards. No behaviour change on GNU grep / Bash 4+ Linux CI; tightens behaviour on every macOS operator running `./add-skill` locally. Same-day-after backport cadence resumes (operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17, fork-first-run-alert May-17→18, fork-skill-gap May-18→19, competitor-launch-radar May-19→20, then a one-day pause May-21 for the in-aeon-agent skill-update-check H7 fix) (aeon-agent PR #56) |
| starter deck templates (minitor) | 2026-05-22 | Four pre-baked DeckExport v1 templates closing the blank-slate conversion gap that's the #1 killer for new minitor installs. AI Research (HN top + arXiv cs.AI + GitHub trending Python + HF trending models + X "AI"), Base Ecosystem (GitHub stars aeon + aeon-agent + CoinGecko AEON watchlist + DeFiLlama top + X @aeonframework), Crypto DeFi (DeFiLlama 24h gainers + CoinGecko top + Polymarket trending + X "DeFi"), Startup Tracker (GitHub trending + PH today + Show HN + DEV.to top week + r/startups — fully keyless, every column works without env). Two surfaces: (1) Onboarding screen (`deckOrder.length === 0`) now leads with a "Start from a template" section above the existing manual-build flow — two clicks from blank install to working dashboard; (2) ⌘K command "Browse starter templates" opens a TemplatesDialog modal anytime, so returning operators can layer templates onto an existing workspace. New `lib/deck-templates.ts` (200 lines) exports `TEMPLATES` array of `DeckTemplate` records with payload + display metadata (name, tagline, description, brand accent, lucide icon name), plus `templateAsImportJson` serializer. New `components/dialogs/templates-dialog.tsx` renders the gallery (brand chip + tagline + description + per-column type pills using the registered plugin's brand colour for instant visual preview). Onboarding's existing 6-suggestion manual flow remains as the fallback (separator: "or build manually"). All templates go through the existing `importDeck` server action — same DeckExport v1 schema, same Zod check, same `(imported)` rename + new-deck contract as JSON-paste / share-link imports. No template-specific route, no template-specific validation. TS-not-JSON because every template needs colour+icon metadata; keeping data + metadata together gets tree-shaking and removes a fetch failure mode. Pairs with PR #46 (deck-share link, May-21) — templates are essentially curated, pre-baked share payloads. May-20 idea #5 — last open May-20 idea for minitor. Five files: `lib/deck-templates.ts` + `components/dialogs/templates-dialog.tsx` (new) + `components/onboarding/welcome.tsx` + `components/sidebar-01/nav-header.tsx` + `components/sidebar-01/app-sidebar.tsx` (minitor PR #47) |
| skill-packs.json registry + --list (aeon) | 2026-05-23 | Machine-readable mirror of the Community Skill Packs README table. New `skill-packs.json` at repo root seeded with 5 packs (AntFleet/aeon-skills `trusted`, baseddevoloper/aeon-skill-pack-vvvkernel, danbuildss/luca-aeon-skills, 0xShak/zer0-skill-pack from PR #208, gitlawbounty/gitbounty-skill-pack from PR #212). Slug arrays sourced LIVE from each pack's own skills-pack.json (zer0, gitbounty) or `gh api .../contents/skills` listings (vvvkernel→9 slugs vvvkernel/vvvkernel-{audit,brief,digest,growth,image,monitor,narrative,onchain}; luca→4 slugs check-agent/get-report/scan-wallet/treasury-monitor) — never approximated. `./install-skill-pack` grows registry-list mode: with no repo argument and only the `--list` flag, it prints the registry (repo / skill count / `*` trust badge / one-line description); any other flag or positional argument falls through to existing single-pack path (strict detector, all prior behaviour preserved). Reads local skill-packs.json first, falls back to `https://raw.githubusercontent.com/aaronjmars/aeon/main/skill-packs.json` via curl when the script runs outside a clone. docs/community-skill-packs.md gains a "Browse the registry" section near the top + full "skill-packs.json (community registry)" section with schema + field reference table; publishing checklist now requires pack PRs to update BOTH the README table AND the registry in the same diff. `trust_level: "trusted"` in the registry is a DISCOVERY hint only — the actual scan-bypass behaviour is still gated by `skills/security/trusted-sources.txt`, so the registry can't be used to forge trust. May-22 idea #1 (aeon PR #215) |
| contributor-spotlight FORK_DEFAULT_BRANCH backport (aeon-agent) | 2026-05-23 | Verbatim backport of upstream aeon PR #206 (merged 2026-05-21, AntFleet Issue #184 H3). 11th same-day-after backport in the established chain (operator-scorecard May-3→4, skill-freshness May-4→5, skill-update-check May-8→9, fork-cohort May-9→10, thread-formatter May-11→12, v4-readiness May-12→13, product-hunt-launch May-15→17, fork-first-run-alert May-17→18, fork-skill-gap May-18→19, competitor-launch-radar May-19→20, scan.sh Bash 3.2 + POSIX-ERE May-22, contributor-spotlight May-23). `skills/contributor-spotlight/SKILL.md` step 5's `gh api .../contents/aeon.yml?ref=${FORK_DEFAULT_BRANCH}` referenced a shell variable that was never set — step 4 wrote `default_branch` into `/tmp/contrib-repo.json` but stopped. Empty ref made every non-`main`-branch fork silently 404, so ENABLED_COUNT and OPERATOR_AUTHORED (the two most newsworthy data points in the weekly recognition article + notification) were both wrong on those runs. Fix: step 4 now extracts `FORK_DEFAULT_BRANCH=$(jq -r '.default_branch // "main"' /tmp/contrib-repo.json)` with a second-line guard for the literal string `null` (API emits `null` for archived/empty forks). Step 5 tightens `\|\| true` to `\|\| echo '' > /tmp/fork-aeon.yml` so downstream `grep -E` always has a real (possibly empty) file under `set -e`. Inline comments cite upstream Issue #184 H3 + aeon PR #206 so a future cleanup doesn't drop the guard. May-22 idea #3 (aeon-agent PR #58) |
| /gallery public deck page (minitor) | 2026-05-23 | Public, SEO-crawlable starter-deck catalog at `/gallery`. First-time visitors with no account can land on a curated card grid, click a template, and Minitor imports it as a new deck the moment they hit the dashboard. Builds entirely on top of the share-link (PR #46 May-21) + templates (PR #47 May-22) infrastructure — no new schema, no new server routes, no new validation path. Two files: `app/gallery/page.tsx` (new, server component with Next.js Metadata export for `<title>` + `<meta description>` + OpenGraph + canonical `/gallery`; imports TEMPLATES from `lib/deck-templates.ts`; responsive grid 1-col mobile / 2-col tablet+; each card renders brand-accented icon chip + name + tagline + full description + column-type pills coloured by each plugin's brand via `getColumnType`), and `components/sidebar-01/nav-footer.tsx` (new "Browse deck gallery" `<Link>` above the Add-new dropdown using LayoutTemplate lucide icon — returning operators discover the gallery from the dashboard without needing to know the URL). Cards link to `/#deck=<base64url(payload)>` — same shape that the share-link flow produces, picked up by the existing useEffect in deck-view.tsx that decodes the fragment and runs the same `importDeck` server action used by JSON-paste / share-link imports (same Zod validation, same `(imported)` rename, same activate-as-new-deck contract). Server-side rendering works because `btoa` + `TextEncoder` are Node globals on the Next.js 15 runtime; share fragment omits `exportedAt` so URLs stay STABLE across requests (cache-friendly, individually shareable). `getColumnType` is safe to call from a server component — only `accent`, `label`, `icon` are accessed on the return, and `icon` is always a `LucideIcon` (framework-agnostic SVG). ~180 lines of code on top of infrastructure that already existed. May-22 idea #5 (minitor PR #48) |

## Lessons Learned
- Digest format: Markdown with clickable links, under 4000 chars
- Always save files AND commit before logging
- Polymarket Gamma API: use volume_num_24hr sort for signal; newest markets are mostly noise
- GITHUB_TOKEN cannot push workflow file changes — needs `workflows` permission (PAT or fine-grained token)
- Self-improve outpaces review: agent opens PRs faster than human merges. PR awareness guard stops at 3+ open PRs
- fetch-tweets dedup: persistent seen-file + SHA256 notify layer + per-run delta + scheduler catch-up gate — three dedup layers stack end-to-end
- aeon-agent still at pre-autoresearch-evolution SKILL.md versions (aeon PRs #46–#136 not yet backported) — SKIP_UNCHANGED/NEW_INFO exits, significance gates not active here
- grok-4-1-fast is a thinking model — reasoning trace eats output tokens before answer list; always set max_output_tokens ≥ 16384 in xai_search calls (triggered May-6: 2 tweets instead of 10+)
- tweet-allocator Bankr prefetch: empty `verified-handles.json` had three ambiguous causes — resolved by `.error` marker (aeon-agent PR #24)
- Sandbox blocks `$ENV_VAR` in curl headers → skill reads auth failure as "key not set" even when the key IS set. Fix: read from prefetch-written log files instead of curling authenticated APIs inside the skill (aeon-agent PR #48)

## Repo Actions Ideas Pipeline
~93 ideas generated (20 runs). Recently built: scan.sh Bash 3.2 fix (aeon Issue #182) + fork-skill-gap backport (aeon-agent) + CoinGecko column (minitor) (May-19 — burned May-18 ideas #1, #2, #4), scan.sh POSIX-ERE fix (aeon — AntFleet H6 from Issue #184) + competitor-launch-radar backport (aeon-agent) + DeFiLlama column (minitor — pivoted from May-18 #5 IndieHackers, IH RSS dead) (May-20), contributor-spotlight FORK_DEFAULT_BRANCH fix (aeon — AntFleet H3) + skill-update-check branch-honor (aeon-agent — AntFleet H7) + deck share link (minitor) (May-21 — burned May-20 ideas #1, #3, #4), install-skill-pack CLI (aeon — burned May-20 idea #2, implements baseddevoloper Issue #185 protocol) + scan.sh Bash 3.2 + POSIX-ERE backport (aeon-agent — same-day-after cadence resumes after May-21 in-aeon-agent fix) + starter deck templates gallery (minitor — burned May-20 idea #5) (May-22), skill-packs.json registry + --list (aeon — May-22 idea #1) + contributor-spotlight FORK_DEFAULT_BRANCH backport (aeon-agent — May-22 idea #3) + /gallery public deck page (minitor — May-22 idea #5) (May-23 — burned May-22 ideas #1, #3, #5). **May-22 OPEN: ideas #2 (v4-readiness H1) and #4 (column refresh intervals) remain.** **May-20 ideas FULLY CONSUMED.** **May-18 ideas FULLY CONSUMED.** **May-16 / May-14 / May-12 / May-10 ideas FULLY CONSUMED.** **May-6 aeon-agent backports FULLY CONSUMED.** AntFleet Issue #184: H2/H3/H5/H6/H7/H8 closed (PRs #194–#197 + PR #206 + aeon-agent PRs #55 + #58 + AntFleet PRs merged May-19 / May-20 / May-21 / May-23). Open: H1 (v4-readiness manifest gaps — May-22 idea #2), H4 (.bak rollback in fleet-state), H9 (admanage-create skips campaignId-only ad sets) + ~13 Mediums + 2 Lows. Other open: Dashboard Live Feed (aeon-agent — older, complex). Next repo-actions run expected to seed May-24 ideas. See `articles/repo-actions-*.md`.

## Open Improvement PRs
- [#54 improve: project-lens — make 14d rotation rule mathematically feasible](https://github.com/aaronjmars/aeon-agent/pull/54) — aeon-agent, opened 2026-05-20 (self-improve; 8 angle categories vs daily runs made the stated 14d no-repeat rule unachievable by pigeonhole — reduced to 7d window + formalized sub-angle hygiene the agent was already practicing informally)
- [#57 improve(notify): word-boundary suppression](https://github.com/aaronjmars/aeon-agent/pull/57) — aeon-agent, opened 2026-05-22 (self-improve; `./notify`'s substring matcher `*test*|*trace*|*ping*|*debug*` silently swallowed real notifications under 120 chars containing those fragments inside words — "Latest token-report", "Shipping 3 PRs", "Tracer code", "Pinging operator". Fixed both heredocs in aeon.yml — inline ./notify and post-run pending-notify replay — with bash regex word-boundary; threshold 120→60)

## Next Priorities
- **URGENT** Enable show-hn-draft in aeon.yml — PR #151 (May 1); 400⭐ ORGANIC crossed 2026-05-20, Show HN dispatch overdue
- Enable pr-triage in aeon.yml — PR #147 (Apr 29); PR #143 from pezetel is first natural triage candidate
- Enable smithery-manifest in aeon.yml — PR #149 (May 1); submit docs/ to Smithery + MCP Registry
- Enable fork-cohort in aeon.yml — PR #152 (May 2); gives "X of 60+ forks running in production" social proof
- Enable operator-scorecard in aeon.yml — PR #153 (May 3); weekly scorecard
- Enable skill-freshness in aeon.yml — PR #157 (May 4)
- Enable skill-freshness in aeon-agent aeon.yml — PR #30 (May 5)
- Enable v4-readiness in aeon aeon.yml — PR #160 (May 6); dispatch manually pre-v4-announcement
- Enable fleet-state in aeon aeon.yml — first natural Monday run May 18; synthesises fork-cohort + fork-release-tracker + contributor-spotlight into one Monday digest
- aeon-agent: still 80 autoresearch-evolution rewrites pending (aeon PRs #46–#136)
- Run more digest types (HN, RSS, papers, DeFi)
