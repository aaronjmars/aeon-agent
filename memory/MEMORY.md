# Long-term Memory
*Last consolidated: 2026-06-07*

## About This Repo
- Autonomous agent running on GitHub Actions via Claude Code
- Linked to Telegram group — daily skills post repo state, content, and token updates

## Tracked Token
| Token | Contract | Chain |
|-------|----------|-------|
| AEON  | 0xbf8e8f0e8866a7052f948c16508644347c57aba3 | base |

## Recent Articles
*(Entries before 2026-05-14 archived to `memory/topics/articles-history.md`)*
| Date | Title | Topic |
|------|-------|-------|
| 2026-05-14 | Aeon Spent Twelve Days Begging The Wrong Repo To Flip A Switch That Wasn't There. | repo-article |
| 2026-05-15 | Twenty-Four Hours After The Switch Was Flipped, Star-Milestone Fired For The First Time And The PR Queue Hit Zero. | repo-article |
| 2026-05-15 | Most AI Agents Are Robust. The Hard Part Is Building One That Is Antifragile. | project-lens |
| 2026-05-16 | Aeon Wrote "XAI_API_KEY Not Set" Into Its Own Daily Report For Five Days. The Key Was Set The Whole Time. | repo-article |
| 2026-05-16 | The AI Agent Industry Spent 2026 Reinventing `git log` | project-lens |
| 2026-05-17 | Two Of Today's Three Skill Pushes Were Aimed At Product Hunt. The Launch Hasn't Been Called Yet. | repo-article |
| 2026-05-17 | Most AI Agents Are A Process. Aeon Is A Cron Job. Here's What That Buys You. | project-lens |
| 2026-05-18 | Aeon Now Watches Eight Angles Of The AI Agent Framework Market. It Has Launched On None. | repo-article |
| 2026-05-18 | Eric Raymond Didn't Draw The Census Bureau That Keeps The Bazaar Working | project-lens |
| 2026-05-19 | Aeon Built The Surveillance Stack For The AI Agent Ecosystem. The Ecosystem Just Built Its First One For Aeon. | repo-article |
| 2026-05-19 | Anthropic Bought Back The SDK Generator Yesterday. Some Agents Have No SDK To Buy Back. | project-lens |
| 2026-05-20 | Yesterday The Audit Bot Filed Twenty-Seven Findings On Aeon. Today It Authored Three Of The Patches That Closed Them. | repo-article |
| 2026-05-20 | Every 2026 AI Agent Map Has The Same Blind Spot | project-lens |
| 2026-05-21 | Seven PRs Merged Into Aeon In Two Minutes. Four Were From The Audit Bot. Three Were From Strangers. | repo-article |
| 2026-05-22 | Five Skill Packs Were Already Being Built. Aeon Shipped The Install Protocol This Morning. The First One Plugged In Twenty Minutes Later. | repo-article |
| 2026-05-22 | Malcolm McLean Wasn't In Shipping. He Was In Trucking. The Container Won Anyway. | project-lens |
| 2026-05-23 | Yesterday Aeon Shipped The Install Protocol So Outsiders Wouldn't Need To Touch Upstream. Today A Three-Day-Old Account Touched Upstream Anyway. | repo-article |
| 2026-05-23 | Every Plugin Registry Eventually Becomes A Trust List. Homebrew Got There In 2010. | project-lens |
| 2026-05-24 | The Bot Watched Itself Fail At Dawn. By Lunchtime It Had Opened The PR To Fix Itself. | repo-article |
| 2026-05-24 | AI Agents Don't Fail Loudly. They Pass When They Shouldn't. | project-lens |
| 2026-05-25 | Every Skill An Outsider Ever Sent Aeon Was A Leaf. This Afternoon One Asked To Replace The Root. | repo-article |
| 2026-05-25 | An AI Agent Can't Leak a Secret It Was Never Allowed to Hold | project-lens |
| 2026-05-26 | Aeon Built The Package Manager. A Stranger Built The Storefront On Top. Nobody Asked Them To. | repo-article |
| 2026-05-26 | A Cell Doesn't Try to Get Better. It Tries to Stay a Cell. | project-lens |
| 2026-05-27 | The Memory Skill That Tried To Live Inside Aeon Came Back Today As A Pointer To Somewhere Else | repo-article |
| 2026-05-27 | A Week-Old Account Could Publish To The Agent Skill Marketplace. 824 Of Them Did. | project-lens |
| 2026-05-28 | The Files A Stranger Wasn't Allowed To Land On Aeon's Main Branch Are Now On It. Aeon Landed Them. | repo-article |
| 2026-05-29 | The Maintainer Spent Friday Afternoon Pulling Skills Out Of His Own Private Fork. A Two-Day-Old Account Filled The Gaps He Wasn't Working On. | repo-article |
| 2026-05-29 | The Open Source Maintainer's Real Bottleneck Isn't Pull Requests. It's Acknowledgement. | project-lens |
| 2026-05-30 | An Agent Filed The Issue At 00:19 UTC. The Maintainer's Agent Filed The Fix At 11:18 UTC. The Maintainer Hadn't Pushed A Commit All Day. | repo-article |
| 2026-05-30 | On Saturdays, The Maintainer Opens Telegram Before The Repo. Most Saturdays, She Never Opens The Repo. | project-lens |
| 2026-05-31 | For Seventeen Days This Fork Shipped Yesterday's Upstream PR. Today It Shipped The Tool That Will Notice The Day It Doesn't. | repo-article |
| 2026-05-31 | In 1796, The Astronomer Royal Fired His Assistant For Being Half A Second Slow. | project-lens |
| 2026-06-01 | Eighteen PRs Merged In Thirty-Seven Minutes. The Maintainer Wrote One Of Them. | repo-article |
| 2026-06-01 | Three 2026 Frameworks Reinvented The Same Thing: Agent Memory You Can `git revert`. | project-lens |
| 2026-06-02 | The Skill Built To Find Six Gaps Was Going To Report Six. The Fix Landed Six Days Before The Cron. | repo-article |
| 2026-06-02 | In 2025 Shopify Saved 27,061 Engineering Hours Letting A Bot Press The Merge Button. The Newest Skill On Aeon Was Built To Refuse To Press It. | project-lens |
| 2026-06-03 | An Agent Named Atrium-Hermes Shipped The Third Way To Install A Skill. None Of The Three PRs Had A Human Author. | repo-article |
| 2026-06-03 | GitHub Made Copilot Better By Cutting Twenty-Seven Of Its Tools. Most AI Agent Roadmaps Are Built To Do The Opposite. | project-lens |
| 2026-06-04 | An External Bot Shipped Six Onchain Security Skills To Aeon On May 29. The First Cron That Calls Them Was Written Today, By The Framework Itself. | repo-article |
| 2026-06-05 | Vigil Was Submitted To Aeon As A Security Scanner. The Maintainer Caught A Shell-Injection In Vigil On Review Round Four. | repo-article |
| 2026-06-05 | On May 21 The MCP Team Removed Sessions From The Protocol. The Agents Already In Production Have Seven Weeks. | project-lens |
| 2026-06-06 | Aeon Has 193 Skills. Fifteen Of Them Are The Machine. Yesterday The Framework Labelled Them. | repo-article |
| 2026-06-06 | Most AI Agent Projects Stop When You Close The Laptop. The Ones That Don't Are A Different Market. | project-lens |
| 2026-06-07 | Three Days Ago A Stranger Shipped Aeon's Onchain Scanner. This Morning The Framework Wrote The Skill That Acts On What It Finds. | repo-article |
| 2026-06-08 | Aeon Is Six Stars From A Launch The Operator Drafted Thirty-Eight Days Ago And Never Enabled. This Morning The Framework Wired The Trigger That Will Fire It. | repo-article |
| 2026-06-09 | The Show HN Draft Was Written Thirty-Nine Days Ago For A 250-Star Project. This Morning It Was Re-Edited At 497 Stars, Forty-Eight Hours Before It Auto-Fires. | repo-article |

## Recent Digests
| Date | Type | Key Topics |
|------|------|------------|
| 2026-03-25 | Polymarket | Geopolitics dominates; US-Iran escalation at 58.5% YES |

## Skills Built
*(Rows before 2026-06-04 archived to `memory/topics/skills-history.md`)*
| Skill | Date | Notes |
|-------|------|-------|
| wallet-risk-weekly skill (aeon) | 2026-06-04 | Jun-02 idea #2. New `skills/wallet-risk-weekly/SKILL.md` — Monday 11:15 UTC weekly risk audit of every Base wallet in `.x402books/wallets.json`. **First scheduled consumer of the HoundFlow security pack** (`approval-audit` + `honeypot-check` have been `workflow_dispatch` only since 2026-05-28 with zero standing runner). Per wallet: scans last ~24k blocks for `Approval` events (chunked ~1800 newest-first under public-RPC result cap), confirms each grant live via `allowance` `eth_call`, flags `>= 2^255` as UNLIMITED. Per **unique** token with a live approval (deduped across spenders): honeypot simulation — sample recent non-zero `Transfer` recipient, `balanceOf`, simulate `transfer(this, balance/2)` via `eth_call` with `from = sampled holder`. Buckets HIGH (UNLIMITED to non-known-safe OR token = LIKELY_HONEYPOT) / MEDIUM (UNLIMITED to Uniswap V2/V3/V4 Universal Router + Permit2 + Aerodrome / finite >$10k-equiv) / LOW / CLEAN. INCONCLUSIVE never escalates — false-flagging operator's own wallets erodes the alert signal everywhere else. Gated notify (HIGH only / new MEDIUM transition / first-run baseline with ≥1 live approval). Article written every run even when silent (weekly CLEAN record proves the surface was checked). State in `memory/topics/wallet-risk-state.json` (atomic .tmp + mv write — mid-write crash can't corrupt prior). 12-state exit taxonomy. `${var}` modes: empty=all base wallets, 40-hex=one wallet, role=`treasury`/`deployer`/`other`, `dry-run`=skip notify. Public Base RPC default (`BASE_RPC_URL` override). `eth_call` only — no funds at risk. WebFetch fallback for sandbox curl failures. Registered disabled at `15 11 * * 1` between ecosystem-pulse (11:00) and capabilities-map (11:30) — Monday morning intelligence stack. skills.json total 181→182 category crypto. (aeon PR #340) |
| repo-article $(date) self-fix (aeon-agent) | 2026-06-04 | Self-improve: `skills/repo-article/SKILL.md` step 1 — `since="$(date -u -d '7 days ago' ...)"` → literal `SINCE=YYYY-MM-DDT00:00:00Z` computed from `${today}` minus 7 days. Runner hook blocks `$(...)` ("Contains simple_expansion"); same constraint PR #63 fixed in weekly-shiplog (May 26), PR #67 fixed in push-recap (May 28), PR #71 fixed in heartbeat (May 30), PR #77 fixed in repo-pulse (Jun 02). repo-article is the **daily 16:00 UTC** repo-state article — the agent improvises this cutoff on every single run. Smallest-effort next item in the anti-pattern list the Jun-02 self-improve run (PR #77) explicitly left "for future runs." Semantic trade: cutoff goes from "exactly 7×24h ago" to "midnight UTC of 7 days ago" (7d–7d+16h on a 16:00 UTC run); the article is a 7-day retrospective so the small overlap at the trailing edge is absorbed by prose framing. Mirrors repo-pulse PR #77's accepted trade. Two skills with the same anti-pattern remain unfixed (left for future runs): `repo-actions:29` (14d window, even days 14:00 UTC — same shape as today's fix) and `star-momentum-alert:69` (3 expansion sites in one `for D in $(seq 13 -1 0); do DATE=$(date ...)` block — bigger fix). (aeon-agent PR #81) |
| narrative-convergence backport (aeon-agent) | 2026-06-04 | 21st consecutive same-day-after backport. Verbatim copy of upstream `skills/narrative-convergence/SKILL.md` from upstream aeon PR #272 (merged 2026-05-29) — same general-ops batch that brought spend-monitor (PR #74, Jun-01) and follow-up-patrol (PR #76, Jun-02). Daily 13:00 UTC cross-skill convergence detector: lists `.outputs/*.md` + last 2 days of memory logs, maps each output to a signal category via `memory/topics/signal-categories.md` (operator-editable seed), builds entity/theme → [{skill, category}] map, scores each on (# distinct skills × # distinct categories × operator-interest match from soul/SOUL.md when present), suppresses entries already covered by an article in the last 7 days, surfaces top 5 with one-line hooks each. Notifies only when ≥2 strong signals survive suppression — the high-confidence "this is breaking in 3+ independent lanes" write opportunity the daily content stack (repo-article, project-lens, thread-formatter) was previously inferring by hand. Three adaptations vs upstream: (1) `./notify` rewritten to `./notify "$(cat .pending-notify-temp/<file>)"` since aeon-agent's notify reads `$1` not `-f`, (2) signal-categories seed kept verbatim **plus** additive native skills (repo-pulse, repo-article, push-recap, project-lens, repo-actions, token-report, star-momentum-alert, star-milestone, thread-formatter) added to natural categories so first-run output is useful without manual edits, (3) `.outputs/` sparse on aeon-agent → memory-logs fallback path is the **primary** one until chains are configured, called out explicitly in SKILL.md so the operator's mental model matches what the skill actually does. Inline backport-note block at top cites PR #272 + each adaptation. Registered disabled at `0 13 * * *` (same slot as upstream), alphabetically inserted between monitor-kalshi and onboard in `# --- Upstream sync` section. skills.json total 98→99 category research (first non-`crypto`/non-`dev`/non-`productivity` addition in 4 backports). Cadence (since aeon May-3 anchor): operator-scorecard May-3→4 → ... → pr-merge-queue Jun-02→Jun-03 → **narrative-convergence Jun-02→Jun-04**. PR #272 backport queue: spend-monitor + follow-up-patrol + narrative-convergence done; mcp-pulse + fleet-scorecard remain — natural targets for upcoming rounds (fleet-scorecard depends on `scripts/prefetch-fleet-scorecard.sh` + `memory/instances.json` which aeon-agent doesn't currently maintain; mcp-pulse is larger and depends on web-fetched npm/GitHub signals). (aeon-agent PR #80) |
| per-column duplicate (minitor) | 2026-06-04 | 6th rung on the per-column UX axis after tab groups (May-29 PR #53) → collapse (May-30 PR #55) → JSON export (May-31 PR #56) → quick-search (Jun-02 PR #58) → pin-to-front (Jun-03 PR #59) → **duplicate (this PR)**. New "Duplicate" entry in every column's More-menu dropdown: clones the source column's persisted settings (typeId, config, alertKeywords, notifyWebhookUrl, refreshIntervalSeconds, filterKeywords, excludeKeywords, tabGroup) into a new column titled `"<source> (copy)"` inserted **immediately after the source** in the same deck (server-side `UPDATE columns SET position = position + 1 WHERE position > src.position` then INSERT at src.position+1 — appending at end would force the operator to drag-scroll a 15-column deck to find their copy, opposite of what duplicate is for). 3 files +160: `app/actions.ts` (+77 — new `duplicateColumn(sourceId, newId, newTitle): Promise<ImportedDeckColumn | null>` server action; captureDeckSnapshot before mutation → reversible from version history; title `.trim().slice(0, 256)` matches renameColumn cap; returns shape importDeck already emits); `lib/store/use-deck-store.ts` (+69 — new `duplicateColumn(columnId)` zustand action; reads source from state, scans `decks[*].columnIds` to locate owning deck; nanoid new id; optimistic insert right after source in `columnIds[]` so visual order matches server; fires server via `fireAndLog` pattern); `components/column/column-card.tsx` (+14 — `Copy` lucide icon imported alongside existing pin/download/settings glyphs; DropdownMenuItem inserted between Rename and Download; toast on success names the new column). **Three key design decisions**: (1) **inherits `notifyWebhookUrl`** because this is an in-install copy not an export — the secret stays inside the trust boundary it was already configured within (export still strips it; share-links still strip it); (2) **drops `pinned`** because pinning is the operator's explicit "primary column" decision, mirroring PR #59's "DnD across pin/unpin no-op — Pin checkbox is the explicit boundary affordance" rule; (3) title `"(copy)"` postfix not `"Copy of X"` prefix — same as finders + most cloud doc apps, postfix reads better mid-deck because the column header shows the original title first and the disambiguator second. NO DB schema change, NO migration, NO version bump on `DECK_EXPORT_VERSION`. Round-trips through export/import/share-link/snapshot identically to any other column. Could NOT run Next 16 build/typecheck (offline sandbox — same constraint as PRs #49/#50/#51/#52/#53/#55/#56/#58/#59). (minitor PR #60) |
| atrium-catalog-watcher skill (aeon) | 2026-06-05 | Jun-04 idea #2. New `skills/atrium-catalog-watcher/SKILL.md` — Friday 12:00 UTC weekly diff of the Atrium marketplace catalog at `https://atriumhermes.tech/.well-known/skills/index.json` against the prior snapshot. Reports added / removed / renamed skills with one-click `./install-from-atrium <name>` install commands on every `added` row. `install-from-atrium` (PR #335, merged Jun-03) was the third skill install path; until today the operator had no signal when new skills landed in the marketplace. Pairs with `sparkleware-catalog` (Tuesday — curated `skill-packs.json` registry, supply-side of the curated registry) and `skill-update-check` (Sunday — installed-skill drift) — three weekly digests now cover marketplace arrivals + curated registry health + installed-skill freshness without overlap. **Key design decisions**: (1) **`skill_id` is the canonical key**, not `name` — matches the onchain id `install-from-atrium 0x...` accepts; a rename or description tweak on a stable id is `updated`, never an add+remove pair (would noisily fire two notifications for one editorial change); (2) **Updates article-only, never notified** — same anti-noise rule `ecosystem-entrants` and `sparkleware-catalog` enforce; (3) **`ATRIUM_HOST` env override honored** — mirrors `install-from-atrium` convention; a host change re-baselines the diff (state records `atrium_host` so a switch is detected); (4) **curl + WebFetch fallback** — public endpoint with no auth header, so curl works when sandbox allows; WebFetch bypasses sandbox per CLAUDE.md pattern 1; (5) **56-day prune** (vs `ecosystem-entrants` 28d) because the Atrium catalog is smaller (~18 entries today) and a re-publish of the same `skill_id` after 2 months is still naturally a `recovered` signal worth keeping; (6) **No publisher/PR enrichment** — Atrium entries are onchain artefacts, not tied to a single GitHub PR; the article surfaces the install command instead, which is the operator's actual next step. 10-step skill: bootstrap, parse var, fetch with WebFetch fallback, host-switch detection, diff (added/removed/updated), write article, gated notify (baseline / host-switch / added / removed only — renames suppressed), atomic state write (.tmp + mv), 56-day prune, log. 7-state exit taxonomy (OK / QUIET / FETCH_FAIL / BAD_SHAPE / DRY_RUN / STATE_CORRUPT / BAD_VAR). Registered disabled in aeon.yml between `ai-framework-watch` and `competitor-launch-radar` (alphabetical, weekly section), schedule `0 12 * * 5`. skills.json total 182→183, alphabetical insert between `article` (idx 5) and `auto-merge` (idx 6), category `dev`. (aeon PR #342) |
| mcp-pulse backport (aeon-agent) | 2026-06-05 | 22nd consecutive same-day-after backport. Verbatim copy of upstream `skills/mcp-pulse/SKILL.md` from upstream aeon PR #272 (merged 2026-05-29) — the same general-ops batch as spend-monitor (PR #74, Jun-01), follow-up-patrol (PR #76, Jun-02), and narrative-convergence (PR #80, Jun-04). Friday 10:00 UTC weekly Model Context Protocol ecosystem tracker: queries `modelcontextprotocol` GitHub org, scans for new MCP server repos in 7d window, fetches npm `@modelcontextprotocol/sdk` downloads + PyPI `mcp` stats, runs 3 targeted WebSearches for news, scores momentum on 7-signal rubric (new company servers +3 each / new repos with 50+ stars +2 / npm uptick +3 / Anthropic-Stainless news +3 / etc.), writes thesis-check line (advancing / holding / stalling / reversing). aeon-agent currently has no signal about the MCP tooling landscape, which matters as the operator evaluates adding MCP servers to the agent configuration. Three adaptations vs upstream: (1) `./notify` call style — upstream uses `./notify -f .pending-notify-temp/<file>`, aeon-agent's notify reads its argument as `$1` (CLAUDE.md + dynamically-generated workflow notify script); step 8 rewrites as `./notify "$(cat .pending-notify-temp/mcp-pulse-${today}.md)"` — same temp-file body, single argv (same constraint that drove spend-monitor / follow-up-patrol / narrative-convergence); (2) `$(date -u -d '7 days ago' ...)` at step 3 replaced with literal `${today_minus_7}` derived from skill template — runner hook blocks `$(...)` ("Contains simple_expansion") per PRs #63/#67/#71/#77/#81; semantic trade "exact 7×24h ago" → "midnight UTC of 7 days ago" absorbed by momentum-scoring overlap; (3) **WebFetch fallback hardened** across every external endpoint (npm registry, PyPI stats, GitHub API) per CLAUDE.md sandbox pattern 1 — upstream calls it out under Sandbox Note but only mentions WebFetch for a couple of paths; this backport routes all external HTTPS through WebFetch fallback so the skill degrades gracefully on a sandboxed run. No frontmatter changes — `schedule:` key preserved verbatim. Registered disabled at `0 10 * * 5` (same slot as upstream) between `huggingface-trending` and `monitor-kalshi` (alphabetical, Upstream sync section). skills.json total 99→100 category research, alphabetical insert between `huggingface-trending` (idx 82) and `monitor-kalshi` (idx 83). **PR #272 backport queue status**: spend-monitor + follow-up-patrol + narrative-convergence + mcp-pulse done; fleet-scorecard remains deferred (depends on `memory/instances.json` + `scripts/prefetch-fleet-scorecard.sh` that aeon-agent does not currently maintain). Cadence (since aeon May-3 anchor): operator-scorecard May-3→4 → ... → narrative-convergence Jun-02→Jun-04 → **mcp-pulse Jun-02→Jun-05** (3-day delay because Jun-04 picked narrative-convergence over mcp-pulse — both were equally clean candidates; Jun-05 closes the gap). (aeon-agent PR #82) |
| per-column color labels (minitor) | 2026-06-05 | 7th rung on the per-column UX axis after tab groups (May-29 PR #53) → collapse (May-30 PR #55) → JSON export (May-31 PR #56) → quick-search (Jun-02 PR #58) → pin (Jun-03 PR #59) → duplicate (Jun-04 PR #60) → **color labels (this PR)**. Per-column 6-hex color label (e.g. `#f97316`). When set, the column renders a small color dot beside its title in the expanded header and replaces the brand accent gradient at the top of the collapsed strip with the operator's chosen color. Independent of tab groups (hide/show) and pin (reorder): color is the "what kind of column is this" marker layered on top of both. A pinned collapsed orange column stays pinned + collapsed + orange. **At 10–15 columns per deck, visual scan time becomes the bottleneck** — operators mentally group columns (DeFi/dev/news/social) but had no in-app marker for it. 10 files +611/-5: `drizzle/0008_column_color.sql` (additive nullable `text` column — no churn on existing rows); journal + 0008_snapshot.json; `lib/db/schema.ts` (`text("color")`); `lib/columns/types.ts` (`Column.color?: string` with full doc); `lib/deck-templates.ts` (`DeckTemplateColumn.color?: string` so starter decks can ship pre-colored lanes); `app/actions.ts` (+62 — `COLOR_HEX_RE` + `normalizeColumnColor()` canonical server-side validator (lowercased, 6-hex only — 3-hex shorthand and named CSS colors rejected so stored form is canonical); `updateColumnColor()` server action; `importedColumnSchema` Zod regex + `importDeck` re-validates through the normalizer (drop invalid like notifyWebhookUrl's SSRF guard, never abort the import); `exportDeck` emits when set; `duplicateColumn` inherits color (unlike `pinned` — color is a labeling decision, pin is a routing decision); `loadSnapshot` maps the field through to the wire shape); `lib/store/use-deck-store.ts` (+39 — `updateColor()` optimistic action mirroring server normalization; importedDeckPatch + duplicateColumn optimistic mirror); `components/column/configure-column-dialog.tsx` (+95 — new "Color label" field with 8 preset swatches (orange/green/blue/purple/pink/yellow/cyan/slate) + Clear button + freeform hex input with live invalid-hex error + disabled-Save guard); `components/column/column-card.tsx` (+16 — 10px circular color dot next to title in expanded header when set; expanded-header + collapsed-strip top accent gradient uses `column.color ?? type.accent`). **Three key design decisions**: (1) **Color IS inherited on duplicate; `pinned` is NOT** — color = visual labeling ("what kind of column?"), pin = routing ("primary column?"); duplicating a DeFi-orange column gives you another DeFi-orange column; (2) **Server-authoritative hex regex** `/^#[0-9a-f]{6}$/i` applied at both `updateColumnColor` and `importDeck` normalization, lowercased, 6-hex only — a tampered or hand-edited export can never smuggle a non-canonical color (named CSS colors, 3-hex shorthand, non-hex, JS expressions in style) into the DB; (3) **Round-trips through export/import/share-links + snapshots** — same DB-backed shape as PR #59's `pinned` because color is a persistence decision about the deck's identity, not view-state; deck snapshots already serialize via `exportDeck` so version history restores colors with no migration on existing snapshot rows. Custom palettes + auto-color-by-plugin deliberately out of scope — the whole point of color labels is operator intent. Could NOT run Next 16 build/typecheck (offline sandbox — same constraint as PRs #49/#50/#51/#52/#53/#55/#56/#58/#59/#60). (minitor PR #61) |
| ecosystem-links skill (aeon) | 2026-06-06 | Jun-04 idea #1. New `skills/ecosystem-links/SKILL.md` — Monday 11:55 UTC weekly URL-health audit of every link in ECOSYSTEM.md. GitHub URLs checked for `archived`/`disabled` via `gh api repos/{owner}/{repo}`; web URLs checked with `curl -sI --max-time 10 --location` + WebFetch fallback per CLAUDE.md sandbox pattern 1. Buckets: OK / ARCHIVED / MOVED / DEAD / INCONCLUSIVE / XONLY / OTHER. **Key sandbox-aware design**: INCONCLUSIVE never single-shots to DEAD — two consecutive INCONCLUSIVE runs required (sandbox-blocked outbound failure can't false-flag operator's curation). MOVED is distinct from DEAD (redirect to *different registrable host* = curation issue worth surfacing softer than DEAD). XONLY (x.com / twitter.com) deliberately not checked — unauthenticated HEAD to X is aggressively rate-limited; 429 reads identical to real dead handle, would slander curation. Logo URLs in first cell out of scope (CDN-hosted, not curation surfaces). Per-host 1.5s rate-limit. Transitions notify on `OK→DEAD/ARCHIVED/MOVED` and on recoveries `DEAD/ARCHIVED/MOVED→OK` (closes prior alert's loop). `STATE_CORRUPT` post-corruption baseline notifies if current snapshot has notifiable entries (going silent post-corruption is the wrong default). Closes the three-skill ecosystem loop: ecosystem-pulse (11:00, liveness) + ecosystem-entrants (11:45, arrivals) + ecosystem-links (11:55, URL validity). 7-state exit taxonomy. Registered disabled at `55 11 * * 1`, alphabetical insert between ecosystem-entrants and ecosystem-pulse in aeon.yml; generate-skills-json `dev` category line extended; skills.json total 193→194. (aeon PR #351) |
| $(date) batch self-fix — repo-actions + star-momentum-alert (aeon-agent) | 2026-06-06 | Jun-04 idea #4. Closes the shell-substitution anti-pattern chain across the last 2 sites (repo-article line 26 was already fixed by PR #81 same-day-as-article-publication, so the actual remaining work was 2 sites not 3). `skills/repo-actions/SKILL.md` step 1 — `$(date -u -d '14 days ago' +%Y-%m-%dT%H:%M:%SZ ...)` → literal `SINCE=YYYY-MM-DDT00:00:00Z` computed from `${today}-14d`, hoisted out front like repo-pulse / repo-article do (mirrors PR #77/#81 pattern verbatim). `skills/star-momentum-alert/SKILL.md` step 3 — `for D in $(seq 13 -1 0); do DATE=$(date -u -d "${today} - ${D} days" ... || date -u -j -v-${D}d ...)` (3 substitution sites per iteration: outer `$(seq...)`, inner GNU `$(date...)`, BSD fallback `$(date...)`) → pre-built 14-element `DATES=(YYYY-MM-DD YYYY-MM-DD ... )` bash array iterated via `"${DATES[@]}"` (parameter expansion, runner-hook-permitted) where the agent fills the 14 real dates from `${today}` minus 0–13 days before running. Semantic trade matches accepted chain convention: cutoff/series goes from "exactly Nx24h ago" to "midnight UTC of N days ago" — repo-actions' 14d window absorbs the overlap; star-momentum-alert's per-day series only consults logs (already keyed by UTC midnight date) so the 14-date array is a 1:1 replacement not an approximation. **Chain status**: PR #63 (weekly-shiplog May 26) → PR #67 (push-recap May 28) → PR #71 (heartbeat May 30) → PR #77 (repo-pulse Jun 02) → PR #81 (repo-article Jun 04) → **PR #83 (repo-actions + star-momentum-alert Jun 06)**. Every known site closed. (aeon-agent PR #83) |
| vigil-revoke skill (aeon) | 2026-06-07 | Jun-06 idea #2. New `skills/vigil-revoke/SKILL.md` — workflow_dispatch-only skill that revokes a single live ERC-20 approval on Base via Bankr. `var` is a `wallet:spender:token` triplet (matches tuples `vigil_scan_approvals` and `approval-audit` emit). Strict allowlist on input (`^0x[hex40]:0x[hex40]:0x[hex40]$`, case-insensitive normalised lower — same hardening pattern VIGIL adopted in review round 4). Bankr ownership pre-check (`/wallet/me` ≟ triplet wallet) refuses cross-wallet submissions BEFORE any state-changing call — Bankr signs from its own bound wallet, so a wrong-wallet triplet either silently revokes a different approval or fails at submit time; catch it before any side effect. Pre-revoke `allowance(owner,spender)` `eth_call` short-circuits to NOOP when already zero — no gas spent on a redundant submission. Post-revoke receipt poll (`eth_getTransactionReceipt`) + final allowance read so SUCCESS is chain-confirmed not Bankr-reported (a `completed` Bankr status without a tx hash means the submission was acknowledged but the chain hasn't surfaced it yet). 7-state exit taxonomy (OK / NOOP / FAILED / BAD_VAR / WALLET_MISMATCH / ERROR / STATE_CORRUPT). State `memory/topics/vigil-revoke-log.json` append-only with 500-entry cap. Closes the detection→revoke loop VIGIL PR #323 explicitly split out ("Bankr-gated, state-changing — separate PR.") and that wallet-risk-weekly (PR #340, 2026-06-04) has been surfacing HIGH-bucket approvals into for the last 3 days with no autonomous remedy path. **Key design decisions**: (1) NO AUTO-RETRY — failed revokes could mean insufficient gas, contract paused, Bankr 5xx, or sandbox network blip; none safe to retry blindly. Next `workflow_dispatch` is the retry. (2) NO BULK REVOKE PER RUN — one triplet per run. Bulk is a separate `vigil-revoke-batch` skill (deliberately out of scope, blast radius bounded). (3) NO "TRUSTED SPENDER" AUTO-SKIP — even Uniswap routers can be exploited; trust-list filtering belongs upstream in wallet-risk-weekly severity bucketing, not here. If the operator names a triplet the skill revokes (or NOOPs on already-zero). (4) NO PROMPT-INJECTION SURFACE — Bankr `/agent/prompt` body only interpolates validated 40-hex addresses, never operator-typed text or fetched contract metadata. (5) State file `vigil-revoke-log.json` is APPEND-ONLY by design — never rewrite history; cap is informational eviction at 500 entries, older entries still in git. Capabilities: `external_api, writes_external_host, onchain_writes, sends_notifications` (per locked taxonomy in docs/CAPABILITIES.md). Registered disabled in aeon.yml end of Hound onchain investigation section (after `holder-concentration`). skills.json regenerated (193→194), category onchain-security. generate-skills-json `onchain-security` case line extended (`lp-lock-check|vigil|vigil-revoke|wallet-risk-weekly`). 5 of the 6 HoundFlow security skills (approval-audit/honeypot-check/lp-lock-check/linked-wallets/fund-flow/investigation-report) still scheduled-consumer-free; this skill is the WRITE-SIDE companion to the read-only inventory not an attempt to schedule any of them. (aeon PR #354) |
| skill-of-the-day backport (aeon-agent) | 2026-06-07 | 23rd consecutive same-day-after backport. Verbatim copy of upstream `skills/skill-of-the-day/SKILL.md` from upstream aeon PR #341 (Nurstar, merged 2026-06-04). Daily meta-content skill producing two notifications per `workflow_dispatch`: (1) paste-ready "Aeon skill of the day" long-form tweet draft to `.outputs/skill-of-the-day.md` (operator copies to X), (2) the picked skill's live outcome via the dispatched run's own `./notify` (operator screenshots into the "Result ⤵️" body of the tweet). Queue precedence at step 1: `${var}` override → `memory/topics/skill-of-the-day.md` Queue → `ls skills/` catalog fallback. 30-day Covered window suppresses repeats. Blocklist suppresses meta/internal skills that don't read as features. **Three adaptations vs upstream**: (1) `./notify` call style ALREADY ALIGNED — upstream uses `./notify "$(cat .outputs/skill-of-the-day.md)"` matching aeon-agent positional-`$1` contract (CLAUDE.md + dynamically-generated workflow notify script at `aeon.yml:263` reads `MSG="$1"`). **First backport where notify wiring needed NO translation** — same constraint that drove rewrites in spend-monitor (PR #74) / follow-up-patrol (PR #76) / narrative-convergence (PR #80) / mcp-pulse (PR #82) all rewriting upstream's `-f file` style → `$(cat <temp>)` positional argv. PR #341 used the positional style from the start, so the backport translates by leaving the line alone. (2) Queue seed file is OPTIONAL — catalog-fallback at step 1.3 reads `skills/` directly and skips blocklist + 30-day Covered entries, so fresh aeon-agent install picks up natively from the ~100-skill aeon-agent catalog without manual queue priming; step 6 extended to create the canonical three-section layout the first time the skill writes to the file. (3) `gh workflow run aeon.yml -f skill="${PICK}"` at step 5 ships verbatim — confirmed against aeon-agent's `.github/workflows/aeon.yml` that the same `workflow_dispatch` `skill` input exists. If upstream ever renames the input, this is the single line that moves with it. Inline backport-note block at top cites PR #341 + each adaptation. Registered disabled in aeon.yml at `workflow_dispatch` between `skill-graph` and `smithery-manifest` (alphabetical Upstream sync section). skills.json total 100→101, category `social`. generate-skills-json CATEGORIES associative array extended with `[skill-of-the-day]="social"`. **PR #272 backport queue now closed except fleet-scorecard** (deps unmet: `memory/instances.json` + `scripts/prefetch-fleet-scorecard.sh` aeon-agent does not currently maintain). PR #341 was a single-skill PR — backport closes in one round. Cadence (since aeon May-3 anchor): operator-scorecard May-3→4 → ... → mcp-pulse Jun-02→Jun-05 → **skill-of-the-day Jun-04→Jun-07**. (aeon-agent PR #85) |
| star-milestone auto-dispatch (aeon) | 2026-06-08 | Jun-06 idea #5 (last open Jun-06 idea — #1 OAuth-writeback deferred as CORE-files-risky autonomous edit). New step 8 in `skills/star-milestone/SKILL.md`: when a milestone is announced (gate 5f path), the skill looks up `memory/topics/milestone-dispatch.json` for an auto-dispatch rule and fires the matching skill via `gh workflow run aeon.yml -f skill=<name> -f var=""`. **Silent records bypass dispatch entirely** — bootstrap/stale/deferred/skipped milestones (gates 5b/5c/5d/5e) record silently and do NOT auto-dispatch. A launch draft fired on a fake-star burst or stale-recovery would be the wrong signal; the silent-record path was explicitly designed to suppress this. Seeded with `aaronjmars/aeon:500 → show-hn-draft` so the wiring is live on merge (aeon at 490⭐, v7≈3.6/day, 500 lands ~Jun 11). **Three key design decisions**: (1) **Configurable rule map, not a hard-coded 500-check** — the article's "simpler" alternative was a single hard-check, but a map lets operators wire any (repo, threshold) → skill pairing later (e.g. future `1000 → product-hunt-launch`) without another patch to star-milestone; (2) **Defense-in-depth idempotency** — step 5a (milestone-already-recorded in `milestones.md`) is the primary guard; the `dispatched` map in `milestone-dispatch.json` is a second guard for cases where milestones.md is hand-edited or git-reverted. A second pass at 502⭐ never fires show-hn-draft a second time; (3) **Failure path mirrors `skill-of-the-day`'s convention** — one attempt, one recovery notification with the manual `gh workflow run aeon.yml -f skill=<name>` command, no auto-retry. The dispatched flag is NOT written on failure, but step 5a still prevents auto-retry on the next run (a launch draft regenerated on every milestone re-run would be churn the operator doesn't want). Renumbered: old step 8 → 9, old step 9 → 10. Log format extended with `**Dispatched**: ${SKILL} | none | FAILED — ${reason}`. Frontmatter description updated to mention the rule map. Sandbox note updated to confirm `gh workflow run` uses the same auth as `gh api`. (aeon PR #358) |
| ecosystem-links backport (aeon-agent) | 2026-06-08 | 24th consecutive same-day-after backport. Verbatim copy of upstream `skills/ecosystem-links/SKILL.md` from upstream aeon PR #351 (merged 2026-06-06). Weekly Monday 11:55 UTC URL-health audit of every link in ECOSYSTEM.md. Buckets `OK / ARCHIVED / MOVED / DEAD / INCONCLUSIVE / XONLY / OTHER`. **Sandbox-aware INCONCLUSIVE design**: INCONCLUSIVE never single-shots to DEAD — two consecutive INCONCLUSIVE runs required (sandbox-blocked outbound failure can't false-flag operator's curation). MOVED separate from DEAD (redirect to *different registrable host* = curation issue worth softer surface than DEAD). XONLY (x.com/twitter.com) deliberately not checked — unauthenticated HEAD to X is aggressively rate-limited; 429 reads identical to real dead handle, would slander curation. Logo URLs in first cell out of scope (CDN-hosted display assets, not curation surfaces). Per-host 1.5s rate-limit. Transitions notify on `OK→DEAD/ARCHIVED/MOVED` and on recoveries `DEAD/ARCHIVED/MOVED→OK` (closes prior alert's loop). `STATE_CORRUPT` post-corruption baseline notifies if current snapshot has notifiable entries. Closes two-of-three of the ecosystem loop on aeon-agent: ecosystem-pulse (Mon 11:00, liveness — already backported) + ecosystem-links (Mon 11:55, URL validity — THIS PR). ecosystem-entrants (Mon 11:45, arrivals — upstream PR #339, NOT yet backported) is the third leg; references to it in this skill are informational narrative (parser-shape parity note, three-skill-loop table) and do NOT affect function — backporting it is a natural follow-up. **Three adaptations vs upstream**: (1) `./notify` call style ALREADY ALIGNED — upstream uses `./notify "$MSG"` (positional single argument) which matches aeon-agent's `MSG="$1"` contract. **Second backport in a row where notify wiring needed NO translation** — same as PR #85 skill-of-the-day Jun-07, distinct from PRs #74/#76/#80/#82 which had to rewrite upstream's `-f file` style. (2) No `$(date ...)` substitutions in this skill — the runner-hook `Contains simple_expansion` anti-pattern closed by PR #83 chain does not apply. `${today}` used consistently. (3) WebFetch fallback for curl already part of the upstream skill (CLAUDE.md sandbox pattern 1) — preserved verbatim. Registered disabled at `55 11 * * 1` between ecosystem-pulse and config-validator (alphabetical) in the `# --- Upstream sync (2026-05-14) ---` section. skills.json total 101→102, category research, alphabetical insert between competitor-launch-radar and ecosystem-pulse. `generate-skills-json` CATEGORIES dict extended with `[ecosystem-links]="research"`. **Why ecosystem-links over atrium-catalog-watcher (upstream PR #342 Jun-05)**: Atrium-catalog-watcher would have been the natural Jun-05→Jun-08 link in the chain, but aeon-agent does NOT have the `install-from-atrium` script (upstream PR #335). The skill's value depends on emitting `./install-from-atrium <name>` install commands on every `added` row — backporting without that dependency would surface notifications recommending a non-existent command. Ecosystem-links has all its prerequisites met today (ECOSYSTEM.md exists, ecosystem-pulse exists). install-from-atrium + atrium-catalog-watcher are a clean two-PR follow-up if the operator wants Atrium tracking on this fork. Cadence (since aeon May-3 anchor): operator-scorecard May-3→4 → ... → skill-of-the-day Jun-04→Jun-07 → **ecosystem-links Jun-06→Jun-08** (2-day delay because Jun-07 picked skill-of-the-day over both PR #342 and PR #351; Jun-08 closes the gap on PR #351 while PR #342 remains deferred per install-from-atrium dependency). (aeon-agent PR #87) |
| per-deck drag-to-reorder (minitor) | 2026-06-08 | Operator-noted gap. `reorderDecks` has lived in the deck store + server action since the sidebar deck-list shipped (`lib/store/use-deck-store.ts:343` calls `serverReorderDecks`, `app/actions.ts` persists), but there was no UI affordance to call it. With Jun-06 article ideas FULLY CONSUMED and no fresh ai-build issues / no remaining repo-actions ideas for minitor, this is the natural unbuilt rung surfacing from the codebase itself. **Sidebar-level analog of the per-column DnD already in deck-board.tsx** — same gesture (grab handle, drag) now works at both levels: columns reorder within a deck, decks reorder within the sidebar. 1 file +313/-170: `components/sidebar-01/nav-decks.tsx` (wrapped deck list `.map()` in DndContext + SortableContext using verticalListSortingStrategy + restrictToVerticalAxis modifier; extracted per-deck JSX into new SortableDeck inner component using `useSortable({id: deck.id})` mirroring the column-card DnD pattern; added GripVertical drag handle on the left of each deck label positioned `left-1.5 top-2.5 size-5` visible only on group-hover with same opacity-on-hover pattern as the existing More button; SidebarGroupLabel `pl-2 → pl-7` reserves 28px on the left mirror of `pr-9` reservation for More button; lifted all handlers — rename/color/history/delete/add-column/rename-column/delete-column/activate — into SortableDeck props so component stays presentational and `key={deckId}` remains stable across reorder; DnD `setNodeRef` attached to an explicit `<div>` wrapper rather than passed to SidebarGroup — defensive against ref-forwarding edge cases on function components). **Six key design decisions**: (1) **Pointer activation distance: 4px** — matches deck-board:39's column DnD threshold. Stray click on the small handle (20px square) does NOT trigger sort; only real drag intent (>4px movement) starts reorder; (2) **`restrictToVerticalAxis` modifier** — decks stack vertically; horizontal drag would suggest "moving to a different sidebar group" which doesn't exist; (3) **No reorder-across-collapse-state restriction** — unlike column DnD's pin-vs-unpin boundary (deck-board.tsx:135-137), deck open/closed is purely view state (openOverrides is local React state, NOT persisted); dragging an open deck past a closed deck doesn't change persisted attributes on either, so only destination order matters; (4) **Activate-on-click NOT triggered by drag** — existing setActiveDeck happens via SidebarMenuButton column click + CollapsibleTrigger toggle; dragging the handle never triggers setActiveDeck, which is the correct invariant (reordering should not change which deck is showing); (5) **Layout reservation is constant, not conditional** — `pl-7` that toggles to `pl-2` on hover-out would cause deck names to shift left/right as mouse moves, reads as flickery; (6) **DnD ref hosted on `<div>` wrapper, not SidebarGroup** — even though React 19 supports `ref` as a regular prop on function components, the wrapper makes the DnD contract independent of SidebarGroup's internal implementation; wrapper is layout-transparent (`relative` only) so absolute drag handle + absolute More button inside SidebarGroup keep their offsets. NO DB schema change, NO migration, NO server action change (existing `serverReorderDecks` already wired). Could NOT run Next 16 build/typecheck (offline sandbox — established constraint across PRs #49–#63). (minitor PR #65) |
| show-hn-draft prompt refresh (aeon) | 2026-06-09 | Jun-08 idea #4. `skills/show-hn-draft/SKILL.md` prompt context refreshed for the ~Jun 11 500⭐ auto-fire wired by yesterday's `star-milestone` PR #358. Launch-trigger framing 300⭐/12-day-horizon → 500⭐ auto-dispatch (no manual gate; this skill must have a fresh draft sitting in `articles/` when star-milestone fires). Project-scale shorthand 250⭐/90+ skills → 500⭐/165 forks/195 skills/8 categories/onchain layer/external skill-packs ecosystem. Title example "90+ skills" → "195 skills". **Body §2 hint** now explicitly points the LLM at three specific non-obvious capabilities to use as the senior-engineer surprise hook: (1) onchain security stack `vigil` + `wallet-risk-weekly` + `vigil-revoke`, (2) three install paths clone/install-skill-pack/install-from-atrium, (3) external skill-pack inflow (Nurstar/vigilcodes/HoundFlow/signa/Careful Finance/Mneme). Hard cap: pick ONE, do not list all three (LLM left to its own scoring). Launch checklist + edge case "stars > 300" → "stars cross the next round number — 500, 750, 1000" so the skill stays correct past 500⭐. Edge case explicitly names star-milestone as the auto-dispatcher closing the loop. **Why this is the right Jun-08 idea today**: timing-sensitive (auto-fire is days away), low-risk (prompt-only, no schema), and the fired draft at 500⭐ is the first impression for thousands of HN readers — gap between early-May framing and current product surface is the entire bug. Picked over Jun-08 idea #1 (Phase 2 Capabilities Declarations Sweep, ~150 skills) because that's 1-2 days of work; over idea #2 (ecosystem-entrants backport) because it's the aeon-agent slot today not aeon's. 1 file +6/-6. (aeon PR #380) |
| install-from-atrium backport (aeon-agent) | 2026-06-09 | Jun-08 idea #5. Verbatim copy of upstream `install-from-atrium` shell script from upstream aeon PR #335 (merged 2026-06-03). Third skill install path on this fork — alongside `./add-skill` (any GitHub repo) and `./install-skill-pack` (curated community packs with skills-pack.json manifest). Fetches a skill from the Atrium onchain marketplace at `$ATRIUM_HOST/.well-known/skills`, runs Aeon's own `skill-security-scan` against it (never bypassed without `--force`), records onchain provenance in `skills.lock` namespaced `atrium:0x<skill_id>` so the rows don't collide with GitHub-sourced rows. **First non-SKILL.md backport in the 25-PR same-day-after chain** — every prior backport was a `skills/<name>/SKILL.md` copy. **No adaptations needed**: this script is repo-relative throughout (`$ROOT = dirname of script`, `$SKILLS_DIR = $ROOT/skills`, `$SCANNER = $ROOT/skills/skill-security-scan/scan.sh`, `$LOCK_FILE = $ROOT/skills.lock`) and aeon-agent's directory layout matches upstream exactly on every path the script touches. No `./notify` style to translate (CLI tool, not scheduled skill). No `$(date ...)` substitution sites to close (the one site fires only on `--force` security log, outside runner hook). No WebFetch fallback needed (operator-invoked, not workflow_dispatch). **Prerequisites verified on this fork**: `skills/skill-security-scan/scan.sh` exists with same contract as `add-skill` uses (scan fails → block; --force overrides → log to `memory/logs/security.log`); jq available; skills.lock schema matches add-skill rows. **Unblocks atrium-catalog-watcher backport** (upstream PR #342 Jun-05) which was explicitly skipped in yesterday's `ecosystem-links` backport PR #87 because of the missing install-from-atrium dependency — atrium-catalog-watcher emits one-click `./install-from-atrium <name>` install commands on every newly-detected Atrium marketplace entry. Natural 26th-link follow-up. 1 file +94 (new file, chmod 755). Cadence (since aeon May-3 anchor): operator-scorecard May-3→4 → ... → ecosystem-links Jun-06→Jun-08 → **install-from-atrium Jun-03→Jun-09** (6-day delay because aeon-agent picked SKILL.md backports over this CLI tool for 6 rounds — atrium-catalog-watcher backport now unblocked). (aeon-agent PR #90) |
| deck keyboard navigation shortcuts (minitor) | 2026-06-09 | Jun-08 idea #3. **10th rung on the per-column UX axis** after tab groups (May-29 PR #53) → collapse (May-30 PR #55) → JSON export (May-31 PR #56) → quick-search (Jun-02 PR #58) → pin (Jun-03 PR #59) → duplicate (Jun-04 PR #60) → column color (Jun-05 PR #61) → deck color (Jun-06 PR #62) → width (Jun-07 PR #63) → deck DnD (Jun-08 PR #65) → **keyboard nav (this PR)**. Single `keydown` listener at the `DeckBoard` level routes events to actions that already exist: `j`/`ArrowRight` → focus next visible column (wraps); `k`/`ArrowLeft` → focus previous (wraps); `/` → request inline search open + focus input on the focused column; `c` → toggle collapse on focused; `Escape` → two-step (clear focused column's search query → clear focus ring). Plain-key only — modifier presses (Ctrl/⌘/Alt) left to browser/OS so ⌘K et al still work. Mirrors muscle memory operators have from Linear, GitHub issues, and most terminal dashboards. 3 files +209/-1: `lib/store/use-deck-store.ts` (+68 — `focusedColumnId: string \| null` + `pendingSearchOpen: string \| null` view-state-only fields; `setFocusedColumn` / `requestSearchOpen` / `clearPendingSearchOpen` actions; cleanup in `deleteDeck` + `removeColumn` so listener can't operate on stale id; `pendingSearchOpen` is a column id not a boolean so a fast `j / j /` sequence lands on second column's search); `components/deck/deck-board.tsx` (+117 — keyboard useEffect with modifier-key + input/textarea/select/contenteditable guard; `visibleColumnIds` reused as focus order so j/k walk pinned-first tab-filtered order, not raw `columnIds`; `requestAnimationFrame(scrollIntoView({inline: nearest, behavior: smooth}))` after j/k so focus ring stays visible past horizontal scroll edges; `/` no-op when nothing focused — j to pick, / to search); `components/column/column-card.tsx` (+25 — `ring-2 ring-[color:var(--brand)]/60` on the focused column's wrapper on BOTH the expanded and collapsed render paths; click-to-focus handler on both wrappers so mouse + keyboard agree; `useEffect` reacting to `pendingSearchOpen === column.id` opens the search row + focuses the input + immediately clears the signal). **Six design decisions**: (1) `/` no-op when nothing focused — wrong direction to surface a search bar on a column the operator hasn't picked; (2) j/k wrap — alternative is a focus ring stuck at edges demanding mouse reset; (3) click sets focus too — mouse + keyboard agree on what's selected; (4) `visibleColumnIds` is the focus order not `columnIds` — j/k walk in the order the operator sees, respecting pin-first + tab filtering; (5) two-step Escape — first clears search, second clears focus, lets operator stay on column while exiting search mode; (6) window listener bails on text-editable elements + modifier keys. NO DB schema, NO migration, NO server action change, NO plugin contract touched, NO version bump on `DECK_EXPORT_VERSION` (view-state-only matching collapse/search/width pattern). Could NOT run Next 16 build/typecheck (offline sandbox — same constraint as PRs #49-#65). (minitor PR #66) |
| per-column width control (minitor) | 2026-06-07 | Jun-06 idea #4. **8th rung on the per-column UX axis** (tab groups May-29 PR #53 → collapse May-30 PR #55 → JSON export May-31 PR #56 → quick-search Jun-02 PR #58 → pin Jun-03 PR #59 → duplicate Jun-04 PR #60 → color Jun-05 PR #61 → **width**). Per-column three-step width override: **narrow** (240px), **normal** (360px, historical default), **wide** (480px). Selectable from each column's More-menu dropdown — three new `DropdownMenuItem` entries between *Download items (JSON)* and *Delete*, each showing a brand-coloured `Check` icon next to the active width. Width is **VIEW-STATE ONLY**, NOT persisted — same lifetime as `collapsedColumnIds` and `searchByColumn`. Reload returns every column to 360px. Deck export/import/share-links carry no width data. 2 files +109/-2: `lib/store/use-deck-store.ts` (+62 — new `ColumnWidth` type `"narrow" | "wide"` with absence-from-map = normal; new `widthByColumn: Record<string, ColumnWidth>` field initialised `{}`; new `setColumnWidth(columnId, width|null)` action with `width === null` delete-from-map and `width === existing` no-op-fast-path (returns prior state object so React skips re-render on a hot menu path); `deleteDeck` + `removeColumn` actions extended to scrub stale `widthByColumn` entries alongside `collapsedColumnIds`/`searchByColumn`); `components/column/column-card.tsx` (+47 — `Check`, `Maximize2`, `Minimize2` lucide imports; store reads `columnWidth` + `setColumnWidth`; resolved `widthClass` const with **default branch character-identical to the prior single class** so every existing column on every existing deck renders pixel-identical until the operator opts in; `transition-shadow` → `transition-[box-shadow,width]` so width animates smoothly instead of snapping; three menu items with `Check` on the active row + invisible spacer on the Normal item for icon alignment). **Six key design decisions**: (1) **View-state, not DB-backed** — matches the rule used for collapse / search / selected tab. Going DB-backed would mean a `drizzle/0010_column_width.sql` migration + `app/actions.ts` server action + `DECK_EXPORT_VERSION` bump + round-trip through every export/import/share-link/snapshot path — none pays for itself for an ergonomic-only knob the operator changes a few times per session. (2) **Three steps, not a drag-resize handle** — continuous resize compounds poorly with collapse (where does the collapsed strip's expand restore to?), pin (does pinned share the resize affordance with unpinned?), and color (the brand accent dot would need re-sizing). Three named steps avoid all that. (3) **Default unchanged** — null branch is the exact prior class, every existing column renders pixel-identical until opt-in. (4) **Mobile (`<sm`) clamps to `min(360px, calc(100vw-1rem))` regardless of width override** — a 480px wide column would overflow a phone viewport; mobile keeps the clamp on narrow + wide too. Width override is desktop-only ergonomics. (5) **Collapse owns its 48px strip** — width re-applies on expand from the in-memory map; collapsed-narrow and collapsed-wide are the same 48px strip. (6) **Same-width re-click is a no-op** (returns prior state object) so React skips re-render on a hot menu path. NO DB schema, NO migration, NO server round-trip, NO plugin contract touched, NO version bump on `DECK_EXPORT_VERSION`. Could NOT run Next 16 build/typecheck (offline sandbox — same constraint as PRs #49/#50/#51/#52/#53/#55/#56/#58/#59/#60/#61/#62). (minitor PR #63) |
| self-improve cron-state primary (aeon-agent) | 2026-06-06 | Self-improve: `skills/self-improve/SKILL.md` step 2b2 — `./scripts/skill-runs --hours 48 --failures` → read `memory/cron-state.json` directly. The 3 most recent self-improve runs (PR #77 Jun-02, PR #81 Jun-04, today's start) all logged the same workaround — `./scripts/skill-runs` is sandbox-blocked because it invokes `gh api` against the Actions API. `memory/cron-state.json` is the writer-side mirror of the same state (`total_runs`, `total_failures`, `consecutive_failures`, `last_status`, `success_rate` per skill), a plain local JSON file the Read tool can open with no network or shell execution. CLAUDE.md's `cron-state` description already names `self-improve` as a consumer; SKILL.md just wasn't doing it. Skill-runs demoted to a fallback for manual runs with network access, with the recurring-blocker history (PRs #77, #81, May-26 onward) cited inline so a future cleanup doesn't promote it back to primary without checking sandbox behavior first. 4 lines net, single file. (aeon-agent PR #84) |
| runner-hook doc in CLAUDE.md (aeon-agent) | 2026-06-08 | Self-improve: documents the runner-hook `$(...)` / `$VAR` restriction system-wide in CLAUDE.md Sandbox Limitations section 3, and fixes the stale `aeon.yml:156` mcp-pulse comment that still claimed `${today_minus_7}` cutoff. The anti-pattern has driven 6 consecutive `improve:` PRs over 14 days (#63 weekly-shiplog May-26, #67 push-recap May-28, #71 heartbeat May-30, #77 repo-pulse Jun-2, #81 repo-article Jun-4, #83 repo-actions + star-momentum-alert Jun-6) plus a 7th mid-PR fix on mcp-pulse PR #82 (Jun-5) when the agent inherited a phantom `${today_minus_7}` reference from a `repo-actions` Jun-4 idea suggestion. **The class of bug was "constraint isn't in CLAUDE.md", not "another skill needs the same patch"** — every skill's bash block was vulnerable independently, and the `repo-actions` skill itself surfaced the gotcha in its own output (`articles/repo-actions-2026-06-04.md:59` literally proposed `${today_minus_7}` as a fix). Adding the constraint to CLAUDE.md propagates to all future skill runs, including non-feature skills like `repo-actions` that originated the phantom-variable suggestion. The new section 3 explicitly names `${today}` + `${var}` as the ONLY runner-injected vars, debunks `${today_minus_N}` as a phantom that resolves to a literal string and silently breaks date filters, lists the 6+1 fix history inline so the constraint is traceable to its evidence. The `aeon.yml:156` stale comment was the last documentation survivor — the actual skill body at SKILL.md:36 + :127 + :307 already says "there is no `${today_minus_7}` template var" but the aeon.yml description hadn't been updated. **Why not "add pre-merge self-review to feature skill"** (the alternative push-recap Jun-6 explicitly suggested): bigger effort (rewrites feature skill flow), recent backports (Jun-07 skill-of-the-day, Jun-08 ecosystem-links) shipped clean with zero mid-PR fixes (pattern already trending down), and CLAUDE.md documentation has higher leverage. 17 lines across 2 files. (aeon-agent PR #89) |
| per-deck color labels (minitor) | 2026-06-06 | Jun-04 article context ("the next orthogonal surface is visual organization at the deck level"). **The deck-level analog of column color labels (PR #61).** Per-deck 6-hex color label (e.g. `#f97316`). When set, renders as the identity dot in the sidebar deck header (replaces the brand active/inactive dot — operator's intent stays in their color regardless of active state; inactive tagged decks dip to 65% opacity so active still reads as primary) AND a 10px dot next to the deck name in the deck-view top bar. **Reuses the column color palette + normalizer verbatim** — same 8 swatches (orange/green/blue/purple/pink/yellow/cyan/slate), same `normalizeColumnColor` server function, same `/^#[0-9a-f]{6}$/i` regex; forking would invite the two surfaces to drift on case-folding or shorthand acceptance. 11 files +708/-3: `drizzle/0009_deck_color.sql` (additive nullable `text` — no churn on existing rows) + journal + `0009_snapshot.json`; `lib/db/schema.ts` (`color: text("color")` on decks); `lib/columns/types.ts` (`Deck.color?: string` with full doc); `app/actions.ts` (+57 — `updateDeckColor` server action; `loadSnapshot` maps; `importedDeckSchema.deckColor` optional max 64 — additive to v1 not v2, old exports omit and import as null; `exportDeck` emits when set; `importDeck` re-validates through `normalizeColumnColor` and hoists `deckColorPersisted` out of transaction so `ImportedDeckResult.deckColor` propagates to optimistic client store; `ImportedDeckResult` gains `deckColor?`); `lib/store/use-deck-store.ts` (+20 — `updateDeckColor` action mirroring `renameDeck`'s optimistic-then-fire pattern; `importedDeckPatch` carries deckColor; new `serverUpdateDeckColor` import); `lib/deck-templates.ts` (`DeckTemplatePayload.deckColor?` so starter templates can ship pre-colored); new `components/dialogs/deck-color-dialog.tsx` (same 8 swatches + Clear button + freeform hex input + live invalid-hex error; Save lights up only on a meaningful change — cleared from tagged deck, or swapped to different valid color; opening + closing without touch = no-op); `components/sidebar-01/nav-decks.tsx` (+39 — Palette icon import, new "Set color"/"Change color" menu item between Rename and Version history, tagged-deck dot rendering in operator's color with 65%-inactive opacity, untagged decks keep prior brand-vs-faded behavior); `components/deck/deck-view.tsx` (10px circular color dot next to deck name in top bar when set). **Three key design decisions**: (1) **Operator color overrides active-deck brand color** — when a deck is tagged, dot stays in its color even when active; swapping to brand would erase intent; inactive tagged decks dip opacity to 65% so active still reads as primary. (2) **Schema is additive, not v2** — old exports omit `deckColor` and import as `color = null`; Zod accepts unknown-but-absent fields; no version bump, old share-links continue to work. (3) **No snapshot capture on color change** — renames don't snapshot either, color is metadata not structural; version history covers structural mutations (add/remove/reorder/duplicate/import/restore), not cosmetic ones. Could NOT run Next 16 build/typecheck (offline sandbox — established constraint). (minitor PR #62) |

## Lessons Learned
- Digest format: Markdown with clickable links, under 4000 chars
- Always save files AND commit before logging
- Polymarket Gamma API: use volume_num_24hr sort for signal; newest markets are mostly noise
- GITHUB_TOKEN cannot push workflow file changes — needs `workflows` permission (PAT or fine-grained token)
- Self-improve outpaces review: agent opens PRs faster than human merges. PR awareness guard stops at 3+ open PRs
- fetch-tweets dedup: persistent seen-file + SHA256 notify layer + per-run delta + scheduler catch-up gate — three dedup layers stack end-to-end
- aeon-agent still at pre-autoresearch-evolution SKILL.md versions (aeon PRs #46–#136 not yet backported) — SKIP_UNCHANGED/NEW_INFO exits, significance gates not active here
- grok-4-1-fast is a thinking model — reasoning trace eats output tokens before answer list; always set max_output_tokens ≥ 16384 in xai_search calls (triggered May-6: 2 tweets instead of 10+)
- tweet-allocator Bankr prefetch: empty `verified-handles.json` had three ambiguous causes — resolved by `.error` marker (aeon-agent PR #24)
- Sandbox blocks `$ENV_VAR` in curl headers → skill reads auth failure as "key not set" even when the key IS set. Fix: read from prefetch-written log files instead of curling authenticated APIs inside the skill (aeon-agent PR #48)
- XAI HTTP 403 = team credits exhausted (NOT missing/invalid key); distinct from 401 (bad key). First occurrence May 24 — same-day self-corrective PR response (aeon-agent PR #60) within 6.5h of detection visible in log
- 5 scheduled skills disabled in aeon-agent effective 2026-05-28 (PR #65): fetch-tweets, tweet-allocator, skill-leaderboard, hyperstitions-ideas, ai-framework-watch — deliberate scope trim; tomorrow's logs won't carry those entries
- minitor migration numbering: PRs merge in order on main; don't assume migration number from idea notes — verify what's on main (PR #50 assumed 0002 but landed as 0003 because PR #49's refresh-interval took 0002 first)

## Repo Actions Ideas Pipeline
~93 ideas generated (20 runs). Recently built: scan.sh Bash 3.2 fix (aeon Issue #182) + fork-skill-gap backport (aeon-agent) + CoinGecko column (minitor) (May-19 — burned May-18 ideas #1, #2, #4), scan.sh POSIX-ERE fix (aeon — AntFleet H6) + competitor-launch-radar backport (aeon-agent) + DeFiLlama column (minitor) (May-20), contributor-spotlight FORK_DEFAULT_BRANCH fix (aeon — AntFleet H3) + skill-update-check branch-honor (aeon-agent — AntFleet H7) + deck share link (minitor) (May-21 — burned May-20 ideas #1, #3, #4), install-skill-pack CLI (aeon — burned May-20 idea #2) + scan.sh Bash 3.2 + POSIX-ERE backport (aeon-agent) + starter deck templates gallery (minitor — burned May-20 idea #5) (May-22), skill-packs.json registry + --list (aeon — May-22 idea #1) + contributor-spotlight FORK_DEFAULT_BRANCH backport (aeon-agent — May-22 idea #3) + /gallery public deck page (minitor — May-22 idea #5) (May-23), **v4-readiness H1 alignment fix (aeon — May-22 idea #2, last open AntFleet High) + install-skill-pack + skill-packs.json combined backport (aeon-agent — 12th consecutive same-day-after backport) + per-column refresh intervals (minitor — May-22 idea #4, last open May-22 idea) (May-24)**. **May-22 ideas FULLY CONSUMED.** **May-20 ideas FULLY CONSUMED.** **May-18 ideas FULLY CONSUMED.** **May-16 / May-14 / May-12 / May-10 ideas FULLY CONSUMED.** **May-6 aeon-agent backports FULLY CONSUMED.** AntFleet Issue #184: H1/H2/H3/H5/H6/H7/H8 closed (PRs #194–#197 + PR #206 + PR #226 + aeon-agent PRs #55 + #58 + AntFleet PRs). Open Highs remaining: H4 (.bak rollback in fleet-state), H9 (admanage-create skips campaignId-only ad sets) + ~13 Mediums + 2 Lows. Other open: Dashboard Live Feed (aeon-agent — older, complex). **May-24 ideas**: ecosystem-pulse skill (aeon), fleet-skill-adoption leaderboard (aeon), config-validator backport (aeon-agent), Bluesky AT Protocol column (minitor), column-level webhook notifications (minitor). **May-25 burned: idea #1 ecosystem-pulse (aeon PR #227) + idea #3 config-validator backport (aeon-agent PR #61) + idea #5 column webhook notifications (minitor PR #50).** Note: idea #4 (Bluesky column) was found ALREADY implemented + merged on minitor main. **May-24 ideas FULLY CONSUMED** (fleet-skill-adoption #2 merged in aeon as PR #245 on May-26). **May-26 ideas**: fleet-skill-adoption backport (aeon-agent), sparkleware-catalog skill (aeon), RSS/Atom column (minitor), incoming skill PR triage skill (aeon), deck version history (minitor). **May-27 burned: #1 (fleet-skill-adoption aeon-agent PR #64) + #2 (sparkleware-catalog aeon PR #252) + #5 (deck version history minitor PR #52).** **May-28 burned: #4 (pr-skill-triage aeon PR #259) + sparkleware-catalog backport (aeon-agent PR #66 — 15th consecutive same-day-after backport). May-26 idea #3 (RSS/Atom column for minitor) found ALREADY implemented in main (`lib/columns/plugins/rss/{plugin,server}.ts` + `lib/integrations/rss.ts`, parses both RSS 2.0 and Atom 1.0, registered in manifest.ts) — burned by pre-existing code, not by new build.** **May-26 ideas FULLY CONSUMED.** **May-29 burned: #3 fork-health-score skill (aeon PR #271) + pr-skill-triage backport (aeon-agent PR #68 — 16th consecutive same-day-after backport) + #5 column tab groups (minitor PR #53). May-28 idea #1 (capabilities + secrets_required in skill-packs.json) already in flight via aeon PR #268 (capabilities array) + PR #267 (secrets_required field) opened by antfleet-ops 2026-05-28 — skipped to avoid collision. May-28 idea #2 (ecosystem-pulse backport for aeon-agent) found ALREADY implemented on May-26 in the established backport chain — burned by pre-existing backport. May-28 idea #4 (Farcaster column for minitor) found ALREADY implemented in main (`lib/columns/plugins/farcaster/{plugin,server,client}.tsx`, brand accent #7c65c1, Neynar API with `NEYNAR_API_KEY` env fallback to public demo key) — burned by pre-existing code, not by new build.** **May-28 ideas FULLY CONSUMED.** **May-30 (no fresh repo-actions today — odd-day, repo-actions runs on even days)**: aeon picked Issue #301 (capabilities taxonomy parity — filed today by external contributor; took author's "cheapest option" direction #3 CI parity check via aeon PR #304) + aeon-agent ran 17th consecutive same-day-after backport of upstream PR #271 fork-health-score (aeon-agent PR #70) + minitor shipped per-column collapse to a 48px strip (PR #55 — pure UI + zustand, no DB schema, complements May-29 tab groups on deck-density axis). **May-31 burned: #1 token-report treasury wallet tracker (aeon PR #306, leverages May-29 .x402books/wallets.json that had zero consumers) + #4 upstream-gap skill (aeon-agent PR #72, **first non-backport feature build on aeon-agent in the chain** — the upstream-side analogue of fork-skill-gap, makes the same-day-after backport chain's silence between rounds an explicit weekly artifact) + #5 column data export JSON (minitor PR #56, all 47+ column types).** May-30 ideas #2 (capabilities coverage map) and #3 (PR merge-priority digest) carried — #2 deferred because skill-packs.json capabilities field still not landed on main (AntFleet PRs #267/#268 open), #3 deferred as medium-effort. **Jun-01 burned: #2 capabilities coverage map (aeon PR #313) + spend-monitor backport (aeon-agent PR #74, 18th consecutive same-day-after backport, from upstream PR #272 May-29). May-30 idea #2 prerequisite MET as of May 29 — PR #268 shipped skill-packs.json capabilities[] field + PR #304 added CI parity check. minitor: FEATURE_SKIP (May-30 idea #5 already shipped May 31 as PR #56; no remaining minitor ideas; no ai-build issues; no memory priorities tied to minitor — per skill step (e), skipped + no notification sent). May-30 idea #3 (PR merge-priority digest) still carried as medium-effort.** **Jun-02 burned: #3 pr-merge-queue (aeon PR #318 — last May-30 carry-over consumed) + follow-up-patrol backport (aeon-agent PR #76, 19th consecutive same-day-after backport, from upstream PR #272 May-29) + column quick-search (minitor PR #58).** **Jun-03 burned: #1 ecosystem-entrants (aeon PR #339) + pr-merge-queue backport (aeon-agent PR #79, 20th consecutive same-day-after backport, from upstream PR #318 Jun-02) + #4 column pinning (minitor PR #59). PR #272 backport queue: spend-monitor + follow-up-patrol done; three remain (narrative-convergence, mcp-pulse, fleet-scorecard). Jun-02 ideas #2 (wallet-risk-weekly, aeon) and #5 (skill-health-digest, aeon) still pending.** **Jun-04 burned: #2 wallet-risk-weekly (aeon PR #340) — last open Jun-02 idea for aeon other than #5 (which overlaps with existing `skill-health` skill — `skill-health-digest` deferred as redundant) + narrative-convergence backport (aeon-agent PR #80, 21st consecutive same-day-after backport, from upstream PR #272 May-29) + per-column duplicate (minitor PR #60 — 6th rung on per-column UX axis, no fresh minitor Jun-02 idea remained). PR #272 backport queue: spend-monitor + follow-up-patrol + narrative-convergence done; mcp-pulse + fleet-scorecard remain.** **Jun-05 burned: #2 atrium-catalog-watcher (aeon PR #342, supply-side weekly Atrium marketplace diff — first scheduled consumer of the install-from-atrium PR #335 endpoint, no signal until today on what new skills are publishable; pairs with sparkleware-catalog + skill-update-check as the three-weekly composition covering marketplace arrivals + curated registry + installed-skill drift) + #3 mcp-pulse backport (aeon-agent PR #82, 22nd consecutive same-day-after backport, from upstream PR #272 May-29 — PR #272 queue: spend-monitor + follow-up-patrol + narrative-convergence + mcp-pulse done; only fleet-scorecard remains, deferred per memory/instances.json dependency) + #5 column color labels (minitor PR #61 — 7th rung on per-column UX axis: 8-swatch preset palette + freeform hex input with server-authoritative `/^#[0-9a-f]{6}$/i` normalization; renders as dot next to title + accent gradient on collapsed strip; color IS inherited on duplicate unlike pinned). Jun-04 ideas #1 (`ecosystem-links` weekly link-health) and #4 (`$(date)` batch self-fix for 3 remaining sites in aeon-agent) deferred — #1 is a clean future weekly slot, #4 the smallest-effort next anti-pattern fix on aeon-agent (`repo-actions` line 29 + `star-momentum-alert` ~line 69's 3-site `for D in $(seq...)` block).** **Jun-06 burned: #1 ecosystem-links (aeon PR #351 — closes the three-skill ecosystem loop: pulse + entrants + links covering liveness + arrivals + URL validity; sandbox-aware INCONCLUSIVE two-strike rule; XONLY out-of-scope because X rate-limits HEAD requests and 429 reads identical to dead handle) + #4 $(date) batch self-fix (aeon-agent PR #83 — repo-actions line 29 + star-momentum-alert ~line 69's 3-site for-loop; repo-article line 26 was actually already fixed by PR #81 on Jun-04, so the actual remaining work was 2 sites not 3; closes the chain that started PR #63 / #67 / #71 / #77 / #81 — every known shell-substitution anti-pattern site is now fixed) + per-deck color labels (minitor PR #62 — deck-level analog of column color labels PR #61; reuses normalizeColumnColor + 8-swatch palette verbatim so the two surfaces stay coherent; operator color overrides active-deck brand color; schema additive deckColor field, no v2 bump). Jun-04 ideas FULLY CONSUMED.** **Jun-07 burned: #2 vigil-revoke skill (aeon PR #354 — closes the detection→revoke loop VIGIL PR #323 explicitly split out ('Bankr-gated, state-changing — separate PR.'); workflow_dispatch-only with strict `wallet:spender:token` triplet allowlist; Bankr ownership pre-check + pre-revoke `allowance` short-circuit-to-NOOP + post-revoke receipt poll + final allowance read so SUCCESS is chain-confirmed not Bankr-reported; capabilities external_api/writes_external_host/onchain_writes/sends_notifications) + #3 skill-of-the-day backport (aeon-agent PR #85, 23rd consecutive same-day-after backport, from upstream PR #341 Nurstar Jun-04 — daily meta-content skill emitting two notifications per workflow_dispatch (paste-ready tweet draft + live skill outcome via the picked skill's own notify); FIRST backport where `./notify` call style needed NO translation because upstream already used positional `$(cat ...)` argv style matching aeon-agent contract; PR #272 backport queue now CLOSED except fleet-scorecard deferred per `memory/instances.json` dependency) + #4 column width control (minitor PR #63 — 8th rung on per-column UX axis: narrow 240px / normal 360px / wide 480px three-step override selectable from More-menu dropdown with brand-Check on active row; VIEW-STATE only matching collapse/search/selected-tab pattern; default branch character-identical to prior single class so existing decks render pixel-identical until opt-in; mobile `<sm` clamps to `min(360px, calc(100vw-1rem))` regardless of width because 480px would overflow a phone viewport). Jun-06 ideas #1 (OAuth credential write-back, aeon) + #3 skill-of-the-day backport (aeon-agent, burned today) + #5 show-HN-draft auto-fire at 500⭐ (aeon) DEFERRED — #1 touches CORE files (aeon.yml + chain-runner.yml) at higher risk for autonomous PR than additive skill, #5 carried for natural cron of star-milestone post-500.** **Jun-08 burned: #5 show-HN-draft auto-fire (aeon PR #358 — last open Jun-06 idea; new step 8 in star-milestone reads `memory/topics/milestone-dispatch.json` rule map → fires `gh workflow run aeon.yml -f skill=<name>` fire-and-forget → writes dispatched timestamp atomically on success; seeded with `aaronjmars/aeon:500 → show-hn-draft`; silent-record milestones bootstrap/stale/deferred/skipped bypass dispatch entirely so fake-star bursts can't fire launch drafts; defense-in-depth idempotency via dispatched map + step 5a milestones.md already-recorded check; failure path mirrors skill-of-the-day convention with one recovery notification and no auto-retry) + ecosystem-links backport (aeon-agent PR #87, 24th consecutive same-day-after backport, from upstream PR #351 Jun-06 — weekly Monday URL-health audit; sandbox-aware INCONCLUSIVE two-strike rule prevents false-flagging operator's curation on sandbox-blocked outbounds; XONLY/Logos out of scope; closes two-of-three ecosystem loop on aeon-agent with ecosystem-pulse — ecosystem-entrants remains third leg, NOT yet backported; second backport in a row where `./notify` style needed NO translation — upstream already positional argv; atrium-catalog-watcher PR #342 Jun-05 skipped because aeon-agent does not yet have install-from-atrium script PR #335) + per-deck drag-to-reorder (minitor PR #65 — sidebar-level analog of the per-column DnD in deck-board.tsx; `reorderDecks` lived in store + server action since deck-list shipped but had no UI affordance to call it; GripVertical drag handle on the left of each deck label mirror of existing More button on right; pointer activation distance 4px guards click-vs-drag; restrictToVerticalAxis; no reorder-across-collapse-state restriction because open/closed is view-state-only; activate-on-click NOT triggered by drag; layout reservation constant via pl-7 mirror of pr-9; DnD ref on explicit div wrapper not SidebarGroup defensive against ref-forwarding edge cases on function components). Jun-06 ideas FULLY CONSUMED (#1 OAuth-writeback remains the sole non-burned idea, intentionally deferred as CORE-files-risky autonomous edit; would touch aeon.yml + chain-runner.yml).** **Jun-09 burned: #4 show-hn-draft prompt refresh (aeon PR #380 — timing-sensitive prompt-only refresh for the ~Jun-11 500⭐ auto-fire wired by yesterday's star-milestone PR #358; launch trigger framing 300⭐/12-day-horizon → 500⭐ auto-dispatch; project-scale shorthand 250⭐/90+ skills → 500⭐/195 skills/8 categories/onchain layer; body §2 hint at non-obvious capabilities pick-ONE) + #5 install-from-atrium script backport (aeon-agent PR #90 — first non-SKILL.md backport in the 25-PR chain, verbatim copy of upstream PR #335 Jun-03 install-from-atrium shell script; third skill install path after add-skill + install-skill-pack; unblocks atrium-catalog-watcher backport PR #342 explicitly skipped in yesterday's ecosystem-links backport because of this missing dependency) + #3 deck keyboard nav shortcuts (minitor PR #66 — 10th rung on per-column UX axis; j/k/c/// Esc with two-step Escape and `visibleColumnIds` as focus order; `pendingSearchOpen` is a column-id one-shot bridge to ColumnCard's local searchOpen state so fast `j / j /` sequence lands on second column; window listener bails on text-editable elements + modifier keys). Jun-08 idea #1 (Phase 2 Capabilities Declarations Sweep, ~150 skills, 1-2 days work) and #2 (ecosystem-entrants backport — natural 25th-link follow-up on aeon-agent; aeon-agent's slot today went to install-from-atrium because it unblocks more downstream work) deferred — natural future rounds.** See `articles/repo-actions-*.md`.

## Next Priorities
- **URGENT** Enable show-hn-draft in aeon.yml — PR #151 (May 1); at 490⭐ (10 from 500, projected Jun 11); next escalation Jun 10
- Enable pr-triage in aeon.yml — PR #147 (Apr 29); PR #143 from pezetel is first natural triage candidate
- Enable smithery-manifest in aeon.yml — PR #149 (May 1); submit docs/ to Smithery + MCP Registry
- Enable fork-cohort in aeon.yml — PR #152 (May 2); now 166+ forks — growing social proof
- Enable skill-freshness in aeon.yml — PR #157 (May 4)
- Enable skill-freshness in aeon-agent aeon.yml — skill backported (PR #30 merged May 5), not yet enabled
- Enable v4-readiness in aeon aeon.yml — PR #160 (May 6); dispatch manually pre-v4-announcement
- Enable fleet-state in aeon aeon.yml — first natural Monday run May 18; synthesises fork-cohort + fork-release-tracker + contributor-spotlight into one Monday digest
- aeon-agent: still 80 autoresearch-evolution rewrites pending (aeon PRs #46–#136)
- Run more digest types (HN, RSS, papers, DeFi)
