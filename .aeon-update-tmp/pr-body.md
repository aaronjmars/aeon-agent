## Upstream sync: `aeonfun/aeon` `3b4c5a3..bf33365`

**22 commits** (2026-09-01 → 2026-09-04) · **30 files applied** · **10 manual** · baseline → `bf33365`.

Auto-generated by the `aeon-update` skill. Framework changes are applied on this branch; operator-owned config and genuine conflicts are surfaced below for manual reconciliation. Merging this PR advances the sync watermark (`memory/topics/aeon-update-state.json` → `bf33365`).

### Applied cleanly
- **Modified skills:** `feature`, `pr-review`, `vuln-scanner`, `deploy-uni-hook` (Solidity templates — `_expectSwapRevert` scaffold fix + clean multi-hook simulation)
- **`eyebrowlock.json`:** 5 artifact entries (`vuln-scanner`, `pr-review`, `feature`, `deploy-uni-hook`, `.claude/skills/aeon`) refreshed to upstream HEAD's v0.4.2 lock — required because those skills' content now matches upstream HEAD and the `ci-skill-integrity` `eyebrow verify` gate (bumped to v0.4.2 in #1002) checks content hashes. Safe: each refreshed skill's tree content is byte-identical to upstream HEAD, and v0.4.2↔v0.4.1 hash-compatibility was confirmed across 72 unchanged shared skills.
- **New scripts:** `scripts/vuln-poc-gate.sh`, `scripts/dev-loop-pr.sh`, `scripts/skill_health_recovery.py`
- **New tests + fixtures (dormant):** `scripts/tests/test_{fleet_scorecard.mjs,llm_gateway.sh,skill_health_recovery.py,chain_runner_invalid_dispatch.sh,dev_loop_handoff.sh,vuln_poc_gate.sh}`, `scripts/tests/live_vuln_poc_gate.sh`, `scripts/tests/fixtures/vuln-poc-foundry/*` — _added but not yet wired: their CI hooks live in the conflicted `ci-tests.yml`/`aeon.yml`, so they don't run until those are reconciled._
- **Scripts / harness:** `scripts/fleet-scorecard.mjs` (non-skill runs no longer leak into per-skill reliability), `scripts/notify-deliver.sh` (per-channel delivery status), `scripts/skill_mode.sh`, `scripts/stage-vuln-scanner.sh`
- **Workflows:** `.github/workflows/chain-runner.yml`
- **Auto-merged (3-way):** `.github/workflows/ci-skill-integrity.yml` (eyebrow gate → v0.4.2) — your local edits kept, upstream's disjoint changes applied. `scripts/llm-gateway.sh` and `apps/dashboard/package-lock.json` also 3-way-merged but net-zero (your tree already carried the upstream change; `llm-gateway.sh` differs from upstream only by ASCII-normalized punctuation).
- **Docs / other:** `docs/CONFIGURATION.md`, `docs/ECOSYSTEM.md` (MythosForge removed), `docs/skill-integrity.md`, `.claude/skills/aeon/references/secrets.md`, `plugin/skills/aeon/references/secrets.md`

_No new skills this window, so catalogs (`skills.json`/`packs.json`) and `eyebrowlock.json` need no regen — the only upstream catalog diff is the git-derived `sha`/`updated`/`generated` churn that `ci-skills-json` normalizes out, and `eyebrow verify` allows content drift on the modified skills (no new egress host or CRITICAL). `AGENTS.md` verified up-to-date (`gen-agents-md.js --check`)._

### Needs manual review (conflicts — your local copy diverges from upstream)

**`skills/skill-health/SKILL.md` (held back — needs an eyebrow rescan):**
- Upstream `f3b3b29` (#1018, detect critical-incident recovery without waiting on lifetime success_rate) is a **clean 3-way merge** over your local `scorable: false` frontmatter line — no textual conflict. It was held back anyway because the merged file gets a new `eyebrow` `contentHash`, and this sandbox can't run the `eyebrow` binary (no pinned v0.4.2 sha) to produce it — shipping the merge without a matching lock entry would fail the `eyebrow verify` gate. **To land it:** re-apply the merge (or take upstream's `skills/skill-health/SKILL.md` and re-add your `scorable: false` line), then `eyebrow scan --path . --lockfile eyebrowlock.json` and commit.

**Workflows + CHANGELOG (operator-customized, upstream moved again):**
- `.github/workflows/aeon.yml` — upstream (dc1a975, 66711d3, 6dc0ad1, c92fbfb, bf33365) adds `GLM_REASONING_EFFORT` env passthrough (×3 blocks), a Foundry install step + PoC-gate execution evidence for vuln-scanner, `AEON_DISPATCH_ID` workflow-correlation, the block-style per-skill `model:` scheduler fix (#1024), and `actions/upload-artifact` → v7.0.1. Overlaps your local workflow customizations.
- `.github/workflows/ci-tests.yml` — upstream (dc1a975, 66711d3, c92fbfb, 66ab2b4, f3b3b29, bd74379) wires the 6 new test scripts above into CI. Add the `run:` lines you want once you reconcile the file.
- `.github/workflows/messages.yml` — upstream (dc1a975) adds `GLM_REASONING_EFFORT` passthrough.
- `CHANGELOG.md` — upstream (dc1a975, 69c6a17, e7ef463) prepends new entries; overlaps your local changelog.

**Package manifests (cosmetic only — safe to take either side):**
- `apps/dashboard/package.json` (158a4ca) and `apps/webhook/package.json` (bccb67c) — **dependency versions already match upstream HEAD.** The only divergence is key ordering (`lint`/`test` swapped, `eslint*`/`wrangler` repositioned) and em-dash escaping (`—` → `—`) from a prior local `npm install` reformat. No functional change; take upstream's copy if you want byte-parity.

**Carried from PR #209 (upstream unchanged this window, your local still diverges):**
- `llms.txt`, `.github/README.md`, `docs/skill-packs.md` — no upstream edits `3b4c5a3..bf33365`; these remain your pre-existing local customizations vs upstream. Reconcile at leisure.

### Operator config changed upstream (not auto-applied — reconcile by hand)
- `aeon.yml` — upstream adds a **`dev-loop` chain** (`workflow_dispatch`, `feature` → `pr-review`, `max_dispatches: 2`, requires an explicit `$chain_target`). Add it to your `chains:` block if you want the provider-neutral coding loop; your enable/schedule/model choices are untouched.

### Upstream commits
| SHA | Date | Summary |
|-----|------|---------|
| dc1a975 | 09-01 | llm-gateway(glm): pin reasoning effort to high (var-overridable) (#999) |
| 4f99198 | 09-01 | docs: remove MythosForge from ecosystem (#1010) |
| 69c6a17 | 09-01 | Bump eyebrow skill-integrity gate to v0.4.2 (#1002) |
| 66711d3 | 09-01 | feat(vuln-scanner): verify high severity claims (#1008) |
| f6d978b | 09-01 | chore(deps): bump tsx in /apps/cli (#1006) |
| 28e43dd | 09-01 | chore(deps-dev): bump @types/node (#1003) |
| 6dc0ad1 | 09-01 | chore(actions): bump actions/upload-artifact (#1011) |
| e7ef463 | 09-01 | Document optional runtime hardening for the Aeon MCP server (#1001) |
| 27a83d2 | 09-01 | chore(deps): hold eslint and globals majors (#1012) |
| 158a4ca | 09-01 | chore(deps): bump the dashboard group with 5 updates (#1014) |
| bccb67c | 09-01 | chore(deps-dev): bump wrangler in /apps/webhook (#1013) |
| c92fbfb | 09-01 | feat(chains): verify dev-loop pr handoff (#1009) |
| 6ae299f | 09-03 | chore(deps): bump qs in /apps/mcp-server (#1021) |
| de12993 | 09-03 | chore(deps): bump fast-uri in /apps/mcp-server (#1016) |
| c635427 | 09-03 | chore(deps): bump fast-uri (#1015) |
| 66ab2b4 | 09-03 | fix(chain-runner): stop a rejected dispatch from poisoning the reliability signal (#1017) |
| f3b3b29 | 09-03 | fix(skill-health): detect critical-incident recovery without waiting on lifetime success_rate (#1018) |
| bd74379 | 09-03 | fix(fleet-scorecard): stop non-skill runs from leaking into the per-skill reliability table (#1019) |
| 420dc85 | 09-03 | fix(notify): report per-channel delivery status and fail on total delivery failure (#1020) |
| d5f8717 | 09-04 | fix(deploy-uni-hook): scaffold comment points at _expectSwapRevert (#1022) |
| 339617b | 09-04 | fix(deploy-uni-hook): DeployHook.s.sol simulates cleanly for every hook kind (#1023) |
| bf33365 | 09-04 | fix(scheduler): read per-skill model: from block-style aeon.yml entries (#1024) |
