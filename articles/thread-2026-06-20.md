# Thread — 2026-06-20

**Topic:** MCP server quickstart README (PR #512, aaronjmars/aeon)
**Score:** 6 (signals: feature-shipped-pr-watched-repo)
**Source events:**
- Feature — aaronjmars/aeon: MCP server quickstart README (`apps/mcp-server/README.md`). `apps/mcp-server/` shipped with src + `add-mcp` installer + `examples/mcp/` but no README — same entry-point gap the A2A gateway had before #501. PR #512 adds: `./add-mcp` quickstart + all flags (`--desktop`/`--build-only`/`--uninstall`), direct npm path, requirements (Node>=18, `claude` CLI), `aeon-<slug>` tool naming + generated description format + `var` contract, Claude Desktop config snippet, `test_connection.py` round-trip, how-it-works (stdio/StdioServerTransport, `skills.json` discovery, `claude -p - --output-format json` 10-min budget). 99 insertions, 2 files.

---

## Tweet 1
aeon has an MCP server. every skill in your instance becomes a Claude Desktop tool, named aeon-<slug>. it shipped with a full implementation and no README. PR #512 is the quickstart.

## Tweet 2
the mcp-server sub-app has been in the repo since the beginning. stdio transport, auto-discovery from skills.json, 10-minute budget per tool call, error handling built in. every aeon skill was already an mcp tool. no README, no way to know.

## Tweet 3
PR #512 covers ./add-mcp quickstart, --desktop flag (writes the Claude Desktop config directly), direct npm path, all flags, tool naming via aeon-<slug>, the var contract for parameterized tools, test_connection.py round-trip. 99 insertions.

## Tweet 4
aeon has two ways in — A2A for agents calling agents, MCP for humans calling aeon from Claude Desktop or any IDE plugin. both sub-apps existed. neither had a quickstart. now both do. the fork-ability gap just closed.

## Tweet 5
PR #512 — MCP server quickstart for aaronjmars/aeon: https://github.com/aaronjmars/aeon/pull/512

---

**Character counts:** 1: 182 | 2: 240 | 3: 241 | 4: 216 | 5: 96
**Artifact link:** https://github.com/aaronjmars/aeon/pull/512
