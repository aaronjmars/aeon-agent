# Thread — 2026-06-17

**Topic:** validate-pack.sh — pre-flight validator for community skill packs (PR #495, aaronjmars/aeon)
**Score:** 6 (signals: feature-shipped-pr-watched-repo)
**Source events:**
- Feature — aaronjmars/aeon: validate-pack.sh (new, executable). Validates skills-pack.json: valid JSON, non-empty skills[], per-skill slug ([A-Za-z0-9_-]), path (no ..), present SKILL.md, capability values against the locked taxonomy → ERROR/exit 1. Capability taxonomy extracted at runtime from install-skill-pack's ALLOWED_CAPABILITIES array — no hardcoded copy. Advisory WARNINGs for missing fields, no LICENSE, off-vocab category, default_enabled:true on write/onchain/messaging skills.
- Pairs with PR template (#494, merged same day). Files: scripts/validate-pack.sh, CONTRIBUTING.md (pack section ref), docs/community-skill-packs.md (publishing-checklist step). PR #495.

---

## Tweet 1
aeon ships a pre-flight validator for community skill packs. validate-pack.sh — broken slugs, missing SKILL.md, invalid capabilities, path traversal — caught before you open a PR.

## Tweet 2
packs install into aeon via PR. the validator logic lived inside install-skill-pack.sh — Actions only. zero way to check locally before opening a PR.

## Tweet 3
validate-pack.sh runs the same checks as install-skill-pack locally. malformed JSON, bad slugs, path traversal (no ..), absent SKILL.md, off-taxonomy capabilities — all exit 1. capability taxonomy pulled from install-skill-pack at runtime, never hardcoded.

## Tweet 4
pack template (#494) and validate-pack.sh (#495) shipped back to back. the template says what to include. the validator says whether it's correct. together they close the pre-PR gap — submitters know before the maintainer looks.

## Tweet 5
validate-pack.sh for aaronjmars/aeon — PR #495: https://github.com/aaronjmars/aeon/pull/495

---

**Character counts:** 1: 175 | 2: 149 | 3: 256 | 4: 228 | 5: 92
**Artifact link:** https://github.com/aaronjmars/aeon/pull/495
