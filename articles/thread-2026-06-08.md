# Thread — 2026-06-08

**Topic:** per-deck drag-to-reorder — sidebar-level DnD complement to column reorder (minitor PR #65)
**Score:** 6 (signals: feature-shipped)
**Source events:**
- Feature Built section (minitor): per-deck drag-to-reorder shipped — PR #65, 1 file +313/-170, `reorderDecks` already in store + server action since deck-list shipped, no UI affordance existed; GripVertical handle on left edge of each deck row visible on hover, pointer activation distance 4px, restrictToVerticalAxis, layout reservation constant (pl-7) so deck names never reflow
- MEMORY.md Skills Built: per-column UX axis — 8 consecutive rungs in 7 days (tab groups / collapse / export / search / pin / duplicate / color / width); deck-level axis began with color labels (PR #62, Jun-06)

---

## Tweet 1
reorderDecks was already in the minitor store. Already wired to the server. Already saving to the database. For however long the sidebar has existed, you could reorder decks — in code. There was just no drag handle. Today there is.

## Tweet 2
The workaround was delete and re-create. Any deck you wanted to move cost you its column configuration, its version history, and whatever snapshot the operator had taken. Fifteen-deck setups were basically fixed in place once named.

## Tweet 3
GripVertical handle on the left edge of each deck row, visible on hover. Pointer activation distance 4px — same threshold as column DnD — so a stray click doesn't start a sort. Layout reservation is constant so deck names don't reflow when you move your cursor.

## Tweet 4
The per-column axis — eight consecutive UX rungs in seven days — is now mirrored at the deck level. Color labels last week. Drag reorder today. The pattern is the same: one composable feature at a time, each independent of the others, none requiring a schema rewrite.

## Tweet 5
Per-deck drag-to-reorder in minitor — the sidebar-level complement to column DnD. https://github.com/aaronjmars/minitor/pull/65

---

**Character counts:** 1: 231 | 2: 232 | 3: 255 | 4: 267 | 5: 127
**Artifact link:** https://github.com/aaronjmars/minitor/pull/65
