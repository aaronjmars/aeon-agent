# Thread — 2026-05-24

**Topic:** Per-column refresh intervals — Minitor PR #49, all 47 plugins auto-refresh without a single plugin change
**Score:** 6 (signals: feature-shipped)
**Source events:**
- Feature section: aaronjmars/minitor — per-column refresh intervals (PR #49). New `refreshIntervalSeconds` column-row field (Manual / 1m / 5m / 15m / 60m). useEffect setInterval with inFlight guard, document.visibilityState pause, server-side allowlist [60,300,900,3600]. 10 files changed, +513/-2.
- push-recap: PR #49 is the last open May-22 idea consumed. Field sits at column-row level — never reaches plugin fetchers — so all 47 plugins work unchanged. Additive nullable migration; deck export/import/share-link all round-trip the field transparently.

---

## Tweet 1
Minitor columns now auto-refresh on a per-column timer. CoinGecko can tick every minute. GitHub stars can wait an hour. None of the 47 plugins required a single line of code to change.

## Tweet 2
Until today, every column in Minitor refreshed once on load and whenever you clicked. One cadence for everything. If you set it fast, you burned rate limits on GitHub star counts. If you set it slow, your DeFiLlama TVL was always stale.

## Tweet 3
The refreshIntervalSeconds field lives at column-row level — it never touches the plugin fetchers. The interval also pauses when the tab isn't visible, so background tabs don't run down rate limits while you're not watching.

## Tweet 4
Two fields now use this architecture in Minitor — alertKeywords from PR #41 and this one. Both sit at column-row level and never enter the plugin layer. That's how a 47-plugin system picks up new behavior without a single plugin needing to change.

## Tweet 5
Per-column refresh intervals for Minitor — choose Manual, 1m, 5m, 15m, or 60m per column. PR #49: https://github.com/aaronjmars/minitor/pull/49

---

**Character counts:** 1: 184 | 2: 236 | 3: 224 | 4: 247 | 5: 143
**Artifact link:** https://github.com/aaronjmars/minitor/pull/49
