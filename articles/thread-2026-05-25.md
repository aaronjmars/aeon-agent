# Thread — 2026-05-25

**Topic:** Per-column webhook notifications — Minitor PR #50, keyword matches now POST server-side with an SSRF guard
**Score:** 6 (signals: feature-shipped)
**Source events:**
- Feature section: aaronjmars/minitor — per-column webhook notifications (PR #50). New `lib/columns/webhook.ts`: SSRF-guarded validator (https-only; blocks localhost + RFC-1918/loopback/link-local/CGNAT/multicast IPv4 + IPv6 equivalents) + bounded (5s), non-throwing, redirect-refusing sender. `persistFetchedItems` fires webhook for NEW matched items only. DeckExport deliberately omits `notifyWebhookUrl` (webhook URLs embed secrets; same export feeds the public share link). Migration landed as 0003 (not 0002 — PR #49 refresh-interval took 0002 first, per push-recap verification).
- push-recap: minitor #50 confirmed merged on main as migration 0003.

---

## Tweet 1
Minitor columns now POST to a webhook URL when a new item matches your keywords. The request fires server-side: 5-second hard timeout, no redirects followed, 11 IP ranges blocked before the payload leaves.

## Tweet 2
Until today, alert keywords in Minitor were passive. They'd highlight matching items in red when a fetch ran, but nothing happened automatically. The column sat there waiting for you to look at it.

## Tweet 3
Each column can now have a webhook URL. When a fetch brings in new matching items, the server fires a POST: {columnId, columnTitle, typeId, matches, timestamp}. Re-fetches don't re-notify — only genuinely new items trigger it.

## Tweet 4
The webhook URL is never included in deck exports or share links. It gets omitted deliberately — webhook URLs usually embed secrets, and the same JSON payload feeds the public /gallery share link. Security took the win over portability.

## Tweet 5
Per-column webhook notifications for Minitor — keyword match fires a POST. PR #50: https://github.com/aaronjmars/minitor/pull/50

---

**Character counts:** 1: 205 | 2: 197 | 3: 226 | 4: 236 | 5: 128
**Artifact link:** https://github.com/aaronjmars/minitor/pull/50
